# 项目状态与决策记录 STATE

> **给任何接手的智能体：先读这份，再动手。**
> 本文件的存在目的只有一个：**让判断力可以继承，而不只是让产物可以继承。**
> 最后更新：2026-09-18（数据集 **v0.5** / 规则集 **`ruleset_v0.1.9`**；**纵向面板已立项**，见 §6.6 与 **D42**；
> **分母研究已完成**，本项目首次有了生态率——活跃仓 6.2% / 存量 1.0%，见 `LIMITATIONS.md` **§26**；
> **100 份人工核对已全部闭环**，
> 四块改动落地；基座 511 → 516；`build_test` 82.8% 与 `boundaries` 85.7% 并列第一。
> 同日：**留出集 55 份盲判完成**（precision 92% / recall 70%，§19），已提交 `0839634`、
> Gitee + GitHub 双端 `ls-remote` 实测一致。
> 追加：三站**留出集补充评论**文案已就绪（`work/share-paste/{zhihu,oschina}-comment-03.txt`、
> `juejin-comment-02.txt`）——✅ **2026-09-15 三站全部已贴**（用户确认）。
> 同日二件：**留出集错标归因**完成（`LIMITATIONS.md` §20），并自披露一处
> **文档口径与实现不符**（主口径是否把「犹豫」计入错标）——**待人裁定，未改任何数字**）
> 09-14：英文短文**已在 dev.to 上线**（id `4649807`，12:23Z），发布后 18 分钟收到**首条非作者反馈**
> （`jo-do`）；回复文案 `work/share-paste/devto-reply-01.md`，**已由人贴出**（12:49Z）。
> 渠道裁决：**外网以 dev.to 为主**，HN 押后（hosts 绕过已落四台，见 `SHARE.md` §0）。
> 09-15：**包已发到 PyPI**（首日三版：`0.3.3` → `0.3.4` → **`0.3.5`**，Trusted Publishing 零 token，
> run `34959442365` / `34966005314` / `34966958406`），0.3.4 把 PyPI 页面换成**英文 README**并加了 `--version`；
> README / `SHARE.md` 里"包不在 PyPI""不能写 pip install"的旧话术已全部作废并改写（D36、§8.6）。
> 0.3.5 修 **PyPI 页面语言切换 404**——0.3.4 只把正文链接绝对化，**漏了顶部那条切换器**
> （`[中文](README.md)` 是相对路径，PyPI 不重写 → 跳回搜索页），现改绝对并加测试钉死（§8.7）。
> 09-15 晚：**0.4.0 —— 工具运行时零依赖**。语料随包改成 `.jsonl.gz`（标准库 gzip+json），
> `pandas`/`pyarrow` 降级为 `[parquet]`/`[test]` 附加依赖：`pip install agent-charters`
> 从**拖 62 MB** 变成**几百 KB 秒装**（wheel 219 KB → 104 KB）。理由与实测见 D37 / `SHARE.md` §8.9。
> 09-15 同日：**规则集升 `ruleset_v0.1.9`** —— 补两条词（正文禁令家族加 `绝不`、`overview` 标题词表加
> `这是什么`），起因是本仓库自己的 `AGENTS.md` 被自家工具判成 8/9，而那个 `boundaries` 还是靠一句
> "解释这个缺口"的话蒙到的。改动前先实测（`LIMITATIONS.md` §22.1）：**对 v0.5 标签中性**
> （558 份逐份标签与覆盖率零变化），新增的 14 份逐条读过 14/14 真命中。**v0.5 资产不重算**（D38）。
> 09-14 二件：**动作 tag `v1` 已建并双端推送** —— 此前 README / `action.yml` 一直让消费方写
> `uses: janzong/agent-charters@v1`，而两端远端**压根没有这条 tag**，那个引用会直接报错；
> 建完在**外部消费方仓库实跑验证**（D35）。同日发现并修掉 `refs` 的**对外措辞比实现宽**：
> 它只认 Markdown 指针，`scripts/check.sh` / `.env` 这类裸文件名从不进候选（`LIMITATIONS.md` §23）。
> 09-15 深夜：**量了「同一仓库两份章程的分歧」——真冲突只有 2.5%，但暴露出 `compare` 的一条真局限**：
> 38–49% 的成对里**有一份是指针/空壳**（`@./AGENTS.md` 13B、`Read CLAUDE.md` 734B），单文件判定会报 0/9、
> 给用户**错误结论**。⇒ 自我更正上一轮凭直觉推荐的选项 3（跨文件一致性），**改推「指针识别 / 跟随」**（D40、§25）。
> 09-15 深夜二：**「指针识别 / 跟随」已落地**（工具 `0.4.1`；当晚已发 PyPI，见下条）——
> `791` 份根级章程里 **111** 份符号链接、**75** 份纯指针（中位 **11 字节**、**69** 份现在报 0/9），
> 新模块 `pointers.py` 接进 `compare` / `brief` / Action 三处并**显式说明跟随**；
> `is_pointer`（数据集标签）一个字未动。`pytest` **185 passed**。
> 09-15 深夜四：**PyPI 第五版 `0.4.1` 已上线**（run `34987867624` 三 job 全绿，`gh workflow run`，不建 tag）——首个**用户可见的行为变化**：工具会跟随指针。默认索引干净 venv 约 2 秒装完、`compare` 对 11 字节的 `@AGENTS.md` 输出「已跟随」而不是 0/9；内容哈希与本地构建一致（见 `SHARE.md` §8.10）。
> 同日：**「对外主推哪条口径」已由人裁定：不动**（保持 92%/70%，D41）——三站更正评论不再追加；§20 那条挂了三天的事至此两项全清。
> 09-15 深夜三：**动作 tag `v1` 已移到 `6c25b06`**（按 D35 的移动规则：动了 `action.yml` +
> `agent_charters/`），并在**外部消费方仓库** `janzong/agent-charters-action-test` 实跑验证
> （run `34985605504`）：runner 日志 `Download action repository 'janzong/agent-charters@v1'
> (SHA:6c25b06…)`；新增的 `pointer-follow` job（`path: CLAUDE.md`，内容只有 `@AGENTS.md` 11 字节）
> **绿**，输出 `followed=1`、`missing=boundaries,workflow,style,environment,agent_meta,gotchas`
> （6 个，与 `AGENTS.md` 那份一致 —— **不是** 9 个）。
> 09-16：**新增决策 D42 —— 判断层与测量层解耦、纵向面板立项**（设计见 `work/longitudinal-plan.md`）：
> 面板 **vintage 2026-09**（558 仓）**只加期、不改框**；每期同一 schema、**老快照永不重标**；
> **死者留档**（404/归档/路径消失标 `dead|missing`，**永不删** —— 死者是对照组）；
> 预登记复评 **2027-09-10**。同期明确**减弱**的投入：对外文案/渠道迭代、`CLAUDE.md` 第二文档方向（D40 已推后）。
> 本轮讨论三条结论（"押格式无关对象"／"判断可变、测量不变"／"死者是资产"）均已落盘。
> 09-16 晚：**纵向面板落地并跑出第一个点**（`work/panel_run.py`；T0.5 = **2026-09-16**）——
> 全量 558 仓 12 批 GraphQL、约 1 分钟：**存活 558/558** ｜ 6 天内**内容改动 103 份（18.5%）** ｜
> 语义稳态 292 ｜ **模板簇 3 簇 / 35 仓**。三个簇**全是指针或空文件**（9 B 的 `CLAUDE.md` ×31、
> 0 B ×2、13 B 的 `.ai/AGENTS.md` ×2），且 T0 就已有 30/2/2 ⇒ §6.6 等的"机写簇出现"**尚未发生**。
> 顺带踩到两个管道坑并写进 `ENVIRONMENT.md` §9.10 / §9.11：GraphQL `text` 会把 NUL 改写成 `^@`（1/558）；
> `byteSize` 与 T0 的 `bytes` 口径不同（CRLF 文件差的就是 CR 个数）。
> 09-17：**季度 timer 已装**（`work/systemd/agent-charters-panel.{service,timer}` → `~/.config/systemd/user/`，
> `enable --now`、`systemd-analyze verify` 通过；ExecStart 同款命令行空跑验证）——
> 排程 3/6/9/12 月 10 日 03:30，**下一次 2026-12-10 触发 T1**；服务只写 tsv + sidecar + 日志、**不自动提交**，跑完通知 Hermes。
> ⚠️ 坑记在 `work/longitudinal-plan.md`：`Persistent=true` 会在首次启用时**补跑已错过的档期**，装前必须先补时间戳。
> 同日复核 dev.to：09-14 那 4 条回复**都已贴出**、**三天无人回应** ⇒ 判据 6 仍是 0；
> 对策稿 `work/share-paste/devto-selfcomment-01.md`（作者自评，把请求降成"贴文件我来跑"，**待人贴**）。
> 09-18：**分母研究完成（补上了本项目从来没有过的"生态率"）**——此前所有百分比分母都是"筛出来的 558 仓"。
> 两框实测：**活跃率（近 90 天有 push、非 fork、非 archived）`AGENTS.md` = 6.2%**（51/817，CI [4.8, 8.1]）、
> **存量率（全部公开仓库）= 1.0%**（9/924，CI [0.5, 1.8]）；世代等权 6.2% 按年化活跃仓数加权后 ≈4.6%，
> ID 均匀抽样的活跃子样本 1.8%（n=57，CI 宽，只能算方向一致）⇒ **结论区间约 2–6%**。
> 预登记判据落点：`<1%` 不成立、`>10%`"主流"不成立、`1–5%`"早期但可测"**成立（上沿略超）**；
> **趋势判据因"世代 vs 仓库年龄"混淆而失效** ⇒ 只交付"没测出来"，**不交付"没在扩散"**。
> 顺带三条硬事实：存量里 **93% 近 90 天没 push**、**fork 29%**、**空仓 8.3%**；
> **`CLAUDE.md` 在野外 5.4% vs `AGENTS.md` 6.2%（CI 重叠）**，59.1% 只是条件率，
> 野外 `P(CLAUDE|AGENTS)=35.3%` ⇒ **语料库偏向"多工具重型仓" 1.7 倍**（新样本偏差，已进 §26）。
> 产物：`work/audit/prevalence.md`（报告）+ `work/prevalence.py`（可复算，seed `20260918`，缓存离线）；
> `LIMITATIONS.md` **§26**。**未改数据集、未加字段、未动对外口径**（D41/D42 不破）。
> 坑已记 §26：`403` 有三种含义（主限流 / 次级限流 / **单仓 ToS 封禁**），第一版误判白卡几十分钟。
> 09-18 二：**章程有效性实验设计稿 v0 已出**（`work/audit/plan-efficacy.md`）——
> 讨论确认：描述层已进入边际递减，唯一剩下的 0→1 是"**章程到底有没有用**"（项目至今只测过"有多少"，
> 从没测过"有没有用"）。设计为**三臂随机对照**（T=给章程 / C1=什么都不给 / **C2=等长无关文档**，
> C2 用来剥掉"多给上下文"的一般效应），预登记判据含**负结论照发**与**判者一致率<70% 即作废**。
> ⚠️ **三个岔路待人裁定，裁定前不跑**：①"有效"怎么定义 ②谁当盲判者 ③人力预算。
> 并明确一条边界：**这个实验能答"章程有没有用"，但不能补上判据 5/6 的"非作者外部使用者"**——两回事。
> 09-18 三：**裁定已回填并开工（先付 0 小时轨）**——①有效定义＝L1 机械判违规为主 + L3 返工轮数为辅 +
> L2 人工盲判抽子集；②判定者＝人 + 另一模型**双判**，**一致率 <70% 实验作废**；③**先付 0 小时**：
> 只开**机队仪表轨**，跑 1–2 个月，相关性为正再投 1.5–2 小时做三臂对照；变体 D 留后手。
> 仪表轨已上线：`work/efficacy_log.py` + `work/efficacy-log.jsonl`（**同一张表将来直接装正式实验**，
> 正式实验只多一个 `--arm T|C1|C2`）。两条纪律写死：**不回填历史**、**每个真实任务都记不许挑**。
> 上线当天就记到**真违规**（我自己：`prevalence.py` 用了未声明的 `requests` → **同日改回标准库**
> `http.client` keep-alive；以及先提交后跑 pytest）——规矩是给智能体定的，智能体先犯，说明"写了不一定被遵守"
> 是真现象。`plan-efficacy.md` §8 裁定表 / §9 仪表轨口径。
> 09-18 四：**当晚自我修正了仪表轨的一个硬伤并补上执行点**——
> ①**主指标改掉**：原想拿"有章程 vs 无章程"当天然对照，但**机队只在自己那几个仓上干活、这些仓几乎都有章程**
> ⇒ 无章程那一臂**没有样本**，靠它做对照只会攒出空表。改为**"章程明写规则的合规率（ceiling check）"**：
> 不需要对照组、当天出数，且正好回答那个真问题。**预登记档位**：合规率 ≥95% ⇒ 天花板效应、
> **不做正式实验（省下 1.5–2 小时）**；70–95% ⇒ 值得做；<70% ⇒ 问题在"没被执行"，换方向。
> ②**规则登记表** `work/efficacy-rules.jsonl`（两条：agent-charters 9 条 ac1–ac9、rmas-v3 5 条 rm1–rm5），
> 每任务只填编号，不用现读章程；没登记的仓回退自由文本；无适用规则写 `--rules none`。
> ③**执行点**：写进 `~/.codex/AGENTS.md`（每次会话自动加载）——只写在项目文档里没用，文档不会自己进上下文。
> ④ `--charter` 字段保留但不再依赖。
> 09-19：**第 3 篇（分母研究）已在 dev.to 上线**（id `4692300`，08:30:25Z，
> 公开接口 200、series `AGENTS.md in the wild` 已在页面上确认）——内容＝活跃仓 6.2% / 存量 1.0%、
> `CLAUDE.md` 5.4% 并驾齐驱、93% 存量仓已死 / 8.3% 空仓，并主动交代趋势测不出来与两框不一致（区间 2–6%）。
> ⚠️ 撞到一个命令陷阱并已加防呆：`publish_devto.py --live` **不带 `--update` 会新建一篇**而不是把草稿转正，
> 故线上是 4692300、另留一个重复草稿 4692252（已改名标 `[副本·已发布，勿发]`；**API 删不掉**，
> `DELETE /api/articles/{id}` 是 404，要清只能在后台手删）。详见 `SHARE.md` 第 3 篇一节。
> 09-19 二：**作者自评请托贴了，但贴错文章**——稿子指定第 1 篇（`4649807`），实际落到第 3 篇
> （`4692300`，发布后 8 分钟、**0 阅读**）。各篇流量：第 1 篇 **57 阅 / 5 评** ｜ 第 2 篇 29 / 4 ｜ 第 3 篇 **0 / 1**。
> ⇒ 请托内容没坏但**挂在没有流量的页面**上、且与第 3 篇（讲普及率）语境接不上。
> 补救稿 `work/share-paste/devto-selfcomment-02.md`（按第 1 篇语境重写，引用该线程 `jo-do` 那句）**待人贴**。
> 详见 `SHARE.md`。
> 09-19 三：**补救请托已贴、贴对了页**（`4649807`，08:43Z）——`GET /api/comments?a_id=4649807`
> 实测已见 `janzong` 那条，顶层评论 4 → 5（页面口径 6 评）。09-19 二那次"贴错文章"到此闭环：
> 请托现在挂在**唯一有流量**的那篇上。页面侧阅读量仍 57、**非作者外部使用者仍 0**（判据 6 不变）。
> 09-19 四：**第 2 篇线上口径已更正**（`4650835`，PUT 实况确认）——09-14 发布时那句
> "Not on PyPI — the install is a clone" 在 09-15 发 PyPI 之后**就是假话**了，而这一篇正是教人
> 「先自己量一遍」的那篇（29 阅）。现改成 `pip install agent-charters`（0.4.1，wheel ~110 KB、
> 无运行时依赖）+ 一条 `Updated 2026-09-19` 的说明（保留"当初只有 clone 一条路"的诚实交代）。
> 实测：`GET /api/articles/4650835` 正文已换、`Not on PyPI` / `git clone` 零残留、页面 200、
> 合集 `AGENTS.md in the wild` 未被 PUT 摘掉（这是 `--update` 的已知坑）。
> 同日仪表轨开始进数据：**17 行里有 14 行跑在没有任何章程的仓里**（Hermes 侧真实任务自记）——
> 这修正了 09-18 那句"无章程那一臂没有样本"：**样本有，是多数**，只是它们填不出合规率的分母
> （没有明写规则就无所谓"违反"）。`report` 现在会拒绝这类比较并显式告警。

---

## 0. 接手入口（新智能体的第一动作）

按顺序读：`README.md` → **本文件** → `LIMITATIONS.md` → `TAXONOMY.md` → `ENVIRONMENT.md`。

读完后的第一件事：确认第 3 节的"不可争议区"，**不要重新辩论已决事项**。
然后从第 5 节的"下一步"里选一个动作开始。

**依赖前提**（缺任何一个都会卡住，详见 `ENVIRONMENT.md`）：
`gh` CLI 已认证 ｜ 项目 venv 已建 ｜ GitHub SSH key 已配 ｜ pip 需走国内镜像

---

## 1. 项目一句话

收集**人写给 AI 智能体的书面规约**（`AGENTS.md` 等），做结构化标注并公开。
数据集 v0.5 覆盖 558 份 `AGENTS.md`，并随包提供 `agent-charters` 命令行工具。

**它不是**：通用知识发现平台、agent 性能评测、新闻聚合。

---

## 2. 当前状态

| 项 | 状态 |
|---|---|
| 数据集 | ✅ **v0.5**（30 字段 / 516 份可统计，`ruleset_v0.1.8`）——**Release `v0.5` 已建**（GitHub + Gitee，2026-09-13；tag 指向 `84c17b9`，两端下载的 sha256 与仓库内逐字节一致）｜旧资产 `v0.2`/`v0.3` 仍在各自 Release（不静默替换，见 D18） |
| 规则集 | ✅ **`ruleset_v0.1.9`**（2026-09-15）：相对 v0.1.8 只多两条词——正文禁令家族 `绝不`、`overview` 标题词表 `这是什么`。**对 v0.5 标签中性**（`--verify-v05` 用改完的真实规则重算 558 份，逐份标签与内容模式完全一致，覆盖率仍 85.7% / 32.2%）；收益在未来采到的中文上（v0.6 中文 +6/+8，逐条读过 14/14 真命中）。**v0.5 行仍写 `ruleset_v0.1.8`**，不重算、不替换（D24/D38）｜遗留：无（正文通道「提到即命中」已量化，代价≈0、决定不修，见 §22.2）|
| 命令行工具 | ✅ `agent-charters stats / brief / compare / show / refs`（**工具 `0.4.1`（PyPI 已发；新增「指针识别 / 跟随」，见 D40）**；0.4.0 起运行时零依赖——语料以 `jsonl.gz` 随包，只用到标准库的 gzip+json；`--version` 自 0.3.4 起）——现在打印的基线是 v0.5 的 `boundaries` 85.7% / `build_test` 82.8%；**输出中英双语**（默认跟 `LANG`/`LC_ALL`，`--lang en\|zh` 可覆盖，见 D33） |
| PyPI | ✅ **09-15 首日起五版：`0.3.3` → `0.3.4` → `0.3.5` → `0.4.0` → `0.4.1`**：<https://pypi.org/project/agent-charters/>（最新 **`0.4.0`**：wheel 104 KB、**`Requires-Dist` 为空**——装包不再拉 pandas/pyarrow；MIT，10 条 classifier、4 条 Project-URL，`requires-python >=3.10`）。上传走 **Trusted Publishing（OIDC）**，仓库里**不存任何 token**；三道闸＝tag 与 version 一致 / 已在 PyPI 就跳过 / `twine check --strict`。run `34959442365`（0.3.3）· `34966005314`（0.3.4）· `34966958406`（0.3.5）· `34971331591`（0.4.0）三个 job 全绿。复验（0.3.3）：从**默认索引**下载的轮子 sha256 `b5b8f631…31d3` 与 PyPI 公布值逐字节一致**第五版 `0.4.1`（同日深夜，run `34987867624` 三 job 全绿）＝0.4.0 之后第一个用户可见的行为变化**：工具会**跟随指针**（`compare`/`brief`/Action 遇到 `@AGENTS.md` 这类 11 字节的指针文件时跟过去判目标，不再报 0/9；见 D40 / §25）。wheel 111,868 B / sha256 `059d3e1c…`；默认索引干净 venv **约 2 秒**装完、`--version` 0.4.1、指针跟随实测生效；内容哈希与本地构建一致（**轮子字节可复现不是承诺** —— zip 时间戳，同 §8.9 那条 gzip 坑）。实况见 `SHARE.md` §8.10 |
**0.4.0 独立复验**：默认索引 `pip install agent-charters==0.4.0` **4.3 秒**装完、`pip list` 里只有本包（轮子 104,851 B / sha256 `9754f371…4b6f4c`）；干净 venv 装完、换无关目录跑 `compare` 得 8/9。**「已在 PyPI 就跳过」已实跑验证**（重跑 run `34966176206`：preflight success、build/upload 直接 skipped）。页面语言：**PyPI 渲染 `README.en.md`（英文）**，GitHub 首页仍是中文 `README.md`，两边顶部互相带切换器——⚠️ 这个切换器**必须写绝对地址**（PyPI 不重写相对路径，相对链接＝404 跳搜索页，0.3.5 修的就是这条）。~~⚠️ 依赖（pandas+pyarrow ≈62 MB）从 251 直连仍会断流~~ → **0.4.0 已从根上解决**：运行时依赖归零，装包不再拉它们（D37）。|
| 测试 | ✅ `pytest` **168 passed**（含"数据集可由 raw 重放""跨哈希种子字节一致""发布校验和""词中命中不许打标签／词首前缀必须打标签"、指针反证闸三组、D33 的"en 输出零汉字 + 中文文案冻结"、D34 的 Action 判定层 5 条、随包/发布两份语料逐字节一致）；**没有 `data/raw` 时 150 passed / 18 skipped**（新克隆与 CI 就是这个形态——已实测，跳过而不是红） |
| CI / GitHub Action | ✅ **09-14 装机并首次跑绿**（`6fa8a8e`/`bb57b0f`，run `34855521055`）：三个 job 全 success —— `tests (py3.10)` **144 passed / 18 skipped**、`tests (py3.12)` 同样、`self-check` 打 8/9 并出 notice。⚠️ 真实 CI 里那 18 条跳过是**设计**（`data/raw` 不入库），不是漏跑；本地带 `data/raw` 是 **168 passed**：`tests` job 跑 py3.10/3.12 两套测试，`self-check` job 用**本仓库自己的 action** 检查本仓库的 `AGENTS.md`（`uses: ./`，即消费方路径 `uses: janzong/agent-charters@v1` 的同一链路）。判定层在 `agent_charters/gha.py`（可测）：①缺哪些类别 ②指向的路径还在不在；`fail-on-*` **默认关闭**（只报告），断链默认不拦——禁令清单里的路径不是断链（见 D34）；`refs` 只认 **Markdown 指针**，裸文件名不扫（§23）。**动作 tag `v1` 已发布，并在外部消费方仓库实跑通过**（2026-09-14，见 D35） |
| 本仓库第一份 AGENTS.md | ✅ **09-14 写**（按 `brief` 的九槽清单正常写，不是为工具定制）。**自家工具当场抓到自家**：`compare` 报 **8/9**，缺的 `overview` 其实写了——标题「这是什么」不在词表里；`boundaries` 一度也漏（写的是「绝不」，规则里只有「禁止」家族）。归因 + 最小对照 → `LIMITATIONS.md` §22，**没有为了变绿改文档措辞**（那是迁就分类器，`work/case-rmas-v3.md` 已吃过一次同样的教训） |
| 仓库 | ✅ https://github.com/janzong/agent-charters （public） |
| 国内镜像 | ✅ <https://gitee.com/janzong/agent-charters>（public；main + tag `v0.1`/`v0.1.1`/`v0.2`/`v0.3`/`v0.5` + 动作 tag `v1` 已对齐（双端 `ls-remote` 实测一致）；**Release `v0.5` 已建**，资产哈希与仓库内逐字节一致；SSH 专用密钥 `id_gitee`） |
| 首发 | ✅ 2026-09-11 知乎《我把 558 份 AGENTS.md 全抓下来标了一遍》<https://zhuanlan.zhihu.com/p/2081788025013539447> |
| 第二站 | ✅ **开源中国已上线**：<https://my.oschina.net/u/9764589/blog/19758304>（2026-09-12，未登录可访问）。更正状态：正文与平台 AI 摘要仍是 87%，两条更正评论（84.7% → **v0.5 最终版 82.8% / 85.7% 并列**）**均已贴**（2026-09-13，用户确认）。文案 `work/share-paste/oschina-comment-02.txt`（496 字符，≤500 限）。排版遗留不影响内容：5 个小标题只生效 3 个、代码块 0 个、正文 URL 未成超链接。 |
| 第三站 | ✅ **掘金已上线**：<https://juejin.cn/post/7684156210712166442>（2026-09-12；未登录可访问；6 个小标题全部生效、2 个代码块正常、链接被 `link.juejin.cn` 重写属正常）。正文用的是 **84.7%（推算错的那版）** → **更正评论已贴**（2026-09-13，用户确认；v0.5 定稿版，文案 `work/share-paste/juejin-comment-01.txt`）。遗留：作者显示名仍是默认「用户66114510929」，建议设昵称+头像。 |
| 站点情报 | 2026-09-11 站内搜 `AGENTS.md` 命中 6 篇博客，**清一色"教程/实战/心得"，没有同类数据资产**——差异化话术用"别人给观点，我给可复算的数据" |
| 口径裁决 | ✅ 2026-09-12 由人定四条定义边界（`TAXONOMY.md`「口径裁决」）：风格收 Git 规范 / 环境收版本号 / 构建测试收"该不该跑" / **AI行为不收标题里的 agent 字样**。前三条实测对语料库影响 0 份（纯定义），第四条 −35 份 |
| 数字更正 | ✅ **三站均已贴**（2026-09-13，用户确认）：`zhihu-comment-02.txt`（597 字符）/ `oschina-comment-02.txt`（496 字符，≤500 限）/ `juejin-comment-01.txt`（464 字符），按 **v0.5 定稿**（82.8% / 85.7% 并列）。⚠️ 三站评论区都走 JS/私域接口，本轮**未能自动复核**正文，按用户确认记录；内容源＝`SHARE.md` §7，由 `work/share-paste/make_comment.py` 抽取 |
| 留出集补充评论 | ✅ **2026-09-15 三站全部已贴**（用户确认；本机读不到——开源中国是 JS 壳、掘金评论区走 `interact_api` 且无签名路由，实测 http 200 但正文里没有评论）：`zhihu-comment-03.txt`（708 字符）/ `oschina-comment-03.txt`（464 字符，≤500 限）/ `juejin-comment-02.txt`（485 字符）。内容＝留出集 55 份盲判 precision 92% / recall 70%（§19），并自陈三个边界（单标注者、无中文、完全一致仅 12.7%）。事实源＝`SHARE.md` §7.5–7.7。⚠️ 措辞已按实测修正：可用池 422 = 516 − **94**（不是 516 − 106，见 §18 新增算术关系） |
| 错标归因 | ✅ **已闭环**（2026-09-13）：10 处错标（`environment` 5 / `boundaries` 5）全部落在两个机制——**标题词多义 5 处**（`dependency`/`tool`/`setup`/`config`/`rules`）+ **正文命中 5 处**（4 处「句式像禁令、语义不是禁令」+1 处配置文件名出现在流程句里）；证据都只 1–2 条。产物 `work/audit/v0.5-holdout-mislabel-attribution.md`（脚本 `mislabel_attribution.py`）。按 D11/§3.2 只归因不改规则 |
| 口径自披露 | ✅ **2026-09-15 修掉「对不上」那一半**：三个脚本现在**两条口径都打印**（留出集 92%/70% 与 97%/71%；in-sample A 组 90%/75% 与 97%/76%；中文那本早已如此），并把写反的抬头描述改对（此前写着「犹豫单列、不计入」，代码却一直计入；in-sample 的宽松口径更是**只算不打印**）。**未改任何对外数字、规则、脚本默认行为**。✅ **「对外主推哪条」已由人裁定：不动，沿用更保守的 92%/70%**（2026-09-15，见 **D41**）——三站更正评论与本仓对外数字保持一致，**不发**新一轮更正 |
| 中文留出集（v0.6） | ✅ **盲判 50/50 完成**（2026-09-13，`LIMITATIONS.md` §21）：15 个中文关键词 → 1065 个新仓库 → 抓到 **1046** 份 AGENTS.md（**1016 份判 `zh`**，v0.5 中文样本的 40 倍）｜随机抽 **50** 份（seed `20260913`，与基座零重叠）｜微平均 **precision 88% / recall 73%**（现行口径；文档口径 95% / 74%）｜文件级完全一致仅 6/50｜**最弱两类**：`agent_meta` recall **26%**（35 处漏 26，占全部漏标 30%）、`gotchas` precision 62% / recall 45%；`workflow` 错标最多（6 处）｜⚠️ 框偏向中文（97% 判 zh 是检索方式的产物，不是 GitHub 真实分布）；单标注者、未过第二人复核。**规则处置待人裁定（D32）：本轮不改任何规则**。 |
| 100 份人工核对 | ✅ **已闭环**（发现 1–56）。四块改动全部落地：①围栏语言标记门 ②删词与收紧 ③补词（净增 119/净掉 2）④收紧（净增 1/净掉 36）+ 指针反证闸。清单 `work/audit/v0.1.8-changelist.md` |
| 判据完成度 | **5 / 6** |
| 未闭环判据 | **至少 1 个非作者的外部使用者** |
| 外部信号 | GitHub **1 star＝作者自己**（`gh api .../stargazers` 实查 2026-09-14）、0 fork、0 watcher；掘金 **阅读 3 / 粉丝 0**；OSC 阅读 1；**dev.to**（09-14 12:23Z 上线）当晚 阅读 **1** / 反应 0 / **评论 1**。**判据 6「非作者的外部使用者」仍未闭环** |
| dev.to 首条外部反馈 | ✅ **09-14 12:41Z（发布后 18 分钟）**：`jo-do`（自称在跑一个「主要给 agent 用的 public board」）评论确认 85.7% / 13.6% 与其经验吻合，原话 *"The gotchas are the file"*，并称赞 rule-based 是 *"auditable beats clever"*。⚠️ **这是「外部读者」，不是「外部使用者」** —— 判据 6 要的是有人真跑 `compare`，不要把这条记成闭环。回复文案 `work/share-paste/devto-reply-01.md`（**已由人贴**，2026-09-14 12:49Z 确认上线，1319 字符、线程正确）|
| dev.to 合集 | ✅ **09-14 建**：`AGENTS.md in the wild`（`collection_id 44219`）→ <https://dev.to/janzong/series/44219>，已含本文。实现＝`PUT /api/articles/<id>` 带 `series` 字段，首次自动建合集；**更新时必须继续带**，否则被摘（已写进 `publish_devto.py` 注释与 `SHARE.md` §5.5）|
| dev.to 第 2 篇 | ✅ **09-14 上线**（id `4650835`）：*I looked at 558 AGENTS.md files: here's a 5-minute check for yours* → <https://dev.to/janzong/i-looked-at-558-agentsmd-files-heres-a-5-minute-check-for-yours-5cih>；tags `ai/agents/devtools/productivity`，同合集 `44219`（公网 200、四 tag 全渲染、合集生效）。主题＝读者自查，**零私库材料**；草稿源码 `work/share-paste/devto-article-02.md` |
| dev.to 第 2 条外部反馈 | ✅ **09-14 13:04Z**：`reidmarlow` 提出环境漂移类失败（未钉版本的 CLI/未 mock 的服务限流）才是真摩擦点，**正好命中实测的 8% 桶**；回复 `work/share-paste/devto-reply-02.md`（**已贴出 13:26Z**）|
| dev.to 评论盯梢 | ✅ **09-14 装机**：`work/share-paste/watch_devto.py` + systemd user timer `devto-watch.timer`（每 30 分钟，**只在有新评论时发 Hermes 收件箱**，无变化静默）。**为什么需要**：两条外部反馈都在发布后 25 分钟内出现，而评论 API 只读、回复必须人贴 —— 漏了就是白丢。⚠️ 单元里必须带 `Environment=PATH=%h/.hermes/hermes-agent/venv/bin:...`（systemd PATH 不含 hermes，实测不加就找不到命令）。状态 `~/.local/state/devto-watch.json`、全文日志 `~/.local/state/devto-watch.log`；卸载 `systemctl --user disable --now devto-watch.timer`。详见 `SHARE.md` §5.5 |
| dev.to 阅读量 | **2026-09-15 复查**：第 1 篇 **阅读 47 / 反应 1 / 评论 5**；第 2 篇 **阅读 19 / 反应 0 / 评论 4**
（09-14 当晚 22:1x 分别是 23/1/4 与 3/0/2）｜`watch_devto.py` 实读：「无新评论」 |
| dev.to 第 5 条评论＝垃圾广告 | ⚠️ **2026-09-15 查明**：第 1 篇 `2026-09-14T15:07:11Z` 的 `marcellusharris901`
是**链接广告**（"Declutter your Drive…" ＋ `tinu.be/…`），**不回**。上面「回复闭环」行说的
"四条外部评论"指 4 条真人评论（均已有作者回复），本条是那之后出现的第 5 条；`watch_devto.py`
首跑只建基线、之后只报未见过的 `id_code`，所以它不会反复报警（**别当漏回复去补**）|
| dev.to 回复闭环 | ✅ **2026-09-14 复核**：四条外部评论**全部有作者回复**（API `GET /api/comments?a_id=` 的 `children` 字段实读，不是"用户说已贴"）——`jo-do`←12:49:28Z、`reidmarlow`←13:26:53Z、`alexshev`←14:19:12Z、`raknaos`←14:19:46Z。四条回复文件的头注与本节各行的「待人贴」标记已同步订正（此前是**过期标记**，会诱导后续会话重复粘贴）。✅ 原注「仍待人贴的是留出集补充评论三条」**已于 2026-09-15 结清**（三站全部已贴，用户确认）|
| dev.to 第 3/4 条外部反馈 | ✅ **09-14 13:35Z / 13:42Z**（均在第 2 篇下）：`alexshev` 给出更干净的判据（"instruction 是否在关键时刻改变一次决策"）；**`raknaos` 真拿尺子量了自己的文件**并问两个具体问题——①清单有没有在 9/9 文件上跑过 ②坑的缺口是写作习惯还是复核习惯。两条都已**用数据实测作答**（见 SHARE.md §5.5），回复 `work/share-paste/devto-reply-03.md`（alexshev，944 字符）/ `devto-reply-04.md`（raknaos，1987 字符）**均已贴出**（14:19:12Z / 14:19:46Z）。⚠️ `raknaos` 自称有 agent 文件 ⇒ 是**判据 6 的潜在人选**，回复末尾已请他跑一次 `compare` |
| 评论盯梢（三个坑一晚踩完） | ⚠️→✅ **09-14 当晚连踩三次、全部修完**：①通知卡在 hermes 上被单元 `TimeoutStartSec=180` 杀掉，而 state 在通知**之前**就写好了 ⇒ 两条评论被静默吞掉（两小时后人工才发现）→ 改成「先落日志 → 通知成功才推进 state」，日志按 `id_code` 去重，超时 180→**420**（实测一次 `notify-hermes.sh` 真花 ~3.5 分钟，旧的 180 必然掐死它）；②**自家回复也会触发通知**（通知链路白跑一轮）→ 按文章作者名过滤，自家评论只记 state；③退出码 10「有新评论」被 systemd 记成 unit 失败 → 加 `SuccessExitStatus=10`。三条都有复验记录，详见 `SHARE.md` §5.5 |
| dev.to 个人资料 | ✅ **09-14 已由人填并复核**（`GET /api/users/me` 实读）：`summary` 140 字符 = 备用文案原文、`website_url` = `https://github.com/janzong/agent-charters`、`github_username` = `janzong`。⚠️ API 无写路由，以后再改只能手点；文案留档 `work/share-paste/devto-profile-setup.md` |
| 仓库 topics | ✅ 已加（agents-md / ai-agents / dataset / prompt-engineering / agent-instructions / llm） |
| 分类准确率 | ✅ **两组留出集都已出**（2026-09-13）：英文 55 份（`work/audit/v0.5-holdout-vs-rule.md` / §19）precision **92% / recall 70%**（TP 221/FP 19/FN 93）；中文 50 份（`work/audit/v0.6-cn-vs-rule.md` / §21）precision **88% / recall 73%**（TP 228/FP 30/FN 86）。in-sample 100 份为 precision A 90% / B 80% / D 92%、recall A 75% / B 79% / D 84%（`v0.5-human-vs-rule.md` / §16，**上界**）。两组与 in-sample 差 3–5pp、方向一致 ⇒ v0.5 未过拟合。最弱：英文 `gotchas` recall 32–38%、留出集 `environment` 57%；中文 `agent_meta` recall 26%、`gotchas` precision 62%。v0.1 自述口径已被取代 |
| 无标签率 | 1.6%（8 / 516，v0.1 时为 2.1% = 11 / 518） |

**当前最真实的问题：发出去了，但还没被看见。** 首站知乎已发（2026-09-11），
72 小时内的观察目标就是判据 6：出现第一个非作者的使用者或引用。
其余渠道的可达性见 `ENVIRONMENT.md` §5.1（V2EX/Reddit 需代理，HN 加 hosts 可通）。

---

## 3. 不可争议区（任何智能体都不应改动）

改动以下任一条 = 走偏。若确有证据需要推翻，必须在第 6 节新增一条决策并说明理由。

### 3.1 法律边界

- **不发布原文全文**。只发布衍生标注、统计特征、极短引用。被抓文件版权归各仓库原作者。
- 数据集**不得**包含 `data/raw/` 下的任何原文内容。该目录已在 `.gitignore` 中。

### 3.2 已否决的方向（不要再提）

| 方向 | 否决理由 |
|---|---|
| 加回 `decisions` 类别 | T0 实测仅 3% 命中，真实数据里几乎无人写决策记录 |
| 放宽 `agent_meta` 定义 | T0 曾达 79% 命中，但那是文件名污染造成的假象，收紧后才降到 27–36% |
| 为准确率无限迭代分类器 | v0.1 目标是验证管线，不是学术级精度。精确率高、召回率偏低是**刻意的**取舍 |
| 抓 10000+ 份数据 | 边际信息价值极低，且会把周期从小时级拖到天级 |
| 先写论文 | 论文是果不是因。没有外部使用者，论文只会无人引用 |
| 做项目新闻摘要 / 排行榜 | 快速过期，无研究价值 |

### 3.3 判据（不可放宽）

抓不满数量 → **改策略**，不降低数量标准。抽不出类别 → **改分类法**，不放弃分类。

---

## 4. 决策记录（带理由）

> 格式：决定 ｜ 理由 ｜ 被否决的替代方案

| # | 决定 | 理由 | 否决的替代 |
|---|---|---|---|
| D1 | 先做具体研究对象，不做通用平台 | 小切口可验证，平台性无法事前证明 | 直接复制 rmas-v3 做第二实例 |
| D2 | v0.1 只做 `AGENTS.md` 一种类型 | 目标是验证管线，不是覆盖最大化 | 一次做 5 种文件类型 |
| D3 | 规模定 300–600 份 | 够统计，且小时级可完成 | 抓 10000+ 份 |
| D4 | 删 `decisions` 类，增 `gotchas` 类 | 实测：前者 3%，后者真实存在却被完全漏掉 | 保留原分类法 |
| D5 | `agent_meta` 收紧为"明确规定 AI 自身行为" | 79% 命中是文件名污染造成的假象 | 保持宽定义 |
| D6 | 增加全文补救通道 | 无标题文件（纯列表式）在标题通道下必然漏标 | 只做标题匹配 |
| D7 | 用 `is_pointer` 字段而非新类别 | 转引用是"无内容"标记，不是内容类别。`categories` 为空是正确的 | 新增 `pointer` 类别 |
| D8 | 一行 = 一份文件 | 条目级切分有主观性 | 一行 = 一条规则 |
| D9 | 版本用两节（0.1 → 1.0） | 数据集的 PATCH 位无真实用途；两节语义可判定 | 三节 SemVer |
| D10 | 主发布 GitHub + ModelScope | HF 官方站实测不通（详见 ENVIRONMENT.md） | 死磕 HuggingFace |
| D11 | 接受"高精确率、低召回率" | 错标污染统计，漏标只损失召回 | 追求高准确率 |
| D12 | 用 GraphQL 批量探测抓取 | 实测可绕开 `code_search` 10/min 限流，且配额几乎不消耗 | 逐仓库调 contents API |
| D13 | **下一步优先级：工具 > 质量 > 推广** | 工具能产生"反复使用"，加数据边际价值已很低 | 先加数据 / 先优化准确率 |
| D14 | 分类规则收进 `agent_charters/` 包，`work/extract_v1.py` 改为调用它 | 工具与数据集两套规则 = 迟早对不上；单一事实源是前提 | 维持脚本内联规则 |
| D15 | **强模式通道总是运行**（不再只靠标题） | 中文文档被整体漏标，`compare` 对中文给出错误的 4/9——**给出错误结论的工具比没有工具更糟** | 继续只做标题匹配 |
| D16 | 假阳性一律写进回归测试 | `make \w+` 命中 "make sure"、`gradle \w+` 命中 "Gradle 9"；关键词法会持续引入这类错误，必须锁住 | 改完就算 |
| D17 | 引入 `ruleset_version`（与 `taxonomy_version` 分离） | 九类定义没变但规则变了，两个数字不同的数据集顶着同一版本号无法机器校验 | 只改关键词、不记版本 |
| D18 | 发 v0.1.1 而不是静默改 v0.1 | 已公开的 Release 资产不应被悄悄替换；数字变化必须留痕（`build_test` 79%→87%） | 覆盖 v0.1 资产 |
| D19 | 不把 `Repository Guidelines` 标为 `overview` | 模板级标题（30 份），不携带内容信息；违反"宁可漏标" | 关键词能命中就加 |

---

## 5. 路线图

### 第一优先：从数据集变成工具

让数据集产生**反复使用的理由**。最小形态：

```bash
agent-charters compare <你的AGENTS.md>
# 输出：你的 9 类覆盖 vs 558 份基线、缺什么、同类项目怎么写
```

**判据**：你自己在写下一份 `AGENTS.md` 时会主动用它。

### 第二优先：可发现性（技术侧）

- ~~给仓库加 topics~~ ✅ 已完成
- 补标准 dataset card
- 同步 ModelScope（需账号）

> 技术侧到此基本做完。**剩下的只有人能做的：把链接发出去。**
> 0 star / 0 流量的状态不会因为再改代码而改变。

**注意**：技术侧只能提高"被找到的概率"，真正的发现靠人分享。

### 第三优先：质量（v0.5 起的现状）

- ~~中文关键词补全~~ → v0.1.1 / v0.1.5 已补两批（含繁体与日文汉字形），仍是关键词法，长尾未清
- ~~人工校验 100 份~~ ✅ **已闭环**（发现 1–56，四块改动全部落地）
- ~~新一批 100 份的"规则 vs 人工"逐类对照~~ ✅ **已出**（`work/audit/v0.5-human-vs-rule.md`，脚本 `human_vs_rule_v0.5.py`；微平均 precision 80–92% / recall 75–84%，in-sample 上界，见 `LIMITATIONS.md` §16），并已写进 v0.5 Release 说明
- ~~留出集估计~~ ✅ **已闭环**（2026-09-13）：55 份盲判全部完成（只看原文、不看规则输出），
  逐类对照 `work/audit/v0.5-holdout-vs-rule.md`，脚本 `holdout_vs_rule_v0.5.py`；
  结论 precision 92% / recall 70%，与 in-sample 差 3–5pp ⇒ 未过拟合，见 `LIMITATIONS.md` §19。
  ⚠️ 中文留出集仍为 0（25 份全用掉了），中文准确率只能靠新采集，见 §18
- ⏳ 扩展文件类型：**2026-09-15 已实测增量**（`work/audit/filetype-probe.md`，脚本
  `work/audit/filetype_probe.py`；样本 482/558＝86%，其余是 api.github.com 经代理的 `IncompleteRead`，
  失败不写缓存、重跑即补齐）。四条关键数：①**同类文件是"同一批仓库的第二份文档"，不是新仓库**
  （482 份里 0 份是"只有 `CLAUDE.md` 没有 `AGENTS.md`"——样本本来就是按"有 `AGENTS.md`"选的）；
  ②`CLAUDE.md` 出现在 **285 个仓库（59.1%）**、715 个文件，扣掉 **192 个符号链接**与 7 个逐字节相同
  ≈**516 个不同文件**（与现有实质语料同量级，但更短：中位约 1.7 KB vs 3.8 KB）；
  ③**去重是前置功课**——`CLAUDE.md` 27% 是符号链接，`AGENTS.md` 自己也有 29% 是根文件的副本/链接，
  现有管线不解析链接（trees API 的 `mode=120000` + blob `sha` 可与数据集 `file_sha` 逐字节比对）；
  ④**310 个仓库（64%）有 ≥2 类同类文件** ⇒ 「其它方向③跨文件一致性检测」有真实底座。
  **后追加实测（2026-09-15）：三选一其实是假的——选项 3 已被数据打掉。** 抽 40 对下正文逐对读
  （`work/audit/pair-divergence.md`，脚本 `pair_divergence.py`，只读）：**真冲突只有 1 条 / 2.5%**
  （`Tencent/AI-Infra-Guard` 的 pip vs uv，属"过期命令"），38% 的成对里**有一份是指针/空壳**
  （174 对全量：**49% 有一边 <400B、36% <100B**）。真正普遍且直接砸到我们头上的是——
  **`compare` / Action 是单文件判定**，对着 13B 的 `CLAUDE.md`（`@./AGENTS.md`）或 734B 的 routing-stub
  会报 **0/9**，给用户的是"你这章程很空"这种**错误结论**。**改建议：先做「指针识别 / 跟随」**
  （工具行为，不动数据不动规则，可单独发 0.4.x），选项 2 / 3 推后。见 `LIMITATIONS.md` §25 与 **D40**。
  **→ ✅ 同日落地（工具 `0.4.1`，已发 PyPI，见 `SHARE.md` §8.10）**：新模块 `agent_charters/pointers.py` 接进
  `compare` / `brief` / GitHub Action 三个入口（`work/audit/pointer-census.md`）——
  判定＝薄 + 在指路 + 自己没有规则，跟随必须**说出来**（"这份是指针，下面的数字说的是 X"）。
  动的是**工具行为**，`is_pointer`（数据集标签）**一个字没改**。已知漏判 1 份（Moped，选择不放宽）。
  ~~原三选一（不做 ／ 只做 `CLAUDE.md` 第二文档 ／ 做「跨文件一致性」）~~ 仅作历史记录。
- ~~留 v0.1.9/v0.1.10 的三项~~ ✅ **2026-09-15 全部清完**（"改规则前先实测"的三条都测完了）：
  **h1 守卫**与**模块名型标题守卫**＝实测后否决（掉 ~37/~136 个真标签、或误伤真环境文档，
  数字见 `work/audit/v0.1.8-changelist.md` §A/B.3）；**全文兜底门放宽**＝量化后**决定不动**
  （`LIMITATIONS.md` §24，脚本 `work/audit/fulltext_gate_audit.py`）：`le1_sections` 只补 7 个标签
  （4 真 / 1 边界 / **2 假**）、中文 0 个，且**不是标签中性**（会改动已发布的 v0.5 标签 ⇒ 按 D18
  要重切数据集）；上界 `always` 补 130 个，但在盲判样本上真:假 ≈ 14:15。见 **D39**

### 本次会话（09-13）留下的下一步队列（按性价比）

1. ~~三站补一条留出集评论~~（智能体可写文案、人负责贴）：✅ **文案已就绪 2026-09-13**
   —— `zhihu-comment-03.txt` / `oschina-comment-03.txt` / `juejin-comment-02.txt`
   （源＝`SHARE.md` §7.5–7.7，经 `work/share-paste/make_comment.py` 抽取，三份均过字数限制）。
   内容＝留出集 55 份 precision 92% / recall 70%，与 in-sample 差 3–5pp，并主动交代
   单标注者/无中文/完全一致 12.7% 三个边界。✅ **2026-09-15 三站全部已贴（用户确认）——这条队列项结清。**
2. ~~v0.6 首选：新采 30–50 份中文章程做中文留出集~~：✅ **已闭环 2026-09-13**
   —— 新采 **1016 份**中文 AGENTS.md（新管线 `work/discover_cn.py` → `work/fetch_cn.py`，
   落在 `data/raw/cn/`，与 v0.5 语料库零重叠），随机抽 50 份、**逐份盲判 50/50 做满**
   （`work/audit/v0.6-cn-calls-a1..a10.json`），结果 **precision 88% / recall 73%**
   （现行口径；文档口径 95% / 74%），已写入 `LIMITATIONS.md` §21。
   两类暴露最明显：`agent_meta` recall 26%（"第二人称规定"写法整类漏）、`gotchas` 双向最弱。
   **规则要不要改属人裁定（D32），本轮一字未改。**
3. ~~错标归因（只记录，不建议改分类器）~~：✅ **已闭环 2026-09-13**
   —— `environment` 5 处（`owncloud/notes`、`leon-ai/leon`、`NateBJones-Projects/OB1`、
   `deanpeters/Product-Manager-Skills`、`coleam00/Archon`）与 `boundaries` 5 处
   （`morganlinton/Albatross`、`youssefvdel/qwengate`、`shikokuchuo/secretbase`、
   `okwasniewski/MiniSim`、`yashdev9274/supercli`）逐条读过原文，**全部落在两个机制**：
   标题词多义 5 处 + 正文命中 5 处（详见 `LIMITATIONS.md` §20 与
   `work/audit/v0.5-holdout-mislabel-attribution.md`）。**顺带撞出一处文档口径与实现不符**
   ——主口径是否把「犹豫」计入错标（92%/70% vs 97%/71%），**待人裁定**，已记进 §20，
   未改任何数字。按 D11 / §3.2，高精确率低召回是刻意取舍，本轮**无任何规则改动**。
4. ~~工具真判据仍未验~~：✅ **已做 2026-09-14** —— 用 `brief` 的九槽清单从零写了 rmas-v3 的
   `AGENTS.md`（命令逐条核实 + 6 条现查仓库才挖到的坑），案例见 `work/case-rmas-v3.md`。
   结论：**能当清单用，不能直接当尺子** —— 照 `brief` 给的槽位名写标题时，`compare` 会漏判
   `agent_meta`（九类里只有这一类；对照实验 5 组，其余八个槽位名都认得）。
   仍是**作者自测**：判据 5/6 的「非作者的外部使用者」缺口**未变**。
   章程正文未入库（rmas-v3 是私有仓库，Gitee API 404 / 网页 403）。
   案例里有一条可复用做法：把草案放进**被描述仓库的镜像目录**（顶层软链 + 一个 `.git`），
   `refs` 就能真核验断链——实测 17 个指向 15 ✓ / 1 ~ / 2 ✗，其中 2 个 ✗ 是 `refs` 分不清
   「去读这个」与「别提交这个」（禁令清单里的路径），已记入案例。
5. ~~英文渠道建议新写短文~~：✅ **短文已写好 2026-09-14** —— `work/share-paste/devto-article.md`
   （v0.5 + 英中两个留出集数字，不带三站更正尾巴；尾部带「找 2–3 个非作者使用者跑 `compare`」的公开请求）。
   渠道选 **dev.to**（实测：Reddit 从 251 全线路超时；dev.to 直连 200 且有正式发文 API），
   发布脚本 `work/share-paste/publish_devto.py`（默认发草稿，`--live` 才上线）。
   ✅ **已上线 2026-09-14 12:23Z**（id `4649807`）：
   <https://dev.to/janzong/i-labeled-558-agentsmd-files-heres-what-they-say-and-what-almost-nobody-writes-down-34gb>
   —— 公网 200，tags `ai/agents/opensource/data` 四枚全部渲染，description 与 og:title 正确。
   key 走 `~/.devto_api_key`（0600，只读不打印）。~~**user 侧待办：发完后去 dev.to 轮换这枚 key**~~
   ✅ **09-15 已闭环**：旧 key 已在 dev.to 侧删除，当前只剩一把写在 `~/.devto_api_key`，
   `GET /api/users/me` → 200 `janzong`，全库无旧值残留（`SHARE.md` §… dev.to key 卫生）。
   ⚠️ 更正一条我 09-14 的口头结论：HN 主站**可达**（只 DNS 污染，pin `209.216.230.207` → 200），
   此前说「IP 级封锁」是因为用了 AliDNS 给的过期 IP —— `SHARE.md` §0 的 hosts 说法是对的。

### 推广摩擦清单（2026-09-14 定，按性价比）

**判断**：当前卡点不是"内容不够"，而是**"看到 → 用上"之间有三道坎**（README 首屏讲 pandas、
包不在 PyPI、没有"什么时候该用它"的场景）。所以最高杠杆不是再发文章。

| # | 动作 | 状态 |
|---|---|---|
| 1 | **README 首屏改成工具优先**：安装 + 一条 `compare` + 真实输出（`openai/openai-agents-python` 6/9） | ✅ **09-14 完成**（`0da259a`，GitHub/Gitee 双端实读确认）｜两项自检：嵌入输出可复现、**不引用 `data/raw/` 路径**（该目录 gitignore，读者跑不出来） |
| 2 | **让读者一条命令装上** | ✅ **09-15 收口（0.4.0）**：运行时依赖归零——`pip install agent-charters` 现在是几百 KB 秒装，不再拖 pandas+pyarrow 62 MB（国内常因此断流）。原始记录：✅ **09-14 完成（不注册 PyPI）**：`pipx install "git+https://gitee.com/janzong/agent-charters"` ——清空虚拟环境实测 **11.4s**、parquet 打在包里（424 KB）、换目录可跑，README 首屏已改。**PyPI 已通**（2026-09-15 已定：注册并发布，见 2b 与 D36）——现在 `pip install agent-charters` 是可用的对外路径 |
| 2b | 发 PyPI（可选） | ✅ **09-15 完成，当天迭代到 `0.3.5`**（`d28b225`/`7ba120b`/`8a39d4c`，run `34959442365` / `34966005314` / `34966958406`）：**Trusted Publishing / OIDC，零 token**；<https://pypi.org/project/agent-charters/>。0.3.4 把 PyPI 页面换成英文 README + 加 `--version`；**0.3.5 修页面语言切换 404**（相对链接漏了一条，见 §8.8）；**0.4.0 砍掉运行时依赖**（见 D37）。pending publisher 五字段由人填；账号侧 2FA＝TOTP（「密码」App）+ 恢复码；复验见 D36 与 `SHARE.md` §8.6–8.8 |
| 3 | **GitHub Action**：PR 里跑 `compare`（缺 gotchas/agent_meta 提示）+ `refs`（指向的路径是否存在） | ✅ **09-14 完成**（`action.yml` 复合动作 + `.github/workflows/charter.yml`）。给外部仓库用：`- uses: janzong/agent-charters@v1` + `with: {path, fail-on-missing, fail-on-dangling}`；本仓库自己也在用（`uses: ./`）。**默认只报告不拦**（见 D34）。命中判据 6 的"反复使用"——但**别人仓库里跑起来才算数**——2026-09-14 用一次性外部消费方仓库（`janzong/agent-charters-action-test`）验证了 `@v1` 的解析、默认只报告（绿）、开启即拦（红+exit 1）三条行为（见 D35），**那是自建 fixture，不算判据 6** |
| 4 | 对语料库里 558 个仓库做"免费体检"外联 | ⏳ **需人裁定**（公开外联、有 spam 风险）。做法：挑 10 个、逐条个性化、给具体结论不写"来 star" |
| 5 | **CLI 输出中英双语**：英文渠道导来的读者不该在中文输出前止步 | ✅ **09-14 完成**（`b30a39f`，工具 0.3.3，138→150 测试）。默认跟 `LANG`/`LC_ALL`（非 zh 环境＝英文），`--lang en|zh` 可覆盖；中文输出与改造前**逐命令 diff 一致**（只有 `brief` 清单里的槽位问句由英文改回中文，见下） |

**不建议做的**：给章程打"质量分"——会诱导优化分数而非文件，与 `LIMITATIONS.md` 的克制冲突。

**其它方向（启发）**：①gotchas 采集流水线（从 CI 失败日志/issue 挖坑，解决"没人写坑"）②纵向测量
（已有 `baseline-2026-09-10.tsv` + `longitudinal.py`，等到 2026-12 才有数据，但是"机器批量写章程"
唯一的测量方法）→ ✅ **2026-09-16 立项**（**D42**，设计 `work/longitudinal-plan.md`：按季运行、**只加期不改框**、
每期同一 schema、**老快照永不重标**、**死者留档**、`2027-09-10` 预登记复评）③~~跨文件类型一致性检测（同一仓库 `AGENTS.md` 与 `CLAUDE.md` 是否互相矛盾）~~ → ❌ **2026-09-15 实测不成立**：真冲突仅 2.5%（`LIMITATIONS.md` §25）；同一次测量改推的替代项是「**指针识别 / 跟随**」

### 需要人类做的事（智能体做不了）

- 对外分享：Show HN / Reddit / 知乎 / V2EX
  → **文案已写好**：见 [`SHARE.md`](SHARE.md)（四版文案 + 发布顺序 + 预期质疑答法）
- 机器发帖会被当作 spam，必须由人来做
- ModelScope 同步（需账号）
- ~~**规则要不要改（D32，两处待裁定）**~~ ✅ **2026-09-15 已裁定并落地**：走 §22.1 的**选项 1**
  （加 A+B），规则集升 **`ruleset_v0.1.9`**，见 **D38**。依据＝先实测再改：对 v0.5 标签中性
  （558 份逐份标签、内容模式、516 份覆盖率零变化），新增的 14 份逐条读过 **14/14 真命中**；
  `绝对不能做` 收益≈0 ⇒ 不收。本仓库 `AGENTS.md` 因此从 8/9 → **9/9**，且 `boundaries` 由假命中变真命中。
  ✅ **同日把最后一条挂账也用数据关掉了**：正文通道的「**提到即命中**」（§22）——一句*讨论*禁令措辞的
  话也算一条禁令——**代价实测≈0，决定不修这个机制**（`LIMITATIONS.md` §22.2）：保守判据下五个语料
  （v0.5 442 份 / v0.6 中文 756 份 / 三组人工盲判 43+38+33 份）**没有一份**靠这种句子拿标签；
  故意放宽到荒唐的判据也只有 1.4% 的上界，且在盲判上「消 1 假阳性 / 丢 1 真标签」净变化 0。
  维护口径：改词表走 §22.1 的流程（`--verify-v05` + 人工读新增上下文），**不给正文通道加语义判别**。

### 工具侧已完成（v0.1.1）

- `agent-charters compare` 可用，中文/英文文件均已实测
- **真正的验收判据仍未完成**：自己写下一份 `AGENTS.md` 时会不会主动用它
  —— 这是唯一能证明"工具有用"的证据，目前没有

---

## 6. 新增决策（后续智能体追加到这里）

> 追加前先确认：这条是否推翻了第 3 节不可争议区的内容？如果是，必须显式说明理由。
>
> 缩写格式：`D编号 ｜ 决定 ｜ 理由 ｜ 否决的替代 ｜ 日期`

### D20 ｜ v0.1.1 双版本口径
**决定**：`compare` 与所有文档统一按 **507 份**（`is_substantive & ~is_pointer`）算覆盖率，
不再混用 518。
**理由**：转引用文件 `categories` 为空是正确的，混进分母会把覆盖率系统性拉低。
**否决**：继续用 518（与 v0.1 文档口径一致但语义错误）。
**日期**：2026-09-10

### D24 ｜ 工具版本与数据集版本解耦
**决定**：`agent-charters` 包升到 `0.2.0`（新增 `brief` 子命令），数据集仍是 v0.1.1。
**理由**：工具在迭代，数据没变；把两者绑在一个版本号里会让"哪份数据/哪版工具"说不清。
**否决**：为加一个命令就重发数据集版本。
**日期**：2026-09-11

### D25 ｜ `brief` 的一切比例必须实时从语料库计算
**决定**：清单里的覆盖率不硬编码，运行时算；只有"该问什么"的文案是人工写的。
**理由**：硬编码的数字一旦与数据脱节，工具就会引用错误基线——比没有工具更糟。
**测试**：`test_brief_lists_every_category_with_live_base_rates` 守着这条。
**日期**：2026-09-11

### D22 ｜ `category_counts` 改为定长稠密（九类全在场，缺席 0）
**决定**：不再输出稀疏 dict。
**理由**：①parquet 会把稀疏 dict 展开成 struct，缺席项变 `None`——下游拿到
`{"gotchas": None}` 而不是 0，极易算错；②稀疏 dict 的键顺序来自 `set` 迭代顺序，
依赖 `PYTHONHASHSEED`，**同一份数据两次生成字节不同，发布资产校验和无法复现**。
**否决**：保持稀疏并在文档里提醒（等于把坑留给使用者）。
**日期**：2026-09-10

### D23 ｜ 数据产物必须"跑两遍字节相同"，并写成测试
**决定**：`tests/` 里用两种 `PYTHONHASHSEED` 重跑抽取，比对 jsonl 与 parquet 字节。
**理由**：一次实测中发现 jsonl 校验和两次不同——不确定性会被误当成"数据变了"，
也会让"可复现"变成空话。
**否决**：只靠人工重跑确认。
**日期**：2026-09-10

### D21 ｜ 待办：把 `STRONG_PATTERNS` 的假阳性审计变成例行
**决定**：每次改关键词，都要先跑"候选词影响面统计 + 抽验命中标题"，再改。
**理由**：本轮就是这样抓出两个假阳性的；凭直觉加词一定会引入噪音。
**否决**：改完直接重跑看总数。
**日期**：2026-09-10

### D26 ｜ 私有来源必须登记，且不得进 FINDINGS
**决定**：任何来自未公开仓库（`/home/janz/workspace/` 下那 8 个）的观察，一律
①在 `work/SELF-TRACE.md` 第 8 节编 S 号登记（载体/来源性质/可公开性）；
②只作"假设来源"，**不得进入 `FINDINGS.md` 作为独立发现**；
③对外只给指纹（提交哈希、时间戳、行数），不给内容。
**理由**：本数据集最值钱的是"谁都能验"。私有观察一旦与 558 份公开语料混同叙述，
读者会把个案读成统计，可复核性就没了。分层比引用格式重要。
**附带更正（同日）**：那批协议文档是**人机对话形成 + 当事人确认**，不是人独立撰写；
故本项目此前 `work/auto_vs_human.md` 的"人写 / 机器写"二分法**不成立**，
对照组的中间那一档此前没有对应物。
**日期**：2026-09-11

### D27 ｜ 私有假设必须配对公开回测才进 FINDINGS
**决定**：私有来源（S1–S13）产出的观察，一律先写进 `work/paired_hypotheses.md`，
写全「可证伪形态 / 可计算口径 / 预期结果」三样；**只在公开语料上验证通过的，才进 `FINDINGS.md`**，
且须注明假设来源。测不出来的留在配对文件里挂账。
**理由**：私有数据当假设发生器、公开语料当验证器，是本项目唯一能同时保住
"有深度"和"可复核"的分工。不配对，就会退化成两套平行的自说自话。
**首个战果**：H-F9（gotchas 被外包）**当天即被证伪**（13.3% vs 26.8%，方向相反），
并顺带量出新槽位——15% 的章程指向外部知识载体/规则目录（发现 16）。
**日期**：2026-09-11

### D28 ｜ 外部引用信号进 v0.2 数据集
**决定**：`refs`（祈使转引 / 知识载体 / 断链）目前是**工具侧能力**，不在语料库里。
v0.2 要把它并入数据集字段（如 `routes_outward` / `hard_route` / `broken_targets`），
届时 `brief` 里的基准率改为从语料库实时计算（现在是硬编码 49%/15%，已在输出里标注出处）。
**理由**：九类按"内容"分类，量不出"知识放在哪里"。发现 16 表明近一半章程是**入口**而非全集，
这是规则集的一处空白；留在工具侧只能服务单个使用者，进了数据集才能被别人复算。
**否决**：直接加成第十类。它不是内容类别，和多工具兼容（发现 17）一样属于**结构信号**，
混进九类会污染已有的覆盖率口径与纵向基线。

**落地更正（2026-09-11，v0.2 实际发布时）**：字段定为四个——
`routes_outward`（并集，54%）/ `imperative_route`（祈使转引，49%）/ `hard_route`（知识载体或规则目录，15%）/
`ref_targets`（指向几个路径）。原计划的 `broken_targets` **没做**：存在性要拿到被指向的文件才能判，
不进数据集（留在 `refs` 命令里现查，四态 `exists`/`by_name`/`missing`/`unverified`）。
**多加 `imperative_route` 的原因**：FINDINGS 16 标题里的 49% 是"祈使式"，而 `routes_outward` 是并集 54%；
若只发并集，文档里那个 49% 就成了"只能靠 `work/` 脚本 + `data/raw` 才算得出"的数——违反 D28 自己的理由。
（复核时还发现发现 16 表格原写"235 份 / 46%"是错的，只算了英文正则，正确是 247/507 = 49%。已更正。）
**日期**：2026-09-11

### D29 ｜ 数据集升 v0.2 并改名（文件名带版本）
**决定**：数据集版本 `v0.1.1 → v0.2`，发布文件改名
`agent-charters-v0.1.parquet → agent-charters-v0.2.parquet`（jsonl 同理）；
工具版本 `0.3.0 → 0.3.1`（无新命令，只是随包语料库换代 + `analyze_text` 多 4 个字段）。
**理由**：这一版**schema 变了**（26 → 30 字段）**且覆盖率不可与 v0.1.1 直接比较**
（`is_pointer` 口径收紧，分母 507 → 511）。若沿用 `v0.1` 文件名，旧使用者会在不知情的情况下
拿到一套数字不同的数据，并以为还是那个 v0.1.1——**静默出错，正是本项目最防的一类失败**
（D20/D21/D22 都是同一主题）。改名让变化在文件名层面就可见。
**代价**：`work/` 下 5 个历史脚本的路径要跟着改（一次性）；旧文件名在 git 历史里仍可取。
**否决**：①沿用旧文件名、只在行内 `ruleset_version` 区分——机器可查但人不可见；
②同时发布 v0.1.1 与 v0.2 两份——两份会各自腐烂，且本项目只有一份事实源。
**日期**：2026-09-11

### D30 ｜ 首发渠道按"可达性"重排（2026-09-11）
**决定**：首发只走 **知乎**（V2EX 的短帖位由 掘金 / 开源中国 顶替）；**V2EX 与 Reddit 暂缓**（SNI 阻断，
必须代理）；**HN 走"加 hosts"方案**（只是 DNS 污染，直连真实 IP 返回 200），排在知乎之后 3–5 天。
**理由**：`SHARE.md` §0 的建议顺序（V2EX/知乎 → HN → Reddit）写在不了解线路可达性的前提下。
2026-09-11 实测：四个渠道里三个在 251 与云电脑出口上不通（详见 `ENVIRONMENT.md` §5.1），
照原顺序排等于把"发不出去"误读成"没人看"——那是比没人看更坏的信号污染。
**代价**：V2EX 那种"反馈最快、攻击性低"的短帖形态暂时用不上；换掘金/开源中国要另写一版中文文案。
**否决**：①为发 V2EX 现装 TLS 分片工具或开境外机——收益不确定，不为此引入新的系统级依赖；
②先发 HN——它只有一次机会，让中文站先把硬伤问出来更划算。
**日期**：2026-09-11

### D31 ｜ 修正幅度必须实测，推算值不得进对外文案（2026-09-12）
**决定**：任何"改规则会让数字变成多少"的说法，**必须跑一遍修复版数据、与旧版逐行比差集**之后才准写进对外文案；
只有方向（"偏高/偏低"）可以在实测前说。审计脚本必须同时输出**问题组合数**与**实际变动条目数**，两者不得混用。
**理由**：v0.1.3 修子串误命中时，我拿审计脚本打印的"23 处 (文件,类别) 组合 / 其中构建测试 16 份"
直接相减，估出 `build_test` = 433（84.7%），并把 84.7% 发到了三个公开站点。
实测逐份比对后：16 处构建测试误命中里有 5 处被正文/强模式通道兜住、并未掉标签，**真正掉的是 11 份 = 438（85.7%）**。
方向我猜对了，幅度错了 1pp——**一个错数字进了三篇公开文案**，比晚一天发出去代价大得多。
**代价**：三站要贴第二轮更正（`SHARE.md` §7，`work/share-paste/*comment-0*.txt`）。
这也是本项目第二次公开更正（第一次是 87% → 84.7% 的 bug 本身）。
**落地**：`work/substring_audit.py` 同时打印两个数；新增 `work/dataset_diff.py`（任意两版数据集逐行比差集）；
`tests/test_smoke.py` 里锁住"词中命中不许打标签、词首前缀必须打标签"两组参数化用例。
**日期**：2026-09-12

### D32 ｜ 定义边界由人裁定，且改规则前先实测影响（2026-09-12）
**决定**：`TAXONOMY.md` 里的**类别定义边界**（"风格收不收 Git 规范""环境收不收版本号"）
不由规则作者自决，由项目所有者裁定并记入 `TAXONOMY.md`「口径裁决」；
裁定后**必须逐条实测对语料库的影响**再决定是否改规则——影响为 0 的（本次第 1、3 条）只记口径、不动代码。
**理由**：人工核对测的是"规则 vs 定义"，定义不定就会把"定义模糊"记成"规则错"（`work/audit/v0.2-agent-verdicts.md`
里 ≥6 条判定卡在这上面）。本次四条裁决里前三条实测影响 0 份（纯定义），只有第 4 条动数字：
`agent_meta` 36.4% → 29.5%（−35 份，`ruleset_v0.1.4`）。
**如何避免"为了数字好看而改定义"**：裁决 4 的方向是**减少**标签（提高 precision、牺牲 recall），
且给出了未采纳的更宽口径与其数字（25.2%），留待 100 份人工核对的数据来定——**不凭"看起来像"删**。
**日期**：2026-09-12

### D33 ｜ 工具输出语言跟 locale，但"提示词语言"是另一件事（2026-09-14）
**决定**：五个命令的文案全部搬进 `agent_charters/i18n.py`（65 条 × 中英），
默认语言按 `LC_ALL` → `LC_MESSAGES` → `LANG` 判断（`zh*` → 中文，**其余含未设置 → 英文**），
每个命令都能用 `--lang en|zh` 覆盖。**`brief` 是刻意例外**：它的 `--lang` 管的是
**喂给模型的提示词**语言（默认 `en`——喂模型最稳，这是产品决定），
所以 `render()` 拆成 `lang`（提示词）与 `ui_lang`（周边清单）；不给 `--lang` 时清单跟 locale。
**理由**：对外渠道是英文（dev.to），读者照抄命令却看到硬编码中文＝把人挡在门口；
但既有中文用户不该因为加英文而看到中文变形——所以中文文案与改造前**逐字一致**，
并用「HEAD worktree vs 工作区」逐命令 diff 验证（`stats`/`compare`/`refs`/`show` 输出字节相同）。
**唯一有意的中文变化**：`brief`「每一项该问什么」原来显示英文（单语言 hack 的副作用，
中英混排），现在跟着 `ui_lang` 显示中文。
**验收**：150 测试（其中 5 条断言 en 输出**零汉字/全角标点**、1 条冻结中文文案）；
从 gitee 新装实测 9 秒 → `--lang en` 英文、zh locale 中文。
**日期**：2026-09-14

### D34 ｜ 进 CI 默认只报告、不拦；断链默认不判（2026-09-14）
**决定**：GitHub Action 的两个开关 `fail-on-missing` / `fail-on-dangling` **默认都是关闭的**
（空 / false），即默认只在 job summary 里报告，不让 job 变红。
**理由**：①章程的覆盖率是**过程指标不是质量指标**（`LIMITATIONS.md`），拿它当门禁等于
逼人往格子里填字，正好走向本项目反对的方向；②`refs` **分不清"去读这个"与"别提交这个"**——
禁令清单里的 `.env`、`data/raw/` 会被报成断链（`work/case-rmas-v3.md` 实测过 2 处），
默认拦会误杀正常章程。③要拦什么由使用者在自己的 workflow 里显式写明（`fail-on-missing: 'workflow,gotchas'`），
**闸门必须是使用者的选择，不是工具的默认**。
**配套**：`fail-on-missing` 里的类别名写错要**报错**（不能静默地"永不触发"）；
没有章程文件时 **skip + warning**，不失败（否则会打断所有没写章程的仓库）。
**日期**：2026-09-14

---

### D35 ｜ 两套 tag 命名空间：`v1` 是动作 tag，`vX.Y` 是数据集 tag（2026-09-14）

**决定**：仓库里同时存在两套 tag，语义不同、移动策略不同，**不要混用**：

| tag | 含义 | 会移动吗 | 谁在用 |
|---|---|---|---|
| `v1` | **动作 / 工具 tag**：`action.yml` + 随包语料库 | **会**（GitHub Actions 大版本惯例；移动理由写进该 tag 的注解） | 外部仓库 `uses: janzong/agent-charters@v1` |
| `v0.1` … `v0.5` | **数据集版本**：标注产物快照 | **不会**（不静默替换，见 D18） | 引数据的读者；各带 Release 与 sha256 |

**为什么必需**：README / `action.yml` / `SHARE.md` 早就让消费方写 `@v1`，
但两端远端**只有数据集 tag**，没有 `v1`—— 那个引用会直接报错（本轮开工前的真实状态）。
2026-09-14 补建并双端推送：`v1` → 注解 tag 对象 `1b8fe95` → commit `d2303f4`（＝建 tag 当时的 HEAD，工具 0.3.3）。

**验证（不看 `git push` 输出，看服务端真值与实跑）**：
1. 服务端真值：双端 `git ls-remote --tags` 都是 `refs/tags/v1 1b8fe95` + `refs/tags/v1^{} d2303f4`；
   `gh api .../git/ref/tags/v1` 解引用同样得 `d2303f4`。
2. **外部消费方实跑**：一次性仓库 `janzong/agent-charters-action-test`（显式 fixture），
   workflow 写 `uses: janzong/agent-charters@v1`，run `34858197698`：
   - runner 日志 `Download action repository 'janzong/agent-charters@v1' (SHA:d2303f4a7bd…)` —— tag 真解析到该 commit；
   - `report-only`（文档承诺的默认形态）**绿**，真输出 `coverage 3/9 categories`；
   - `enforce`（`fail-on-missing: 'workflow,gotchas'`）**按设计红**：
     `##[error]missing categories you asked to enforce: workflow, gotchas` + exit 1。
3. **这不算判据 6**：fixture 是我们自己建的，不是"非作者的外部使用者"。

**同日一次真实移动（顺便演示规则本身）**：同一轮改掉了 `action.yml` 里比实现宽的对外描述
（见 §23）——那属于"动作相关内容" ⇒ `v1` 从 `d2303f4` 移到 `d8b2737`
（注解 tag 对象 `1b8fe95` → `942adc4`，双端 `--force`）。**移动也复验了**：同一个消费方仓库
`workflow_dispatch` 重跑（run `34858578663`），runner 日志
`Download action repository 'janzong/agent-charters@v1' (SHA:d8b2737…)`，
两个 job 结论不变（`report-only` 绿 / `enforce` 红 + exit 1），
`gh api .../contents/action.yml?ref=v1` 读到的也是订正后的描述。

**顺带踩的坑**（已记进 `ENVIRONMENT.md` §9.8）：`gh repo create --source=. --push` 走 HTTPS，
而本机 git-over-HTTPS 到 GitHub 是死的 —— 仓库被建成**空仓**，`gh` 只报
`failed to run git: exit status 128`。**新仓库先 `remote set-url` 成 SSH 再 push。**

**移动规则（写给后续会话）**：改 `v1` 只改**动作相关的内容**（`action.yml`、`agent_charters/`、
随包语料库）；纯记账（`STATE.md` / `SHARE.md` / `work/`）留在 main，不必跟着移 tag。
移法：`git tag -f -a v1 -m "<为什么移>" <commit> && git push --force origin v1 && git push --force gitee v1`。

**2026-09-15 再次移动**：`v1` 从 `942adc4`（注解对象）→ `d8b2737` 之后的 `6c25b06`
（新注解对象 `dfcadc9`，双端 `--force`）。理由：动了**动作相关内容** —— 新增
`agent_charters/pointers.py`（指针识别 / 跟随）+ `action.yml`（新增 `followed` 输出与描述）。
服务端真值：双端 `ls-remote --tags` 都是 `refs/tags/v1 dfcadc9` + `refs/tags/v1^{} 6c25b06`。
**外部消费方实跑复验**（run `34985605504`）：`Download action repository
'janzong/agent-charters@v1' (SHA:6c25b06…)`，`report-only` 绿、`enforce` 按设计红、
新增 `pointer-follow` 绿（`followed=1` / `missing` 6 个）。

**日期**：2026-09-14（2026-09-15 补记第二次移动）

### D36 ｜ PyPI 首发走 Trusted Publishing（零 token），并把"能不能装"当成发布的一部分（2026-09-15）

**决定**：①发布**不进仓库任何凭据**——GitHub Actions 用 job 的 OIDC 身份向 PyPI 换一次性上传凭据
（Trusted Publishing）；②**发布本身也按"外部使用者路径可用"的标准验证**，不只是"CI 绿了"。

**为什么零 token**：本项目已经出过两次 key 卫生事故（dev.to key 两次贴进对话）。API token 一旦落盘/进日志/
进 `~/.pypirc` 就是长期凭据，还要轮换；OIDC 把这一类问题整个删掉。代价：账号侧必须开 2FA
（PyPI 强制，且**开了不能关**），且 pending publisher 五个字段必须与仓库精确匹配（不符＝403）。

**三道闸（都在 `publish.yml` 里，且本地都真跑过）**：①Release 触发时 tag 与 `pyproject.version` 逐字一致；
②版本已在 PyPI 上就跳过（同一版本号不能重传）；③上传前 `twine check --strict`。
另加一条测试钉死"发布路径里不许出现 `password:` / `secrets.pypi` / `twine upload`"。

**首发实况**（`agent-charters 0.3.3`，2026-09-15）：
- 触发：`gh workflow run publish.yml` → run `34959442365`，preflight / build / upload **三 job 全绿**；
- PyPI 侧：wheel sha256 `b5b8f631…31d3`（219,591 B）、sdist `bdb56763…325e`（243,557 B），
  MIT、10 条 classifier、4 条 Project-URL；
- **独立复验**（不看 CI 自己的输出）：从**默认索引**（真 PyPI、非镜像）`pip download` 轮子 →
  sha256 与 PyPI 公布值**逐字节一致**；干净 venv 实装 → 换到无关工作目录跑
  `compare` 输出 8/9、`stats` 语料库 558 份 ⇒ 语料库确实随包走、不依赖仓库目录。

**同日跟进 0.3.4**（run `34966005314`）：首发当场暴露两个对外问题，当天解决——
①PyPI 页面的 long description 是中文 README（英文渠道读者撞墙，与 D33 同一个问题）
＋ README 里 18 条相对链接在 PyPI 上 404（PyPI 不重写相对路径）；
②顺手补了 CLI 的 `--version`。
做法：新增 `README.en.md`（全文翻译、**链接全部绝对化**，相对链接 0 条），
`pyproject.toml` 的 `readme` 指向它 ⇒ GitHub 首页留中文（沿用既有链接与中文渠道流量）、
**PyPI 页面是英文**，两个 README 顶部互相带语言切换。
**数字交叉核对**：英文版 79 个数字令牌逐个回查中文版，只剩两处"中文版本身已过期"的差值
（测试 138→160、parquet 224→222 KB；实测 227,784 B）——没有新编的数字。

**"已在 PyPI 就跳过"这道闸已实跑验证**（不是只有单测）：0.3.4 发布后重跑一次
`gh workflow run publish.yml` → run `34966176206` 里 `build` 与 `upload` **都是 skipped**，
只有 preflight success。

**同日第三次跟进 0.3.5**（`8a39d4c`，run `34966958406`）：0.3.4 的"链接全绝对化"**漏了一条**——
两个 README 顶部的**语言切换器**。PyPI 不重写相对路径，`[中文](README.md)` 直接 404 跳搜索页
（用户当天实测）。这条也说明"改完只数自己改过的那几条"是不够的，**复验要扫全量外部引用**。
修法：两条切换器改绝对地址 + 新增测试 `test_pypi_readme_has_no_relative_links`
（剥掉代码块与行内代码后，相对链接必须一条不剩）+ 版本一致性测试里钉死 `readme == "README.en.md"`。
⚠️ **PyPI 的 long description 是上传时的元数据快照**——改它**必须发新版本**，所以这类"一条链接"
的错也要走一整轮发布；这正是把检查前移到测试的理由。留档 `SHARE.md` §8.8。

**顺带发现（已记 `ENVIRONMENT.md` §9.8）**：`gh repo create --source=. --push` 走 HTTPS，
而本机 git-over-HTTPS 到 GitHub 是死的 → 建出**空仓**且报错像"push 没执行"。

**日期**：2026-09-15

### D37 ｜ 工具运行时零依赖：语料随包发 `jsonl.gz`，而不是 parquet（2026-09-15）

**决定**：`pyproject.toml` 的 `dependencies` 置空。随包语料改成
`agent_charters/data/agent-charters-<DS>.jsonl.gz`（`gzip.compress(..., mtime=0)`，解压后与
`data/processed/agent_charters_<DS>.jsonl` **逐字节相同**）；`pandas`/`pyarrow` 降级为
`[parquet]`（读/写 parquet）与 `[test]` 两个附加依赖。`data/processed/` 仍同时产出
parquet + jsonl（发布资产，`SHA256SUMS` 不变），**parquet 依旧是发布格式**。

**理由**：为读一张 550 KB 的表而拖 pandas + pyarrow（≈62 MB），代价落在最不该掉链子的那一步——
`pip install agent-charters`。国内直连 PyPI 拉这两个包实测断流（`exit=124`），而**外部读者装不上
不会来提 issue，他们只是走掉**；同时 GitHub Action 每次运行也要多下 62 MB（`action.yml` 里
`pip install $GITHUB_ACTION_PATH`）。改完 wheel 219 KB → **104 KB**、干净环境实测
`stats/compare/show/brief` 全部可用且数字与 parquet 路径**逐字节一致**。

**否决的替代**：①保留 pandas 但改成惰性导入（省不了安装体积，一条命令就要 62 MB）；
②换成 polars / 自带 mini-DataFrame（换汤不换药，或把一个 60 行的类伪装成 pandas）；
③只改文档劝人用镜像（治不了根，且把问题推给读者）。

**守卫**：`test_the_package_has_no_runtime_dependencies`（`dependencies` 必须为空 +
模块级 AST 不许 import pandas/pyarrow/numpy）、`test_cli_runs_with_pandas_and_pyarrow_unavailable`
（把这两块从 import 路径摘掉，`stats` 仍要跑）、`test_the_bundled_corpus_and_the_parquet_agree`
（两条读法统计必须逐字节一致）、`test_bundled_corpus_matches_the_published_one`（随包副本解压后
必须等于发布 jsonl）。**否决面**：`Corpus` 不再是 DataFrame，`load_corpus().columns` 之类的
pandas 写法在库里失效（`pd.DataFrame(load_corpus())` 一行可拿回）。

**日期**：2026-09-15

### D38 ｜ 规则集升 `ruleset_v0.1.9`：只加两条词，且**先证明对已发布资产标签中性**（2026-09-15）
**决定**：`BODY_RULES["boundaries"]` 的中文禁令家族加 `绝不`；`HEAD_RULES["overview"]` 加 `这是什么`。
`HEAD_RULES["boundaries"]` 加 `绝对不能做` **实测收益≈0，不收**。`RULESET_VERSION` → `ruleset_v0.1.9`。
**v0.5 数据资产一个字不动**（558 行仍写 `ruleset_v0.1.8`——它们由 v0.1.8 产出，工具与数据版本解耦，D24）。

**理由**：本仓库自己的 `AGENTS.md` 被自家工具判成 8/9（§22），而且那个 `boundaries` 是**假命中**——
靠文档里"解释这个缺口"那句话中的「禁止」二字命中，两条真禁令（`绝不提交原文全文` / `绝不把密钥写进仓库`）
**从来没被认出来**。这是"文档/工具不一致"里最难看的一种，且修复成本极低。

**为什么敢动规则**（D32 要求先实测，§22.1 是实测记录）：对 v0.5 **逐份标签中性**——
`work/audit/rule_gap_impact.py` 先在内存里加词测（516 份覆盖率零变化、558 份逐份零变化），
落地后又用 `--verify-v05` **以改完的真实规则**重算 558 份，逐份标签与内容模式**完全一致**，
覆盖率仍是 `boundaries` 85.7% / `overview` 32.2%。收益落在将来采到的中文上：v0.6 中文 1046 份 +6/+8。

**为什么这不算"往词表里塞词"**：塞词会被否决的原因是它补不了 §22 的"提到即命中"机制——**这一点照旧成立**，
所以本决定只当「补两条明确缺失的高频说法」。**遗留已在同日量完并关掉**：正文通道仍是字面匹配，
但「提到即命中」的代价实测≈0（`LIMITATIONS.md` §22.2）⇒ **不为它写语义判别器**，只当已知性质记着。

**守卫**：`tests/test_smoke.py` O5 六条（四正例：`绝不` 进 `boundaries`、`这是什么`/`这是什么项目` 进 `overview`；
两反例：光有「绝对不能做」标题不出 `boundaries`、「这不是什么」不被子串带走）。
**否决的替代**：①只加 B（自家文档也能 9/9，但 `boundaries` 还是假的）；②都不加（把缺陷留在自家文档上）；
③同时收 `绝对不能做`（实测≈0，纯增维护面）。另记一条编号提醒：`v0.1.9` 这个号原先被
「h1 守卫 / 模块名型标题守卫 / 全文兜底门放宽」的挂账预占（§5「第三优先」）——**同日三项全部清完**：
前两项实测后否决，兜底门量化后决定不动（见 **D39** / `LIMITATIONS.md` §24）。

**回滚**：revert 这一个 commit（`ruleset_v0.1.9` 从未被任何资产使用，回滚无副作用）。

### D39 ｜ 全文兜底门**不放宽**：实测 ≤7 个标签、2 个是假的，且会改动已发布资产（2026-09-15）
**决定**：`analyze_text` 的全文兜底门维持现行口径（`empty`：完全没有标签，或 ≤1 个标题时才跑全文）。
`le1_sections` / `le1_counts` / `always` 三种更宽的口径**都不采纳**，只在 `extract.py` 里留成
**审计开关**（`FULLTEXT_GATES`，默认值＝现行规则，行为零变化）供将来重切数据集时再量。

**理由**（按重要性排）：①**不是标签中性**——与 v0.1.9 那两条词（558 份逐份零变化）不同，
这个门会让**已发布的 v0.5 标签本身变**（7 处），按 D18「不静默替换已发布资产」要重切数据集；
②收益 7/4600+ (文件,类别) 对 ≈ 0.15%，覆盖率最多动 0.5pp；③方向可疑：上界 `always` 补 130 个标签，
但在有人工盲判的样本上**真 14 / 假 15**——全文通道是最弱的通道；④挂账的原始痛点
（`crewAIInc/crewAI` 丢 1 个 `build_test`）已在 `LIMITATIONS.md` §24 如实记录，这比改门去捞它更合本项目的取向。

**否决的替代**：为这 1 个真损失去改门（净收益 4 真 − 2 假 = 2 个标签，代价是重切数据集 + 版本号）。

**守卫/复验**：`work/audit/fulltext_gate_audit.py`（可重跑，输出含逐条证据）；
`tests/test_smoke.py` 现有 174 条全绿即证明默认口径未被动过（审计开关是新增的 keyword-only 参数）。

**顺带清掉的东西**：`STATE.md` §5「第三优先」里那三条"留 v0.1.x 实测"的挂账**全部结清**
（h1 守卫 / 模块名型标题守卫＝否决；全文兜底门＝量化后不动）。

### D40 ｜ 不做「跨文件一致性」产物，改做**指针识别 / 跟随**（2026-09-15）

**决定**：①**放弃选项 3**（跨文件 `AGENTS.md`/`CLAUDE.md` 一致性检测产品）——实测真冲突率 **2.5%**
（1/40），没有可检测的量；②选项 2（把 `CLAUDE.md` 当第二份语料、新切 v0.6）**推后**，因为增量估算里
38% 的成对有一份是空壳/指针，`≈516 份新文档`要打折；③**改做「指针识别 / 跟随」**：
`compare` 与 GitHub Action 遇到 `@./AGENTS.md`（13B）/ `Read \`CLAUDE.md\``（734B routing-stub）/
`mode=120000` 符号链接时，**要么跟随到目标文件再判，要么在输出里明说"这份是指针，真正的规则在 X"**，
不再报 0/9。**这是工具行为改动，不动数据集、不动规则集**（可单独发一个 0.4.x）。

**理由**：①只有"单文件判定撞上指针"这件事**普遍（38–49%）且会给出错误结论**——用户看到"0/9"会以为
自己的章程是空的；②选项 3 的卖点（"两份文档互相矛盾"）被数据证伪：58% 是真分叉但相似度常接近 0，
两份是**为不同工具写的不一样的东西**，不是同一套规则的两个版本；③成本极低，且现有 `is_pointer`
反证闸（§10/§14）已经是半个能力——只缺"跟随/转述"那一步。

**否决的替代**：为一致性检测新建一整套双文档管线（无被测对象，纯成本）；把 `CLAUDE.md` 全部并进语料
（增量里混着指针与副本，会把覆盖率稀释而不是提高）。

**守卫/复验**：`work/audit/pair_divergence.py`（可重跑，输出含逐对证据）＋
`LIMITATIONS.md` §25（含四条边界：分档而非随机抽样、只判 8 个槽位、正则漏判、九类用自家分类器）。

**状态：已实现并已发布（同日，工具 `0.4.1`，PyPI run `34987867624`；外部消费方 `v1` 实跑见下）**。新模块 `agent_charters/pointers.py`；
`pointers.resolve(path)` 是三个入口（`compare` / `brief` / Action）的唯一接线点。
落地前先量了规模（`work/audit/pointer-census.md` + `pointer_probe.py`）：791 份根级章程里
111 份符号链接、156 份非链接小文件、**75 份纯指针**（中位 11B、73/75 目标在树里、69 份当前报 0/9）。
判定＝**薄 + 在指路 + 自己没有规则**；`@路径` 语法必须认（`is_pointer` 的正则完全不认它，而它是主导形态 68/103）。
已知漏判 1 份（`RobertoMachorro/Moped`，44B 的 "Refer to @AGENTS.md **mandatory** instructions."
被反证闸挡下）——**不改闸**（与 D39 同一处置）。新增 8 条测试，`pytest` 185 passed。

**已上线到消费方**（同日）：①PyPI `0.4.1`（run `34987867624`）——`pip install agent-charters` 即得；②动作 tag `v1` 已移到 `6c25b06`，并在外部消费方仓库
`janzong/agent-charters-action-test` 实跑 —— 新增 job 拿一份 **11 字节**的 `CLAUDE.md`（`@AGENTS.md`）
跑 `uses: janzong/agent-charters@v1`：**绿**，`followed=1`、
`missing=boundaries,workflow,style,environment,agent_meta,gotchas`（与 `AGENTS.md` 一致，不是 9 个）。
顺带踩到一个通用坑并记进 `ENVIRONMENT.md` §9.9：**`GITHUB_STEP_SUMMARY` 是每个 step 各一个文件**，
后面的 step 读不到前面 action 写的内容 ⇒ 断言要用公开 `outputs`，别读那个文件。

### D41 ｜ 分类口径对外**不动**：保持 92% / 70%（2026-09-15，人裁定）

**裁定（用户原话）**：「对外先不动」——即**保持** §19 / §20 里那个更保守的口径
（precision **92% / recall 70%**，把 12 处盲判标「犹豫」的类别记在规则头上），
**不**改用「犹豫出局」的 97% / 71%，因此**不需要**再往三站补一轮更正评论。

**为什么这条要单独立案**：它是 §20 挂了三天的那件「待人裁定」的收口。本轮先把
**能自动修的那一半**修掉了（`f0e6d78`：三个脚本两条口径都打印、三处写反的抬头改对），
剩下的「对外主推哪条」由人拍板——拍的就是"沿用保守那条"。

**代价与守卫**：①对外数字与三站已贴出的更正评论**完全一致**，没有"同一件事两套数"的风险；
②两条口径现在都由脚本打印（`holdout_vs_rule_v0.5.py` / `human_vs_rule_v0.5.py`），
想换口径随时能算出新值、也随时能看到代价（+12 TP / −12 FP / recall +1pp）；
③**换口径要再发一轮公开更正**——这条写进 `LIMITATIONS.md` §20 的收口段，避免后续会话
以为"口径可以随手换"。

**日期**：2026-09-15

### D42 ｜ 判断层与测量层解耦：纵向面板立项，**不再加功能 / 加维度**（2026-09-16）

**裁定（用户原话）**：「不再增加功能和增加维度了」；纵向跟踪**立项**
（设计、契约、判据全部在 `work/longitudinal-plan.md`）。

**背景**：本日三轮开放讨论（①"看使用情况" ②"模型变强会不会把价值吃掉" ③"仓库淘汰 / 焦点迁移"）
收敛到同一条设计原则 ——
**判断层**（视角、什么该增该减、机会在哪）**应持续变化且零成本**；
**测量层**（快照、基线、字段口径）**必须稳定、自动、不因判断变化而中断**。
两者一旦绑死，就会重演这个领域最常见的失败模式：判断一变就重标或丢弃旧数据，
于是**永远只有横截面、没有时间轴**。

**为什么是现在**（四条，都不是"看好"）：
1. 横截面有免费替代品（谁都能抓一份今天的），**面板没有替代品**（只能现在开始记）。
2. §3.1 **不存原文全文** ⇒ "采集那一刻算了哪些派生字段"是**不可逆决策**，晚一期就是永久缺一期。
3. 这是唯一一类**随模型变强而升值**的资产：模型越强越读得懂历史序列，
   但序列不会因为模型变强而重新长出来（模板 / 骨架 / 仓库内可推事实则会被免费吃掉，见 §6.5）。
4. `baseline-2026-09-10.tsv`（558 仓）与 `work/longitudinal.py` 早已就位，
   但 **251 上没有任何定时任务**，实际处于"躺着"状态 —— 缺的不是能力，是把决策做掉。

**约束（防走偏，后面谁都不许随手改）**：

- 面板**只加期、不改框**；换框＝**新 vintage**，两框并列、**不合池**（采样框本身是结论的一部分）。
- 每期**同一 schema**（列与 T0 一致）。**要加派生字段＝走一条新决策，不夹带** ——
  因此"人写 / 机写"字段（§6.5 说 v0.2 必修的那条）本轮**明确不启动**，先挂 §5。
- **老快照永不重标**（与 D18 一致）；规则集升版只影响新期，跨期比较只用同 `ruleset_version` 切片。
- **死者留档**：404 / 归档 / 路径消失标 `dead|missing` 并保留行、**永不删除**。
  死者是**对照组**；只留成名的几家＝幸存者偏差，是本题材最容易被后人一句话打死的错误。
  抓取抖动（`IncompleteRead` / 限流）**不算死亡**。
- **预登记复评时点 2027-09-10**（T4 之后），按 plan 里 5 条预登记判据决定 **继续 / 换 vintage / 转存档**。
  —— 长期主义 ≠ 无限期等待；写死时点是为了让它成为**决策**，不是**信仰**。

**同期明确"减弱"的投入**：对外文案与渠道迭代（外部信号 0，不靠多写改变，见 §6.7）；
`CLAUDE.md` 第二文档 / 跨文件一致性方向（D40 已实测打掉、推后）。

**日期**：2026-09-16

## 6.5 实验结论：自动化对定位的影响（2026-09-10/11）

**问题**：编辑器/Agent 已能一键生成章程，语料库还有差异化用处吗？
**实验**：11 个仓库（7 种语言）× 3 档提示词，移走仓库内章程文件后生成，与人写版本对比。
详见 `work/auto_vs_human.md`。

**结论**：

1. 平均类别 机器 5.3 vs 人写 4.6，字节 7098 vs 4131。
   `style` 机器 11/11、人写 6/11；`overview` 机器 10/11、人写 3/11。
   **"章程模板"这条线不要站，会被免费吃掉。**
2. **`workflow` 人写 8/11、机器 0/11**（逐份查原文确认不是分类器假象）。
   但在提示词里点名要"贡献流程"后 **3/3 立刻写出**。
   → **机器漏的不是仓库里有的信息，是提示词没点到的槽位；短板在提问，不在知识。**
3. 点名要"坑"→ 3/3 写出，且是真知识（README 与代码不一致、拼错的键名等）。
   **上一轮"机器写不出经验类内容"的说法已被推翻**（修正记录见 `FINDINGS.md` 发现 11）。
   仍然写不出的是"曾经出过事"的历史——当前仓库状态里没有证据。
4. `gotchas` 人写 0/11、机器 0/11。**不是写不出，是没人想起来写。**
5. 机器 11/11 收敛到同一骨架；而人写的 Whale/nano 也在用同一个
   `Repository Guidelines` 骨架（语料库里 39 份标题、20 份 ≥4/6 子节）。
   **真正的轴不是"人 vs 机器"，是"能否从仓库推导"。**

**对定位的修正**：差异点从"数据"变成**"该问什么"**——
机器的覆盖面由提示词形状决定，而"该问哪些问题"的答案就是 558 份的分布。
`compare` 因此有了更硬的产品形态：**生成时的检查清单**。

**v0.2 必修**：**人写/机器写判定字段**（sha 聚类 + 模板骨架指纹 + 结构特征）。
自动化越普及这个字段越关键，而目前还没有别人需要解这个问题。

**已测（2026-09-11）**：`gotchas` 的来源结构 —— 120 条人工标注：
**58% 可从仓库读出 / 34% 不是坑 / 8% 需要经历或外部世界**。
见 `work/gotcha_origin.md` 与 `FINDINGS.md` 发现 13。
- 机器点名要坑能补上大部分（与发现 11 一致）
- 真正拿不到的是那 8%：证据在仓库之外（OS 行为、上游状态、昨天的故障）
- 副产品：人的 Gotchas 章节有 1/3 不是坑 → 已写入 `LIMITATIONS.md`，
  并给 `compare` 一个新的检查项：你的坑章节里有多少是真坑

**已做（2026-09-11）**：`agent-charters brief` —— 生成时的检查清单 + 可粘贴提示词。
端到端验证：拿 `brief` 产出的提示词重跑 ts2/minisim/katok，
覆盖从 **4/4/5 类 → 9/9/9 类**，`workflow` 与 `gotchas` 全部写出且内容实在。
（见 `work/auto_vs_human.md` 第 6 节）

⚠️ 由此暴露的新问题：**填满 ≠ 写好**。清单把"漏槽位"解决了，
但也可能诱导生成器把九个格子都填上凑数。**判据必须从覆盖率换成内容质量**，
否则下一步优化的是数字不是章程。

## 6.6 纵向基线（2026-09-10 固化）

**已做**：`data/processed/baseline-2026-09-10.tsv`（558 个仓库的 `file_sha` 快照）
＋ `work/longitudinal.py`（与新抓结果比对：内容变化 / 消失 / 新增 / 模板簇）。

**为什么**：机器生成的章程正在变多，但**任何单次抓取都测不出这个变化**——
只有纵向对比能。基线的价值只随时间增长，今天不存，三个月后就没有对照组。

**重抓后怎么用**：

```bash
.venv/bin/python work/fetch_full.py <仓库清单>   # 重抓（覆盖 data/raw/）
.venv/bin/python work/longitudinal.py            # 与基线比对
```

判据：**内容变化数**与**新增里的同 sha 模板簇**随时间上升，
就是"章程在被批量生成"的直接证据。

**注意**：重抓前先备份 `data/raw/full_manifest.jsonl`，否则会覆盖掉这次的原始清单。

**→ ✅ 2026-09-16 正式立项（D42）**：上面这套"躺着"的机制改成**按季运行**，
设计与纪律全部写进 `work/longitudinal-plan.md`。要点：
①面板 **vintage 2026-09**（558 仓）**只加期、不改框**，换框＝新 vintage（两框并列、不合池）；
②每期**同一 schema**、**老快照永不重标**（与 D18 一致），跨期比较只用同 `ruleset_version` 切片；
③**死者留档**：404 / 归档 / 路径消失标 `dead|missing` 保留，**永不删**——死者是对照组，
只留成名的几家＝幸存者偏差；抓取抖动（`IncompleteRead` / 限流）**不算死亡**。
三个正交指标：**存活率**（仓库还在吗）／**活跃率**（文件还在同一路径吗）／**语义稳态**（还在维护的仓库里基线动不动）。
预登记复评时点 **2027-09-10**（T4 后）——长期主义 ≠ 无限期等待。

**→ ✅ 2026-09-16 晚：第一个点已跑出**（`work/panel_run.py`；多跑了一个 **T0.5 = 2026-09-16** 的
验证点，T1 = 2026-12-10 不变）。6 天实测：

```
存活率   558/558（dead 0 ｜ moved 0 ｜ missing 0）
活跃率   ok 555 ｜ archived 3
内容     未变 455 ｜ 已改 103（18.5%）｜ 新入框 0
语义稳态 有 push 但 sha 未变：292
模板簇   3 簇 / 35 仓
```

**最重要的读法是"没发生什么"**：3 个簇**全是指针或空文件**（9 B `CLAUDE.md` ×31、0 B ×2、13 B `.ai/AGENTS.md` ×2），
而且 T0 就有 30/2/2 —— §6.6 想抓的"**机器批量写章程**"的直接证据**尚未出现**。
⚠️ 18.5% 是**框内抖动**（样本本就按活跃选、median commit age ≈ 0 天），不能外推成 GitHub 全体；
存活率 100% 也是样本框的**大概率结果**，不是发现。三个指标的价值都在**随时间走向**，不在单点。

## 6.7 现在的收敛点：停止加维度，去用一次

内部已产出 5 个产物（数据集 / compare / 两轮实验 / gotcha 分析 / brief），
**外部信号仍是 0**。停滞的真正风险不来自样本偏置，来自**闭环缺失**：
我们越确信它有价值，越需要一次外部确认。

**结论稳健性分级见 `FINDINGS.md` 发现 15**——机制与排序可引，绝对数字不可引。

> **2026-09-16（D42）澄清**：上面这条"停止加维度"**不覆盖纵向采集**。
> 两者是同一件事的两面：**不加维度（冻结 schema、冻结规则集），但持续测量（按季快照、序列不断）**。
> 一句话记法：**判断可以变，测量不能变。**

## 7. 已知的最大缺口

**"平台"假设至今未被检验。**

原始设想是：rmas-v3 只是第一个实例，真正的目标是"学习—研究—开发系统"，多机多智做多实例。

但本项目**没有复用 rmas-v3 的任何内核**（节点-边模型、抽取管线、前端），复用率 0%。

按既定平台判据（"新域实例首日复用内核资产 ≥50%，否则叫复制不是平台"），
**本项目目前是一个平行的独立项目，不是第二个实例。**

这不是失败，但它意味着：**最核心的设想，一次都没被验证过。**
下一次启动新域实例时，这是必须先回答的问题。
