# 环境
.venv/
__pycache__/
*.py[cod]
.pytest_cache/

# 数据：原始数据（含 558 份原文）不进版本库
# data/processed/ 下的衍生标注数据集**要**进版本库，因此不做全局 *.parquet/*.jsonl 排除
data/raw/
data/cache/

# 编辑器 / 系统
.DS_Store
.idea/
.vscode/

# 本地临时
*.log
tmp/

# 打包产物（pip install -e . 生成）
*.egg-info/
build/
dist/

# 审计工作台产物：内嵌 100 份原文全文 → 与「Release 不含原文全文」的口径冲突，
# 只在本机浏览器用，绝不进版本库、绝不随 Release 发布。
work/ui/workbench.html
work/ui/cache/
work/ui/build.log
