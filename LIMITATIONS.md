# 已知局限 LIMITATIONS v0.3

> **请在使用数据前读完本文件。**
> 本数据集是 **v0.5**（分类法仍为 `taxonomy_v0.1`，判定规则 `ruleset_v0.1.8`），
> 目标是**跑通管线**而非达到论文级精度。以下局限是已知且诚实的。
>
> **不可比性**：相对 v0.1.1 见 §10，相对 v0.4 见 §14（分母 511 → 516）。
> **v0.5 新增的三条必读**：§13（`Do not …` 通式约 3% 假阳性，且它决定了头条排序）、
> §14（`is_pointer` 口径改动）、§15（代码块里的 `# 注释` 曾被当成标题）。

## 1. 分类由规则完成，不是逐份人工标注

`categories` 由 `work/extract_v1.py` 的关键词 + 模式匹配生成，
**没有经过逐份人工校验**。

**实测准确率**（在 v0.1 上随机抽 12 份人工核对）：

- 完全/基本准确：**9 份（75%）**
- 有漏标未错标：**3 份（25%）**
- **错标：0 份**

即**精确率高、召回率偏低**——分类器倾向于漏标而不是误判。
这个失败方向是刻意的：错标会污染统计，漏标只是损失召回。

> **v0.1.1 / v0.1.2 两轮改动都未重新做人工核对。** 验证方式是：
> 逐个候选关键词统计影响面并抽验命中的真实标题（见 `TAXONOMY.md` 演进史），
> 加上回归测试锁住已发现的假阳性。v0.1.2 另做了 11 份 `is_pointer` 的**逐一人工标注**
> （真 4 / 边界 2 / 误 5），但那是对单一字段的核对，不是对九类的核对。
> 这些都是**弱于人工核对的证据**。

**实际影响**：各类别覆盖率是**下界估计**，真实覆盖率只会更高不会更低。

## 2. 分类失败的已知位置

- **无标签率 1.6%**（8 / 516，v0.1 时为 2.1%）：实质文件但无任何标签
- 未归类样本集中在：散文式无标题文件、极短文件（200–600 字节）、
  标题用语罕见的中文文件（v0.1.1 已补一批中文标题关键词，长尾仍在）
- 注意：`is_pointer=True` 的文件 `categories` 为空是**正确结果**，不计入失败

## 3. 样本有选择偏差，不代表 GitHub 全体

候选仓库来自 `gh search code --filename AGENTS.md` 与 **AI/agent 相关 topic**
（`ai-agents`、`agent`、`mcp`、`llm`、`coding-agent`、`claude-code` 等）。

**含义**：

- 偏向 AI 工具生态，TypeScript / Python 占比偏高
- 不能用来推断"所有 GitHub 项目的章程写法"
- 但**用于描述 AI 领域自身的规约实践是合适的**

## 4. 中文样本过少（4.8%）

516 份中仅 **25 份**为中文（`doc_language=="zh"`；另有 17 份中英混排、1 份日文），
**任何按语言做的对比分析都缺乏统计效力**。
分类器的关键词以英文为主（v0.1.1 补了中文同义词，但仍是关键词法）——
**中文章程上的结论请一律当作下界**。

## 5. `content_mode` 判定粗糙

仅依据指令词（do not / must / never / always / avoid / required）的出现密度阈值切分，
不做语义判断。个别文件会误判，例如以操作说明为主的文件可能被标为 `knowledge`。

## 6. 类别边界存在重叠

例如 "Commit Style" 会同时命中 `style` 和 `workflow`。
这是有意保留的——该章节确实同时涉及两者——但意味着**各类别不是互斥划分**，
不能做"占比之和为 100%"这类计算。

## 7. 快照性质，不是时间序列

每份文件记录的是一个**时间点**的内容（`commit_date` + `file_sha`），
**无法用于分析同一仓库的规约演化**。纵向分析需要 v0.2 的多时点采集。

## 7.5 数据格式上的两个坑（已在 v0.1.1 处理，旧版使用者需知道）

1. **稀疏的 `category_counts` 会被 parquet 展开成 struct**：
   v0.1 的 parquet 里，缺席类别存为 `None`/`NaN`（即 `{"gotchas": None}`），
   而不是 0。下游用 `d["gotchas"] >= 1` 会直接报错或静默算错。
   v0.1.1 起改为**定长稠密**：九类全在场，缺席为 `0`。
2. **生成过程曾经不确定**：`category_counts` 的键顺序来自 `set` 迭代顺序，
   而它依赖 `PYTHONHASHSEED`——同一份数据两次生成字节不同，校验和无法复现。
   v0.1.1 起顺序固定，并有测试在两种哈希种子下比对产物字节。

> 用 v0.1 的 jsonl（而非 parquet）不受第 1 条影响；两版都受第 2 条影响。
> （v0.2 起文件名改为 `agent-charters-v0.2.parquet` / `agent_charters_v0.2.jsonl`。）

## 8. 版权边界

数据集**不含任何原文全文**，仅含衍生标注、统计特征与极短引用。
原文版权归各仓库作者，许可以各自仓库为准（本批 70% 为 MIT/Apache-2.0）。

如需原文，请用 `repo_full_name` + `file_path` + `file_sha` 自行从 GitHub 取回。

## 8.5 `gotchas` 覆盖率不等于"写了好坑"

`gotchas` 覆盖率（**13.6%**，70/516）是**带标签的文件比例**，不是内容质量指标。
对 347 条坑的人工标注（`work/gotcha_origin.md`）显示：
**34% 的条目根本不是坑**——是通用建议、规则或操作步骤被塞进了 Gotchas 章节。
引用这个数字时请说明它衡量的是"有没有这一节"，不是"这一节写得好不好"。

## 9. 分类规则变更会导致不可比

`extractor_version` 与 `taxonomy_version` 记录在每一行里。
**跨版本比较前必须先确认这两个字段一致。**

⚠️ **v0.1.1 是一个特例，必须知道**：`taxonomy_version` 仍是 `taxonomy_v0.1`
（九类的**定义**没变），但**判定规则变了**（新增强模式通道 + 扩充标题关键词），
因此 **v0.1 与 v0.1.1 的覆盖率数字不可直接比较**（如 `build_test` 79% → 87%）。

为让这件事可机器校验，v0.1.1 起每行多一个 `ruleset_version` 字段：
- `taxonomy_version` = 九类的定义版本（类别是什么、边界在哪）
- `ruleset_version` = 判定规则的版本（怎么判、用哪些关键词）

**跨版本比较时两个都要看。** 要复现 v0.1 的旧数字：
`analyze_text(text, strong=False)` 再配回 v0.1 的关键词表。
按版本规则，整数位递增（如 v0.1 → v1.0）意味着定义变更，**旧结论需重新验证**。

## 10. v0.2 改了 `is_pointer` 口径——与 v0.1.1 的覆盖率不可直接比较

v0.1.2 规则集收紧了"转引用文件"的判定（`is_pointer`）。

- **旧口径**：提到 ≥2 个 `.md` 文件名 **且** 体积 < 2000B → 判为指针。
  问题：把"有实质规则、只是引用了文档"的短章程也剔出了统计。
  11 份裁判定的文件逐一人工标注：**真指针 4 / 边界 2 / 误判 5**。
- **新口径**：`瘦（去链接去路径后 <400B）` **且** `确实在指向（祈使转引或 ≥2 个 .md）`，
  **或**作者自陈"本文件不写规则 / 全部规则在别处"。

**后果**：可用于统计的实质文件 **507 → 511**（指针 11 → 7；v0.5 又变成 **516 / 指针 2**，见 §14）。
九类覆盖率随之变动
**≤1 个百分点**（多数向下，`structure` 反而 +1：标题词表补齐了"分工/职责/ownership"
这类章节），对结论无实质影响，
但**两版数字不能放在同一张表里比**。逐行用 `ruleset_version` 区分（`ruleset_v0.1.1` / `ruleset_v0.1.2`）。

**已知残留误差**（明写出来）：极短但含具体规则的章程（约 200–400B）曾被判成指针。
**这条已在 v0.5 修掉**——`buttondown/docs` 等 5 份收回统计口径，原 `xfail` 测试转为普通断言，
详见 §14（分母 511 → 516 就是这次改动）。仍判为指针的只剩 `chroma`（129B 单句）与
`plotly/dash`（纯链接索引页）2 份。

**同一处口径过宽在两轮里各出现一次**：`agent_charters/refs.py` 第一版用"出现任意 `.md` 路径"
代理"把知识外包"，命中 87%（假阳性机器）；`is_pointer` 用"提到 ≥2 个 .md 文件名"代理
"正文是空壳"。**都是拿"出现过文件名"代理"内容为空"**——本项目最该记住的一条教训。

## 11. 标题关键词曾按**子串**匹配，`构建测试` 被高估 2.2pp（09-11 发现 → 09-12 已修，随 v0.3 发布）

**状态：已修**，随 `ruleset_v0.1.3` / 数据集 v0.3（2026-09-12）。下面按"问题 → 修法 → 实测影响 →
一处自我更正"记全，方便别人复算或反驳。这是本项目"数字错了就改并留痕"的第 2 例（第 1 例见 §10）。

### 11.1 问题

`taxonomy.py` 的标题通道原是 `if k in heading.lower()`——**纯子串，没有词边界**。于是：

- `ci` 命中 `De**ci**sions` / `Prin**ci**ples` / `Dis**ci**pline`
- `script` 命中 `Type**Script**`；`build` 命中 `allow**Build**s`；`commit` 命中 `un**commit**ted`

还有一类不是子串、但同样该算误命中：裸词 `make` 是普通英文动词，`Make changes` / `make sure`
在旧口径下都算"这份文件写了构建"。

### 11.2 修法（两处代码，不动语料、不动九类定义）

1. **词首匹配**：命中点必须落在词首；连字符算词边界（`commit` 仍命中 `Pre-Commit`）。
   **词首前缀命中保留**——`convention`→Conventions、`boundar`→Boundary、`test`→Testing
   是规则有意为之，不能用 `\b` 一刀切。
2. `build_test` 标题词表**删掉裸词 `make`**，只留 `makefile` 与具体目标（`make build` / `make dev` …），
   与 `STRONG_PATTERNS` 里既有的"只认具体 make 目标"口径对齐。
   依据：511 份里 12 个标题命中裸词 `make`，逐条看 **7 份是散文**（`Make changes`、`Where to make changes`、
   `make sure`、`makes variables global`），只有 5 份真是构建工具。

### 11.3 实测影响（v0.2 → v0.3，511 份可用样本）

复算脚本：`work/substring_audit.py`（对**出错的那版快照**跑，不是对修好的数据跑）；
逐份明细：`work/audit/substring-impact.md`；差异留档：`work/v0.2-to-v0.3-diff.md`。

- v0.2 里有 **23 处 (文件, 类别) 组合 / 23 份文件**，其标签**只**靠误命中撑着（标题通道无干净命中、正文通道也无）。
- 修好后：**18 处真正掉了标签**，其余 **5 处**靠正文或强模式通道兜住——标签仍成立，只是证据列变干净。
  这正是"证据质量"与"类别正确性"分开判的意义（同 §8 的精神）。

| 类别 | v0.2（误命中也算） | v0.3（修正后） | 差 |
|---|---|---|---|
| 构建测试 | 87.9% (449) | **85.7% (438)** | **−2.2pp（−11 份）** |
| 流程 | 66.1% (338) | 65.9% (337) | −1 份 |
| 环境 | 44.8% (229) | 44.4% (227) | −2 份 |
| AI行为 | 36.8% (188) | 36.4% (186) | −2 份 |
| 架构 | 59.9% (306) | 59.7% (305) | −1 份 |
| 风格 | 56.9% (291) | 56.8% (290) | −1 份 |
| 禁令 / 概览 / 坑 | — | 不变 | 0 |

合计 **18 份文件各掉 1 个标签，无一例新增**。11 份掉 `build_test` 的逐份回原文核过，
证据全是词中命中（principles / behavior / TypeScript / specification / description），
**没有一例是真阳性**。结论方向**在 v0.3 时**不变：`build_test` 仍是第一名、仍明显领先第二名（65.9%）。
⚠️ 该排序在 v0.5 变了——`boundaries` 补上正文通式后与之并列第一（82.8% vs 85.7%），
且 2.9pp 的差距小于通式约 3% 的假阳性幅度，见 §13 与 `FINDINGS.md` §1。

### 11.4 一处自我更正（对外数字 84.7% → 85.7%）

09-11 我按审计脚本打印的"**23 处组合 / 其中构建测试 16 份**"直接相减，估出 `build_test` = 449 − 16
= **433 份（84.7%）**，并在 09-12 把 84.7% 写进了知乎评论、开源中国评论、掘金正文三个公开站点。
实测逐份复核后：16 处构建测试误命中里有 5 处被正文/强模式兜住、并未掉标签，**真正掉的是 11 份**，
正确值是 **438 份 = 85.7%**。

**教训**：把"有问题的组合数"当"会变动的条目数"用，是一个方向已知、幅度未验证的推算——
这类推算**不能直接进对外文案**，必须先跑一遍修复版数据、比对两份数据集的实际差集。
补的锁：`work/substring_audit.py` 现在会同时输出"误命中组合数"与"实际掉标签数"，两者不再混用。

## 12. 定义边界是**人定的**，且有的边界会直接改数字（2026-09-12）

分类准确率有个前置问题：**"这一类该不该收这段内容"是定义问题，不是规则问题。**
规则作者自己拍板会把"定义模糊"记成"规则错"，测出来的准确率不可信。

2026-09-12 由项目所有者裁定四条边界（全文见 `TAXONOMY.md`「口径裁决」），并**逐条实测了对语料库的影响**：

| # | 裁决 | 实测影响 |
|---|---|---|
| 1 | `风格` **收** Git 规范 | **0 份**（这类标题本就被 `convention` / `style` / `规范` 命中）——纯定义 |
| 2 | `环境` **收** SDK/语言版本号 | 候选 **1 份**且是探针误报 → 暂不加规则，先记口径 |
| 3 | `构建测试` **收**"该不该跑构建" | **0 份**（这类章节含 `构建/测试` 字样，本就会命中） |
| 4 | `AI行为` **不收**"标题里出现 agent/instructions" | **−35 份**：`agent_meta` 36.4% → **29.5%**（`ruleset_v0.1.4`） |

**为什么裁决 4 要减**：那类标题几乎都是**文件自己的名字**（"WSL2 Distro Manager — Agent Instructions"），
而本语料库每份文件按定义都是写给 agent 的——**标签不携带区分度**。这与 §8/v0.1b 的教训同源
（当年靠文件名叫 `AGENTS.md` 得出 79% 命中率）。抽查确认没有误伤：
真有行为内容的文件靠正文通道（`you are` / `be concise`）保住了标签。

**更宽的删除口径（再删 `agent tool`/`prompt`/`note`/`skill`/`behavior`/`workflow`）实测会到 25.2%，
未采纳**——那些章节通常是内容型，等 100 份人工核对的 precision 数据再定。

**引用须知**：v0.4 的覆盖率与 v0.3 不可直接比（同一批文件，定义边界改了）。
逐行用 `ruleset_version` 区分。

## 13. `boundaries` 的正文通式 `Do not …`：精确率不是 100%（v0.5 新增）

v0.5 之前，正文通道只认 `never commit` / `do not commit` / `must not` 与中文模式
（`严禁` / `不得` …），于是 `Do not use pnpm`、`Don't re-write it`、`must never import node:*`
这类**最常见的禁令写法全丢**。v0.5 补了通式：

```
\b(?:do not|don't|must not|must never)\s+(?!need\b|hesitate\b|worry\b|forget\b)[a-z'-]+
```

**代价（必读）**：

- **已知残余假阳性约 3%**（按文件计）。抽 34 份"只靠 1 处证据"的文件逐条看过，
  3 处是**描述句而不是禁令**：`harnesses may ignore but do not reject`、
  `workflows do not initialize submodules`、`monitors don't receive mouseUp`。
  它们都是句中陈述，不是划线。
- **刻意不再收窄**：试过"只认行首/列表项"的变体，全库只命中 26 处（句中 2606 处），
  抽查 14 处句中命中里 12 处是真禁令（`Don't replace it with whole-file rewrite`、
  `Do not ship Ollama`、`must never introduce regressions`）——**收紧会让召回塌掉**，
  不是更准。所以保留通式，把 3% 记在这里。
- **对头条数字的影响**：通式把 `boundaries` 从 **65.9%**（同一规则集下关掉通式复算，340/516；
  v0.4 旧规则 65.6% = 335/511）推到 **85.7%**，
  与 `build_test` 的 82.8% 只差 2.9pp。**2.9pp < 3% 的假阳性幅度**，
  因此不要把"禁令"报成严格第一；要么报**并列第一**，要么同时给出 **45.2%（233 份，标题通道＝
  专门开一节写禁令）** 这个副口径（`FINDINGS.md` §1、`LIMITATIONS.md` §17）。

**为什么仍然采纳**：误排除真禁令的代价更大——那是本数据集最想量化的那类知识（"划的线在哪"）。
一条描述句被算作禁令，损失一个文件的精度；一条真禁令被漏掉，损失的是这一类的**召回**，
而 `boundaries` 的价值恰恰在"八成半文件至少写过一条禁令"这个规模上。

## 14. v0.5 改了 `is_pointer` 口径——分母 511 → 516，与 v0.4 不可直接比

`is_pointer` 的两道原门（薄门 + 路由信号、作者自陈语义门）**只描述形态**，分不清
「短但写了规则」与「短且只指向别处」。100 份人工核对里的 C 组复审发现：被判指针的 7 份短文件里
**5 份是误排除**（`buttondown/docs`、`voxel51/fiftyone`、`calesthio/OpenMontage`、
`JuliusBrussee/caveman`、`MadsLorentzen/ai-job-search`）——它们各自都带着自己的约束：

| 仓库 | 字节 | 被误判的原因 | 它其实写了 |
|---|---|---|---|
| `buttondown/docs` | 314 | `See \`billing.md\`` 触发行级路由信号 | `Uses **Bun**, not npm or pnpm` + 三条具体规则 |
| `voxel51/fiftyone` | 374 | 同上 | 按目录分流的规则 + `do not add Material UI` |
| `calesthio/OpenMontage` | 384 | 语义门 `contains routing rules` | `MANDATORY: Read …` + `Do not act …` |
| `JuliusBrussee/caveman` | 734 | 薄门 + `read X.md` | `Binding routing:` 仓库归属表 |
| `MadsLorentzen/ai-job-search` | 1586 | 语义门 `thin-pointer`（**文件在描述自己的设计**） | 工作区结构 + `Do not repeat rules` |

**修法（两条）**：

1. **加反证闸**：文件里只要有它自己的规则/约束/可执行命令（祈使词、`binding`/`mandatory`、
   反引号里的构建命令），**就不是指针**——无论多薄、路由信号多强。
2. **收紧语义门**：删掉 `thin[- ]pointer`。它命中不了"本文件没有内容"的自陈，
   而是被"文件描述自己的设计"触发（`this workspace uses a unified thin-pointer design`，
   主语是 workspace 的设计，不是它指向的文件）。

**实测影响**：可用样本 **511 → 516**；仍被判为指针的只剩 2 份（`chroma-core/chroma` 的
129B 单句、`plotly/dash` 的纯链接索引页）。⚠️ 因此 **v0.5 的覆盖率与 v0.4 不可直接比**：
分母变了，且这 5 份是**短文件**（规则在它们身上几乎什么都没标出来），
排除它们等于把规则的漏标一起藏起来——收回来会让百分比**往下走**（这正是 v0.4 审计的结论）。

**同步修掉的测试坑**：原来的回归测试用的是合成文本（`See \`billing.mdoc\``，`.mdoc` 不触发
`POINTER_PAT`），所以它通过、真实文件却落在指针池里。现在这条测试直接喂真实文件正文
（`data/raw/full/buttondown__docs.md`）。**教训：闸门类回归必须直接喂真实文件。**

## 15. 代码块里的 `# 注释` 曾被当成标题（v0.1.6 起已修，v0.5 首次公开体现）

`split_sections` 曾把**任何** `# 注释` 行当标题。129/558 份文件在代码块里有这类行
（最多一份 54 行），后果是 `# 3. Build` / `# Run tests` / `# Install` 这类 **shell 注释
变成了章节标题**，往 `build_test` 灌假证据，也让 `section_count` 严重失真。

v0.1.6 起 `split_sections` 跳过围栏内的行，但**这条修正是 v0.5 才第一次体现在发布数据集里**
（v0.4 数据由 `ruleset_v0.1.4` 生成）。实测影响：

| 仓库 | `section_count` | `build_test` 标签次数 |
|---|---|---|
| `diffblue/cbmc` | 146 → **92** | 51 → **22** |
| `blakeblackshear/frigate` | 80 → **37** | 19 → **6** |

全库 `build_test` 覆盖率 **85.9% → 82.8%**（分母同时 511 → 516）。**这是修复不是回归**：
被去掉的证据是 `# Build with different SAT solver` 这类命令注释，不是章节标题。

## 16. 分类准确率的实测值（100 份人工核对，v0.5）——以及为什么它是**上界**

v0.1 那轮"抽 12 份：准确 9 / 漏标 3 / 错标 0"是自述、样本小、且**是规则作者自己判自己**。
v0.5 补上了真正的测量：100 份配额样本，其中 **26 份中文由用户独立盲判**（第二标注者），
其余由本智能体盲判（只看原文、不看规则输出）。

**微平均（分组，不合并）**：

| 组 | 份数 | 标注者 | 完全一致 | precision | recall |
|---|---|---|---|---|---|
| A 主样本（唯一能代表全体） | 55 | 本智能体 | 13% | **90%** | **75%** |
| B 中文普查 | 26 | **用户** | 8% | **80%** | **79%** |
| D 稀有类加成 | 9 | 本智能体 | 11% | **92%** | **84%** |

**逐类最弱（A 组）**：`gotchas` recall **38%**、`overview` 41%、`agent_meta` 55%。
共同点是"内容散在正文里、没有专门章节"——标题通道结构性命中的盲区（见 §2「分类失败的已知位置」）。

**B 组（中文，用户判）暴露的是另一面**：`build_test` precision 仅 **61%**
（「运行 / 命令 / 启动」这类动词在中文散文里被当成构建证据）、`gotchas` 67%、`style` 71%。
⇒ **中文样本上的"构建测试"被高估**，这是配额样本结论（n=26），不要外推到全体。

⚠️ **三条必读的口径限制**：

1. **这是 in-sample 上界**，不是留出集估计：这 100 份**正是驱动 v0.5 规则改动的那批样本**。
   真正的准确率估计需要新一轮未参与改规则的样本。
2. **"完全一致"很低（8–13%）是正常的**：它要求 9 类逐类全中，而人判本身在 1–2 类上就会犹豫
   （A 组 55 份里标了 95 处「犹豫」）。**precision / recall 才是该引用的数字。**
3. **换一个判读者，数字会动**：A 组（本智能体判）错标少漏标多、B 组（用户判）两边都多。
   这正是 §12"定义边界是人定的"在数字上的体现——引用时请带上标注者。

复算：`.venv/bin/python work/audit/human_vs_rule_v0.5.py`（逐类表 + 逐份分歧清单）。

## 17. 「专门开一节写禁令 = 59.9%」这个数字是错的（自查发现，**从未对外发布**）

v0.5 文档（`FINDINGS.md` §1/§2、`README.md`、`SHARE.md`）里曾写
"**59.9%（309 份）** 的文件**专门开了一节**写禁令"。

**它就是错的，正确值是 45.2%（233 / 516）。**

- 定义（现在写明）：**章节标题命中 `HEAD_RULES["boundaries"]`** 的文件数。
  "专门开一节写禁令"的字面含义就是标题通道，没有第二种读法。
- 原值是怎么进来的：写进文档时**没有记录定义**，事后无法复算——用三种可定义的口径都对不上
  309：标题通道 **233**、标题 ∪ 强模式 **302**、全文兜底通道 **315**。
  最接近的是"标题 ∪ 强模式"在 **v0.1.7 旧规则**下的 **308**（仍差 1 份，且旧规则已废）。
  ⇒ 判定为不可复算的数，作废重报，并且**从此把该口径的定义写进表格**。
- 影响面：这是**副口径**，头条数字 `boundaries` **85.7%（442/516）**不受影响，
  九类覆盖率与排序也不受影响。三站文案（`work/share-paste/`）**没有引用过**这个数，
  所以没有一条对外渠道被污染。
- 教训：**主数字写了"可由 parquet 直接复算"，子口径也必须**。只要一句话里出现
  "其中 X% 是 Y"，X 的定义就要当场写进定义列并留下脚本——这次漏了，靠自查才抓出来。

复算：`.venv/bin/python work/audit/boundaries_channels.py`（三个口径一次打印）。
⚠️ 这个脚本要读原始快照 `data/raw/`（含 558 份原文，**不进库**），所以在仓库里只能本地跑；
**只靠 `data/processed/*.parquet` 能复算的是口径 3（85.7%）**。

## 18. 留出集：本语料库内**做不出中文的**留出估计（2026-09-13）

前面 §16 的准确率是 **in-sample**（那 100 份正是驱动 v0.5 改规则的样本）。为了给一个
真正留出的数字，按下面的口径重新抽样：

- **排除**：v0.4/v0.5 改规则用过的**全部**样本——`work/audit/v0.4-sample.json` 里
  `identity` 各组的并集，去重后 **106 份**（主样本 55 + 中文普查 26 + 边界件 10 + 稀有类 9，
  外加盲判子集与指针组与前三组重叠）。
  ⚠️ 只能按 `repo + file_path` 认身份，**不能只按 `file_sha`**：空壳文件几十个仓库共用一个
  sha，按 sha 排除会连带误杀。
- **留出抽样**（`work/audit/v0.5-holdout-sample.json`，seed `20260913`，可复现）：
  排除后可用池 **422 份**，从中无分层随机抽 **主样本 55**（只有这组代表全体），
  另抽稀有类加成 9、边界件 4（分组报告，不并入整体）。**实测与已用样本零重叠。**
  算术关系（别把两个数直接相减）：106 份排除项里只有 **94 份**落在实质基座内，
  另 10 份是非实质空壳、2 份是转引用文件（本就不在基座里），故 516 − 94 = **422**。
- ⚠️ **中文留出集 = 0**：语料库里 **25 份中文全部在上一轮用掉了**。所以
  "中文上 `build_test` precision 61%"（§16）这个数字**在本语料库内无法做留出验证**——
  要验只能**新采集中文章程**，这是下一轮采集的目标。
- 指针的留出集也 = 0（2 份都在上一轮用过）；非实质空壳还剩 30 份。

复算：`.venv/bin/python work/audit_sample.py --main 55 --edge 4 --rare 9 --blind 55 \
--seed 20260913 --version v0.5-holdout --exclude work/audit/v0.4-sample.json`
（脚本 2026-09-13 加了 `--exclude` / `--note` 两个参数；不加 `--exclude` 时行为与旧版一致）。

## 19. 留出集实测：precision 92% / recall 70%（2026-09-13，55 份盲判）

§16 那 100 份是 **in-sample**（正是驱动 v0.5 改规则的样本），§18 抽出了真正的留出集。
现在把**盲判做完了**：55 份主样本，逐份只看原文、不看规则输出，判完才打开规则结果对照。

口径与结果（完整表 `work/audit/v0.5-holdout-vs-rule.md`，可复算）：

- **主口径**：只算盲判确认的类别；判「犹豫」的类别单列，不计漏标/错标（同 §16）。
- 55 份中文件级完全一致 **7 份（12.7%）**；漏标 **93** 处、错标 **19** 处。
- **微平均 precision 92%｜recall 70%**（TP 221 / FP 19 / FN 93）。
- 逐类 recall：`build_test` 95% / `boundaries` 88% / `structure` 78% / `workflow` 78% /
  `style` 79% / `environment` 57% / `agent_meta` 45% / `overview` 40% / `gotchas` **32%**。
- 逐类 precision：`structure` 100% / `gotchas` 100% / `style` 97% / `workflow` 97% /
  `agent_meta` 93% / `overview` 92% / `build_test` 89% / `boundaries` 88% / `environment` 77%。

**与 in-sample 对照（同一规则、同一标注者，只换样本）**：

| 指标 | in-sample A 组 55 份（§16） | 留出集 55 份（本节） |
|---|---|---|
| 微平均 precision | 90% | **92%** |
| 微平均 recall | 75% | **70%** |
| 漏标 / 错标 | 79 / 25 | **93 / 19** |

**结论：v0.5 的规则改动没有明显过拟合。** 两组只差 3–5pp 且方向一致（precision 略升、
recall 略降），说明 §16 的 in-sample 数字**没有**被显著高估，可以在说明口径后引用。
逐类看，留出集上 `build_test`、`style` 比 in-sample 好；`environment`（57% vs 77%）
和 `boundaries`（88% vs 98%）明显更差，值得下一轮单独看分歧清单。

**这个数字的边界（别过度引用）**：

- **单一标注者**：55 份的盲判是本智能体做的，没有第二人独立复判；§16 的 B 组（用户判）
  已证明换人判数字会移动，所以 92%/70% 只对「本标注者 + 本分类定义」成立。
- **没有中文、没有指针文件、没有非实质空壳**：这三类在留出集里都是 0（§18），
  中文的 precision/recall 仍然没有留出验证。
- **「完全一致」很低是正常的**：9 个类别任取子集，两份判断逐类全中才算完全一致；
  这个数是严格口径，不代表 87% 的分歧都严重——多数分歧是 1–2 个类别。
- **漏标仍是主要短板**（93 vs 19），且集中在 `gotchas`/`overview`/`agent_meta`/
  `environment` 四类——与 §16 同向：它们都散落在正文里、没有专门章节，是标题通道的盲区。

复算：`.venv/bin/python work/audit/holdout_vs_rule_v0.5.py`
（盲判原文 `work/audit/v0.5-holdout-calls-a1..a7.json`，抽样 `work/audit/v0.5-holdout-sample.json`）。

## 20. 留出集错标归因（10 处）+ 一处**口径与实现不符**的自披露（2026-09-13）

本节两件事，**第二件需要人裁定，我没有动任何对外数字**。

**（一）错标归因**：留出集里与 in-sample 差得最远的两类是 `environment`（precision 77% /
recall 57%，A 组 83% / 77%）与 `boundaries`（88% / 88%，A 组 92% / 98%）。这 10 处错标
逐条读过原文，**全部落在两个机制里**：

- **标题词多义 5 处**：`dependency` / `tool` / `setup` / `config` / `rules` 各自横跨两个语义域
  （依赖更新流程、tool 抽象、协作范式、provider 配置归属、lint 规则），标题通道只认词面。
- **正文命中 5 处**：4 处是「句式像禁令、语义不是禁令」（流程顺序 `Do not mark …`、
  代码约定 `don't inline`、风格打包 `don't reformat` / `don't ship`、纯描述句 `don't receive`），
  1 处是配置文件名 `.env.example` 出现在讲发布流程的句子里。

10 处的证据都只有 1–2 条（`environment` 5 份全是 1 条），与 §13 说的「残余假阳性集中在
只靠 1 处证据的文件里」一致。逐条证据与判断：
`work/audit/v0.5-holdout-mislabel-attribution.md`（脚本 `work/audit/mislabel_attribution.py`）。
按 §3.2 / D11，**只归因、不改规则、不提改法**。

**（二）口径与实现不符（自披露）**：§19 与两份 `work/audit/*.md` 报告的抬头都写着
「人判**犹豫**的类别单列，**不计入**漏标/错标，另给宽松口径作对照」。但两个脚本的主口径
都是 `for c in rule - call` ——**犹豫项实际被计进了错标**，且 in-sample 脚本里算好的
宽松口径（`bl_fp`）**从未打印**，报告里找不到那一列。实测（同一批 55 份、同一批规则）：

| 口径 | TP | FP | FN | precision | recall |
|---|---|---|---|---|---|
| 现行实现（犹豫**计入**错标） | 221 | 19 | 93 | **92%** | **70%** |
| 文档口径（犹豫**出局**） | 233 | 7 | 93 | **97%** | **71%** |

⇒ 对外引用的 **92% / 70% 是两者里更保守的那个**（把 12 处「我也拿不准」记在规则头上）；
按文档写的口径复算会得到 97% / 71%。**这不是数字算错，是「文档描述的口径」与
「代码实际的口径」不一致**——引用者照文档复算会对不上。

两组（in-sample 与留出集）的**主口径相同**，所以 §16 与 §19 的「差 3–5pp」仍是同口径比较，
过拟合结论不受影响。**采用哪条口径对外，属人裁定**（D32）；在此之前本仓库不改对外数字、
不改规则、不改脚本行为，只把事实记在这里。
