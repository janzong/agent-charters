# D 稀有类组·第三批（3 份，D 组收官）盲判裁决

- 日期：2026-09-13 ｜ 规则版本：`ruleset_v0.1.7`（只判不改）
- 盲判落盘：`work/audit/v0.4-blind-calls-d3.json`
- 口径纪律：D 是配额样本，**只报分组结果、不并入整体**。

## 一、逐份对照

| 仓库 | 字节 | 规则输出 | 我的盲判 | 裁决 |
|---|---|---|---|---|
| `lemonade-sdk/lemonade` | 19639 | overview, structure, build_test, style, workflow, environment, boundaries, **agent_meta**（8） | overview, structure, build_test, style, environment（+workflow 犹豫） | **错标 ×1（文件级）**：agent_meta 的唯一证据是 `## Code Style` 正文里的 "**PR descriptions should be concise**"（`\bbe concise\b`）——**发现 34 的第 7 例，该模式至此 7/7 全假**，且这是在 152 行的文件里把 agent_meta 整个类拉起来。**人错 ×1**：`## Critical Invariants`（真 boundaries 章节）我盲判漏了；`## Contributing`（真 workflow）我判成犹豫、规则命中——两项都算我错。**发现 16 又一实证**：`## Build Commands` 与 `## Testing` 的 ```bash 块里 10 行注释被当标题（`# 1. Setup…`、`# 2. Build C++ server`、`# 5. Windows MSI installer (WiX 5.0+ required)`、`# 6. macOS signed installer`、`# CLI tests…`、`# Endpoint tests…`、`# LLM tests…`、`# Audio transcription tests`、`# Image generation tests (slow)`、`# C++ unit tests`），其中两条还额外产出 environment |
| `HKUDS/LightRAG` | 25237 | overview, structure, build_test, style, workflow, environment, **boundaries**, gotchas（8） | overview, structure, build_test, style, environment, workflow, gotchas（7） | **人错 ×1**：我漏了 boundaries——该文件确有两处硬约束与禁令（`### LightRAG Initialization (Critical)` 的"**always** call `await rag.initialize_storages()`"、`### Testing` 的"Bun built-in runner（**NOT** Vitest/Jest）"），规则通过**全文兜底通道**命中，方向对（但要记一笔：**这条标签是"整篇扫出来"的，不是任何章节的结论**）。**段级错标 ×1**：`## Key Implementation Patterns` 靠 `heading:pattern` 拿 style（那是用法示例＝structure）。**发现 16 又一实证（本批最大）**：19 个假标题进统计（`# Install with uv`、`# Install with API support`、`# Copy and configure environment`、`# Build WebUI`、`# Run server`、`# Run specific test file`、`# Full suite — ~7000 tests…`、`# Run with custom workers`、`# Configure batch size`、`# Query Configuration`…），其中 environment 被灌进 7 条、build_test 被灌进 6 条 |
| `can1357/oh-my-pi` | 28777 | structure, build_test, style, workflow, environment, boundaries（6） | 同 6 + agent_meta（7） | **漏标 ×1（文件级）**：agent_meta——`## Default Context` 的术语澄清"用户说 agent 时指 `packages/coding-agent/` 的实现，**not you (the assistant)**"是对 AI 自身身份的明确界定，0 命中。**段级错标 ×1**：`### Streaming tool previews` 靠 `heading:tool` 拿 environment（讲 TUI 流式预览＝style/structure，发现 9 又一例）。**发现 42 又一例**：`### Quick reference`（Node/Bun 常用 API 对照）无标签——v0.1.7 撤掉该词后这里也没有替代通道。**注**：`## GitHub` 整节（发评论/开 issue/改 PR 的授权纪律，含一串 MUST/NEVER）拿到 boundaries 是对的，但它同时是 workflow 的强候选（规则只给了 boundaries） |

**小结**：3 份规则 22 个标签、人工 20 个（我错 2 处）；文件级漏标 1、错标 1；段级错标 2；发现 16 现场 29 个假标题。

## 二、发现 34 收口｜`\bbe concise\b` 7/7 全假

上批在 `openai-agents-python` 上量到 6/6 全假，本批 lemonade 撞上第 7 例
（"**PR descriptions should be concise**"）——**且这一次它把整份文件的 agent_meta 拉了起来**。
累计 7 处上下文：文档要简洁（ComfyUI）、摘要要简洁（doris/lemonade）、PR 描述要简洁（lemonade）、
commit message 要简洁（openai-agents-python）、单测要简洁（palmier-pro）、标题要简洁（photoprism）。
⇒ **v0.1.8 直接删**，无讨论空间。

## 三、发现 16 的三份新现场（D 组合计 29 个假标题）

D 组最后三份里，lemonade 10 个、LightRAG 19 个、oh-my-pi 0 个。
加上 A 组的实测（86/558 触发逃逸守卫、修掉后 15–18 份掉标签、**掉的标签全是假阳性**），
修法（看围栏语言标记）在 D 组也能覆盖：这三份的围栏分别标了 `bash` / `sh` / `bash`，
按"非 markdown 语言标记一律当代码块"处理即可全部消掉。

**顺带修正一处口径**：LightRAG 的 boundaries 来自**全文兜底通道**（整篇扫，不是章节结论）。
这类"整篇命中"的标签在报告里要能区分开——它是**最弱的证据形态**（正文里偶然出现一个 `must not`，
整份文件就被记一笔），建议 v0.1.8 在数据集里给标签加一列 `evidence_level`（section / doc-fallback）。

## 四、D 稀有类组收官

- **D 稀有组 9/9 完成**（grok-cli、mini-kode、ai-website-cloner-template、qvopenapi-rs、CORAL、apidash、lemonade、LightRAG、oh-my-pi）
- D 组整体统计（配额样本，**不并入 511 基座**）：
  - 9 份里规则给 62 个标签、人工给 65 个
  - 文件级漏标 8、错标 4；段级错标 7、段级漏标 20+
  - **gotchas 是 D 组的设计目标**：9 份里人工判有 gotchas 的 5 份（qvopenapi-rs、CORAL、apidash、LightRAG、lemonade?），
    规则命中 5 份——**方向一致、数量一致**，但其中 2 份（CORAL、lemonade）的 gotchas 是**蹭到的**（章节标题里有 gotcha/pitfall 词就中，正文其它节的真 gotcha 反而漏）
- 进度：A 55/55 ✅ ｜ D 9/9 ✅ ｜ **B 中文余 16（待用户盲判）**
- 发现累计 **46 条**（本批无新增编号，发现 34/16/42 各补实证）
- 下一步：等 B 组的 16 份盲判结果 → 与 C/A/D 一起汇成 v0.1.8 的改动清单
