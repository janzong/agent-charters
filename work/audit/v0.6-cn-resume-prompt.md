# v0.6 中文留出集 · 接力简报（新会话直接读这份开工）

> 用途：会话上下文过长时换新会话接力。本文件自包含，读完即可动手；背景细节在
> `STATE.md` §0/§3/§5 与 `LIMITATIONS.md` §18/§19/§20。
> 最后更新：2026-09-13（盲判 **42/50**）

## 任务

**继续盲判 v0.6 中文留出集，把 50 份判完。** 剩 **8 份**：
`masonsxu/cloudwego-microservice-demo`（16.9KB）、`skywind3000/vim`（16.6KB）、
`whtoo/How_to_implment_PL_in_Antlr4`（14.2KB）、`moyanj/lunaris`（9.3KB）、
`wind0ws/MyToolbox`（8.9KB）、`ndnhatvien/Awesome-Context-Engineering`（8.4KB）、
`zhangjunmengyang/quant-research-platform`（6.7KB）、`xcanwin/KeepChatGPT`（4.8KB）。

## 三步

1. **读原文判**：按 `work/audit/v0.6-cn-worksheet.md` 的序号顺序，打开
   `data/raw/cn/<owner>__<repo>.md`，列出它**实际有什么**（9 类，可多选）。
   判之前**不要跑 `agent-charters`、不要看任何规则输出**——这是留出集，先判后比。
   工具单次输出有硬截断（约 6k tokens），大文件用 `sed -n 'A,Bp'` 分块读（最长 28.6KB）。
2. **落盘**：每判完一批（4–8 份）新建 `work/audit/v0.6-cn-calls-aN.json`，格式抄
   `v0.6-cn-calls-a1.json`（`{_note, batch, files: {repo: {call: [], borderline: [], why: ""}}}`）。
   下一个批次号 = `a{已有文件数+1}`（现有 a1–a9，所以从 **a10** 开始）。
3. **出对照**：`.venv/bin/python work/audit/cn_vs_rule_v0.6.py`
   → 刷新 `work/audit/v0.6-cn-vs-rule.md`（首行打印覆盖度，末尾列出尚未判读名单，
   并告诉你下一个批次该叫 `aN`）。

判完可用 `.venv/bin/python -m pytest -q`（基线 138 passed）确认没碰坏东西。

## 口径与纪律（别丢）

- 分类定义在 `agent_charters/taxonomy.py` 的 `CATEGORIES`：**定义可以看，规则输出不可以看**。
- 两类口径都要给：**现行实现**把「犹豫」计入错标（保守），**文档口径**把它出局（见 §20）。
- `call` = 确认有；`borderline` = 拿不准（单列）；`why` 写清判据，便于后续复核。
- **覆盖不满 50 份前，任何 precision/recall 都不得对外引用**（只能看趋势）。
- 框偏向中文：抓到的 1046 份里 97% 判 `zh`，这是「用中文关键词检索」的产物，
  不能读成 GitHub 的真实语言分布。

## 已知的判读手感（前 14 份总结，供对齐）

- 中文章程的 `agent_meta` 明显多于英文：如「必须先回答 yes sir」「用中文回复」
  「分支名不要默认加 `codex/` 前缀」——判这一类要多看开头段与「约定」段。
- `environment` 常以包管理器形式出现：`uv` / `conda` / `bun` / `npm` + 环境激活步骤。
- `gotchas` 多是「改了 X 要重跑 Y」「某工具不支持 Z」这类一句话提醒，很少独立成节。
- 纯指针型文件（只列文档入口）通常只拿 `build_test` / `boundaries` / `agent_meta` 中的一两类。

## 不要做的事

- 不要重新辩论 §3 不可争议区（不发布原文全文、不放宽判据、不为准确率无限迭代分类器）。
- 不要改规则、不要改标题词表——盲判阶段只记录，规则改动要人裁定（D32）。
- 有一件**仍待用户裁定**、与本任务无关的事：§19 的口径描述与实现不符
  （92%/70% vs 97%/71%，见 §20）。**别顺手改数字。**
