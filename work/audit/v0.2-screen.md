# v0.2 人工核对 —— 机器预筛（不构成判定）

> 本文件由 `work/audit_screen.py` 生成，只做**证据分级**与**可疑命中标记**，不做准确/漏标判定。

> 可疑 ≠ 错误：`bogus_head` 只说明「这个标签是靠子串碰上的」，该类内容可能确实存在。


## 主样本（随机 30）

### 1. `AAswordman/Operit`（禁令,构建测试,流程）

- 禁令（标题）：执行准则 ← heading:准则
- 构建测试（全文强模式）：[全文兜底命中]
- 流程（全文规则）：[全文兜底命中]

### 2. `Kiln-AI/Kiln`（AI行为,禁令,构建测试,环境,概览,流程）

- AI行为（标题）：Agent Tools ← heading:agent tool
- 禁令（标题）：Never Make Legal Decisions as an Agent ← heading:never
- 构建测试（标题）：Never Make Legal Decisions as an Agent ← heading:ci  ⚠ bogus_head:ci@Never Make Legal Decisions as an
- 环境（标题）：Agent Tools ← heading:tool
- 概览（标题）：Project Overview ← heading:overview
- 流程（标题）：Code Review Guidelines ← heading:review

### 3. `KomorGiaoGiao/FirstCoder`（禁令,构建测试,环境,架构,风格,流程）

- 禁令（正文）：Security & Configuration Tips ← body:\bdo not commit\b
- 构建测试（标题）：Build, Test, and Development Commands ← heading:build
- 环境（标题）：Security & Configuration Tips ← heading:config
- 架构（标题）：Project Structure & Module Organization ← heading:structure
- 风格（标题）：Coding Style & Naming Conventions ← heading:style
- 流程（标题）：Commit & Pull Request Guidelines ← heading:pull request

### 4. `OpenBMB/UltraRAG`（AI行为,禁令,构建测试,环境,架构,风格,流程）

- AI行为（标题）：16.5 Modify UI pipeline behavior ← heading:behavior
- 禁令（标题）：6) Variable Resolution and Data Flow Rules ← heading:rule
- 构建测试（标题）：Phase A: Build ← heading:build  ⚠ bogus_head:ci@9) Retriever/Generation/Prompt S, bogus_head:ci@1) Install dependencies
- 环境（标题）：5.5 Prompt vs Tool step semantics ← heading:tool
- 架构（标题）：2) Repository Map (What Matters Most) ← heading:repository map
- 风格（标题）：8.1 Registration styles ← heading:style
- 流程（标题）：5.4 Branch block ← heading:branch

### 5. `QuintinShaw/pi-dynamic-workflows`（流程）

- 流程（标题）：Workflow documentation ← heading:workflow

### 6. `VoltAgent/voltagent`（构建测试,坑,概览）

- 构建测试（正文）：Validating Changes ← body:```(?:bash|sh|shell)?\n[^`]{
- 坑（标题）：Gotchas ← heading:gotcha
- 概览（标题）：What is VoltAgent? ← heading:what is

### 7. `bostrot/wsl2-distro-manager`（AI行为,禁令,构建测试,坑,概览,架构,风格,流程）

- AI行为（标题）：WSL2 Distro Manager — Agent Instructions ← heading:agent instruction
- 禁令（全文规则）：[全文兜底命中]
- 构建测试（标题）：Commands ← heading:command
- 坑（标题）：fluent_ui Gotchas ← heading:gotcha
- 概览（标题）：What This Is ← heading:what this is
- 架构（标题）：Key Directories ← heading:key director
- 风格（标题）：State Management Patterns ← heading:pattern
- 流程（标题）：Commits ← heading:commit

### 8. `career-ops-hq/career-ops`（AI行为,禁令,构建测试,概览,风格）

- AI行为（标题）：Personalization ← heading:persona
- 禁令（标题）：Source-of-Truth Boundary (CRITICAL) ← heading:boundar
- 构建测试（标题）：First Run — Onboarding (IMPORTANT) ← heading:run
- 概览（标题）：What is career-ops ← heading:what is
- 风格（标题）：Stack and Conventions ← heading:convention

### 9. `deepseek-ai/deepseek-harness`（禁令,构建测试,架构,风格,流程）

- 禁令（正文）：Secrets / .env ← body:\bnever commit\b
- 构建测试（标题）：Commands ← heading:command
- 架构（标题）：Repository layout ← heading:layout
- 风格（标题）：Conventions ← heading:convention
- 流程（标题）：Pre-stable APIs and released Session data ← heading:release

### 10. `get-bb/bb`（禁令,构建测试,流程）

- 禁令（正文）：Build And Test ← body:\bnever commit\b
- 构建测试（标题）：Task Completion ← heading:task
- 流程（标题）：Issues, Pull Requests, And Debugging ← heading:pull request

### 11. `google/adk-python`（环境,概览,架构,风格）

- 环境（标题）：Development Setup ← heading:setup
- 概览（标题）：Project Overview ← heading:overview
- 架构（标题）：ADK Knowledge, Architecture, and Style ← heading:architecture
- 风格（标题）：ADK Knowledge, Architecture, and Style ← heading:style

### 12. `humeo/code-helm`（禁令,构建测试,环境,架构,风格,流程）

- 禁令（正文）：Security & Configuration Tips ← body:\bnever commit\b
- 构建测试（标题）：Build, Test, and Development Commands ← heading:build
- 环境（标题）：Security & Configuration Tips ← heading:config
- 架构（标题）：Project Structure & Module Organization ← heading:structure
- 风格（标题）：Coding Style & Naming Conventions ← heading:style
- 流程（标题）：Commit & Pull Request Guidelines ← heading:pull request

### 13. `koala73/worldmonitor`（禁令,构建测试）

- 禁令（标题）：Critical boundaries ← heading:boundar
- 构建测试（全文规则）：[全文兜底命中]

### 14. `langchain-ai/deepagents`（AI行为,禁令,构建测试,坑,架构,风格,流程）

- AI行为（正文）：Development workflow ← body:\byou are\b
- 禁令（标题）：Suppressing ruff rules ← heading:rule
- 构建测试（标题）：Core development principles ← heading:ci  ⚠ bogus_head:ci@Core development principles, bogus_head:ci@SDK and dependencies
- 坑（标题）：Warnings are errors ← heading:warning
- 架构（标题）：Global development guidelines for the Deep Agents monorepo ← heading:monorepo
- 风格（标题）：PR conventions ← heading:convention
- 流程（标题）：Development workflow ← heading:workflow

### 15. `mercurialsolo/claudectl`（构建测试,环境,架构,风格）

- 构建测试（标题）：Build & Test ← heading:build  ⚠ bogus_head:ci@Key Design Decisions
- 环境（标题）：Key differentiators vs. similar tools ← heading:tool
- 架构（标题）：Architecture ← heading:architecture
- 风格（标题）：Conventions ← heading:convention

### 16. `minmaxflow/mini-kode`（构建测试,概览,架构,风格,流程）

- 构建测试（标题）：Development Commands ← heading:command
- 概览（标题）：Technology Stack ← heading:technology stack
- 架构（标题）：Architecture ← heading:architecture
- 风格（标题）：Code Style Guidelines ← heading:style
- 流程（标题）：Release Process ← heading:release

### 17. `netdata/netdata`（AI行为,禁令,构建测试,环境,架构,风格,流程）

- AI行为（正文）：Working With The User ← body:\byou are\b
- 禁令（标题）：Repository Rules ← heading:rule
- 构建测试（标题）：Development Principles ← heading:ci  ⚠ bogus_head:ci@Development Principles, bogus_head:ci@Scope Discipline At Every Step, bogus_head:ci@Followup Discipline, bogus_head:ci@Durable AI-Facing Artifact Forma
- 环境（标题）：Requirement Language ← heading:requirement
- 架构（标题）：Local-Only Working Directory ← heading:directory
- 风格（标题）：Durable AI-Facing Artifact Formatting ← heading:format
- 流程（标题）：Git And PR Workflow ← heading:workflow

### 18. `open-mercato/cezar`（禁令,构建测试,环境,架构）

- 禁令（正文）：Task routing ← body:\bnever\b.{0,40}\b(?:secret|
- 构建测试（标题）：Task routing ← heading:task
- 环境（标题）：Zero config ← heading:config
- 架构（标题）：Repository layout ← heading:layout

### 19. `patriceckhart/zot`（AI行为,禁令,构建测试,环境,架构,流程）

- AI行为（标题）：Terminal behavior ← heading:behavior
- 禁令（正文）：Code ownership map ← body:\bmust not\b
- 构建测试（标题）：Starting a task ← heading:task
- 环境（标题）：Tools and confinement ← heading:tool
- 架构（标题）：Code ownership map ← heading:ownership
- 流程（全文规则）：[全文兜底命中]

### 20. `pydantic/pydantic-ai`（AI行为,禁令,构建测试,环境,架构,流程）

- AI行为（正文）：Your primary responsibility is to the project and its users ← body:\byou are\b
- 禁令（正文）：Requirements of all contributions ← body:\bmust not\b
- 构建测试（标题）：Gathering context on the task ← heading:task
- 环境（标题）：Requirements of all contributions ← heading:requirement
- 架构（标题）：Your primary responsibility is to the project and its users ← heading:responsibilit
- 流程（标题）：Development workflow ← heading:workflow

### 21. `sooperset/mcp-atlassian`（禁令,构建测试,坑,概览,架构,风格,流程）

- 禁令（标题）：Rules ← heading:rule
- 构建测试（标题）：Running the server ← heading:run
- 坑（标题）：Gotchas ← heading:gotcha
- 概览（标题）：Quick reference ← heading:quick reference
- 架构（标题）：Repository map ← heading:repository map
- 风格（标题）：Code conventions ← heading:convention
- 流程（标题）：Dev workflow ← heading:workflow

### 22. `streamlit/streamlit`（构建测试,概览,架构,流程）

- 构建测试（标题）：Shell & Build Policy ← heading:build
- 概览（标题）：Streamlit Repo Overview ← heading:overview
- 架构（标题）：Folder Structure ← heading:structure
- 流程（全文规则）：[全文兜底命中]

### 23. `yashdev9274/supercli`（AI行为,构建测试,环境,架构,风格）

- AI行为（标题）：Agent Instructions for Supercode Monorepo ← heading:agent instruction
- 构建测试（标题）：Build Commands ← heading:build  ⚠ bogus_head:script@Types & TypeScript, bogus_head:ci@Key Dependencies
- 环境（标题）：Environment Variables ← heading:environment
- 架构（标题）：Agent Instructions for Supercode Monorepo ← heading:monorepo
- 风格（标题）：Code Style Guidelines ← heading:style

### 24. `zgsm-ai/costrict`（无）


### 25. `Smith42/astroPT`（构建测试,风格）

- 构建测试（标题）：Build/Lint/Test Commands ← heading:build
- 风格（标题）：Code Style Guidelines ← heading:style

### 26. `Weaverse/.agents`（AI行为,构建测试,环境,架构,风格,流程）

- AI行为（正文）：Agent/Command Content Structure ← body:\byou are\b
- 构建测试（标题）：Build, Lint & Test Commands ← heading:build  ⚠ bogus_head:script@Description of what it does, bogus_head:ci@Check dependencies before using , bogus_head:script@Agent Invocation Pattern (in des
- 环境（标题）：Validate JSON configuration ← heading:config
- 架构（标题）：Repository Structure ← heading:structure
- 风格（标题）：Code Style Guidelines ← heading:style
- 流程（标题）：Git Commands in Workflows ← heading:workflow  ⚠ bogus_head:commit@Show uncommitted changes

### 27. `ag-grid/ag-charts`（AI行为,禁令,构建测试,环境,概览,流程）

- AI行为（标题）：AI Agent Instructions ← heading:agent instruction
- 禁令（标题）：Critical Rules ← heading:rule
- 构建测试（全文规则）：[全文兜底命中]
- 环境（标题）：Pre-Commit Requirements ← heading:requirement
- 概览（标题）：Project Overview ← heading:overview
- 流程（标题）：Pre-Commit Requirements ← heading:commit

### 28. `jantimon/web-performance-debugger`（流程）

- 流程（全文规则）：[全文兜底命中]  ⚠ fulltext_only

### 29. `moKshagna-p/musico`（AI行为,禁令,构建测试,概览,架构,风格,流程）

- AI行为（正文）：Musico - Project Context for LLMs ← body:\byou are\b
- 禁令（标题）：Architectural Rules & Guidelines for LLMs ← heading:rule
- 构建测试（全文规则）：[全文兜底命中]
- 概览（标题）：Musico - Project Context for LLMs ← heading:project context
- 架构（标题）：Monorepo Structure ← heading:structure
- 风格（标题）：Frontend Conventions (`apps/web`) ← heading:convention
- 流程（标题）：Development Workflow ← heading:workflow

### 30. `peteromallet/Arnold`（AI行为,禁令,构建测试,环境,风格,流程）

- AI行为（标题）：Filling feedback with subagents ← heading:subagent
- 禁令（标题）：Step Rules ← heading:rule
- 构建测试（标题）：Settled Decisions Section Format ← heading:ci  ⚠ bogus_head:ci@Settled Decisions Section Format, bogus_head:ci@Settled Decisions, bogus_head:ci@Settled Decisions, bogus_head:ci@Gate Principle
- 环境（标题）：Configuration ← heading:config
- 风格（标题）：Settled Decisions Section Format ← heading:format
- 流程（标题）：Workflow ← heading:workflow


## 中文补充 10

### 1. `DSH-APP/DSHA`（禁令,概览,架构,风格）

- 禁令（标题）：技术约束（围绕这些设计） ← heading:约束
- 概览（标题）：一句话 ← heading:一句话
- 架构（标题）：分层与归属（改代码前先看这里） ← heading:归属
- 风格（标题）：编码约定 ← heading:约定

### 2. `doocs/md`（AI行为,构建测试,环境,架构,风格,流程）

- AI行为（标题）：Agent Instructions ← heading:agent instruction
- 构建测试（标题）：常用命令 ← heading:命令  ⚠ bogus_head:build@allowBuilds
- 环境（标题）：依赖管理 ← heading:依赖
- 架构（标题）：Monorepo 结构 ← heading:monorepo
- 风格（标题）：Git 规范 ← heading:规范
- 流程（全文规则）：[全文兜底命中]

### 3. `liaohch3/claude-tap`（禁令,构建测试,架构,流程）

- 禁令（标题）：Documentation Boundary ← heading:boundar
- 构建测试（标题）：测试 ← heading:测试
- 架构（标题）：标准目录 ← heading:目录
- 流程（标题）：Review Guidelines ← heading:review

### 4. `op7418/CodePilot`（AI行为,禁令,构建测试,概览,流程）

- AI行为（标题）：项目协作定位 ← heading:协作
- 禁令（标题）：共享规则入口（不在此重复，按需读） ← heading:规则
- 构建测试（标题）：自检命令（Codex 跑测试时） ← heading:测试
- 概览（标题）：项目协作定位 ← heading:定位
- 流程（标题）：Codex review 规则 ← heading:review

### 5. `rullerzhou-afk/clawd-on-desk`（禁令,构建测试,坑,概览）

- 禁令（标题）：Constraints ← heading:constraint
- 构建测试（标题）：Common Commands ← heading:command
- 坑（标题）：High-Risk Gotchas ← heading:gotcha
- 概览（标题）：Project Overview ← heading:overview

### 6. `xerrors/Yuxi`（禁令,构建测试,风格,流程）

- 禁令（标题）：证据规则 ← heading:规则
- 构建测试（正文）：证据规则 ← body:```(?:bash|sh|shell)?\n[^`]{
- 风格（标题）：Yuxi Agent 开发约定 ← heading:约定
- 流程（标题）：Review 与交付 ← heading:review

### 7. `aotemiao/artemis`（禁令）

- 禁令（标题）：产出要求 ← heading:要求

### 8. `gqcn/agentbox`（禁令,构建测试,环境,概览,架构,风格,流程）

- 禁令（标题）：顶级规范要求 ← heading:要求
- 构建测试（标题）：单元测试规范 ← heading:测试
- 环境（标题）：开发工具与脚本规范 ← heading:工具
- 概览（标题）：项目定位统一要求 ← heading:项目定位
- 架构（标题）：架构设计规范 ← heading:架构
- 风格（标题）：顶级规范要求 ← heading:规范
- 流程（标题）：开发流程规范 ← heading:流程

### 9. `linaproai/linapro`（禁令,构建测试,环境,概览,架构,风格,流程）

- 禁令（标题）：顶级规范要求 ← heading:要求
- 构建测试（标题）：单元测试规范 ← heading:测试
- 环境（标题）：开发工具与脚本规范 ← heading:工具
- 概览（标题）：项目定位统一要求 ← heading:项目定位
- 架构（标题）：架构设计规范 ← heading:架构
- 风格（标题）：顶级规范要求 ← heading:规范
- 流程（标题）：开发流程规范 ← heading:流程

### 10. `linaproai/linapro-site`（禁令,构建测试,环境,概览,架构,风格,流程）

- 禁令（标题）：顶级规范要求 ← heading:要求
- 构建测试（标题）：单元测试规范 ← heading:测试
- 环境（标题）：开发工具与脚本规范 ← heading:工具
- 概览（标题）：项目定位统一要求 ← heading:项目定位
- 架构（标题）：官网站点结构要求 ← heading:结构
- 风格（标题）：顶级规范要求 ← heading:规范
- 流程（标题）：开发流程规范 ← heading:流程


## 汇总

- 标了标签但取不到证据（抽取器缺陷）：0
- 只有全文强模式兜底（证据最弱）：1
- 靠非独立词子串碰上的标签：10
- 无任何标签的文件：1

### 子串误命中清单（建议优先人眼复核）

- Kiln-AI/Kiln :: 构建测试 ← bogus_head:ci@Never Make Legal Decisions as an
- OpenBMB/UltraRAG :: 构建测试 ← bogus_head:ci@9) Retriever/Generation/Prompt S
- langchain-ai/deepagents :: 构建测试 ← bogus_head:ci@Core development principles
- mercurialsolo/claudectl :: 构建测试 ← bogus_head:ci@Key Design Decisions
- netdata/netdata :: 构建测试 ← bogus_head:ci@Development Principles
- yashdev9274/supercli :: 构建测试 ← bogus_head:script@Types & TypeScript
- Weaverse/.agents :: 构建测试 ← bogus_head:script@Description of what it does
- Weaverse/.agents :: 流程 ← bogus_head:commit@Show uncommitted changes
- peteromallet/Arnold :: 构建测试 ← bogus_head:ci@Settled Decisions Section Format
- doocs/md :: 构建测试 ← bogus_head:build@allowBuilds
