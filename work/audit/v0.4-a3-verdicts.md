# A 主样本·第三批（3 份）盲判裁决

- 日期：2026-09-12 ｜ 规则版本：`ruleset_v0.1.7`（本批**只判不改**，改动攒到 v0.1.8 一次落地）
- 盲判落盘：`work/audit/v0.4-blind-calls-a3.json`

## 一、逐份对照

| 仓库 | 字节 | 规则输出 | 我的盲判 | 裁决 |
|---|---|---|---|---|
| `opendlt/accumulate_studio` | 4634 | structure, build_test, workflow, environment, gotchas | +overview, +boundaries | **漏标 ×2**：①首段"Monorepo for Accumulate's agent-facing tooling: …"是干净的项目介绍，overview 0 命中（词表只认标题里的 monorepo）；②`## Permitted commands` 的"需人工批准"清单（npm publish / mainnet / force-push）是 boundaries，被 `heading:command` 判成 build_test |
| `tirth8205/code-review-graph` | 5254 | build_test, workflow, environment, boundaries, agent_meta | workflow, boundaries | **错标 ×2（文件级）**：environment 全靠 `heading:tool` 命中三个"MCP 工具"章节（是**给 agent 用的工具**，不是项目工具链）；agent_meta 靠 `\byou are\b` 命中引号里的 `"ready to push when you are"`。另 build_test 来自 `heading:command`（讲的是"必须用非交互参数"，是禁令不是构建）。**漏标 ×1**：gotchas（"会挂住等 y/n"） |
| `shibanet0/datamitsu-config` | 6078 | build_test, style, environment | +boundaries, +workflow | **漏标 ×2**：①三条英文禁令全丢（`must never import node:*`、`Do not edit auto-generated files manually`、`Do not add new keys`）；②Lefthook pre-commit / validator priority 是 workflow，未命中。**错标**（段级）：`heading:tool` 把 `One Config Per Tool`、`JS/TS config tools` 两条**约定**判成 environment（文件级 environment 由 Runtime Environment 撑着，未掉标签） |

**小结**：3 份里规则 14 个文件级标签、人工 14 个，但**对不上的有 8 处**（错标 2 文件级 + 4 段级、漏标 5）。

## 二、新增通道问题（附全库量化）

| # | 通道 | 实测 | 判断 |
|---|---|---|---|
| 9 | `heading:tool` → environment | **独撑 33 份**。抽样看：`Tooling`/`Toolchain`/`Language and toolchain` 是对的；`Agent Tools`/`Subagents, …, Tool Discipline`/`New Agent Tool`/`Streaming tool previews` 是 agent_meta / workflow / gotchas | **混合通道，precision 约 6-7 成**，不宜整词撤；适合逐词否决 |
| 10 | 英文禁令漏标 | **本批第 3 例**（上批 `DO NOT READ OR EXECUTE`、这批 `must never import` / `Do not edit … manually` / `do NOT use TodoWrite`）。boundaries 正文通道只有 `never commit`/`do not commit`/`must not` + 中文模式 | 确证：需要补英文禁令一般式（注意 body 通道是 `re.I`） |
| 11 | `\byou are\b` → agent_meta | **5 份**都是同一段 beads 模板里的引号短语 `NEVER say "ready to push when you are"` | 纯假阳性；模式需排除引号内出现 |
| 12 | `heading:command` → build_test | 独撑 14 份，抽样约 8 成是真命令清单；`Permitted commands`（禁令）、`Lifecycle Mapping (Implicit Commands)`（agent 生命周期）是误判 | 多数可用，个案否决 |
| 13 | overview 漏"Monorepo for X"/"X is a Y" 型首句 | 本批 1 例（accumulate_studio），上批 1 例（gestion） | 英文 overview 正文通道缺失的另一面 |

## 三、进度

- 我判的 A 主样本：上批累计 26 + 本批 3 = **29**
- 剩余：A 主样本 **26** + B 中文 16 + D 稀有 9 = **51**
- 规则改动**全部攒着**（发现 1–13 已列表见 `v0.4-a1-verdicts.md` / `a2` / 本文件），v0.1.8 一次落地后用 100 份回归集重跑计数
