# v0.4 人工核对工作表（待人工判定）

> 抽样（seed `20260912`，可复现，清单见 `work/audit/v0.4-sample.json`）：**主样本 55 = 无分层随机（只有这组能代表全体）**；中文 **普查** 26 份；边界件 10 份（转引用 0 + 空壳 10）；稀有类加成 9 份。后三组**只报分组结果，不并入整体估计**。

> 判定口径：**准确** = 该类确实存在；**漏标** = 文件里明明有这类内容但没打上；**错标** = 打上了但文件里没有这类内容。

> 每组判定完，把汇总填进文末的统计表——那份汇总才是我要写进 `LIMITATIONS.md` 的东西。

> ⚠ **判「错标」请回到原文**，不要只信「命中证据」列：那一列是**抽取器实际用的证据**，它可能是错的
> （实测：`ci` 命中了 `De**ci**sions` / `Prin**ci**ples`，`script` 命中了 `Type**Script**`）。
> 「命中证据」错的场合，类别本身仍然可能是对的——两件事要分开判。每条的完整原文路径见该条末尾。


---


## 主样本（随机 55，用于整体估计）

### 1. `AtomFlow-AI/MoleCode` — AGENTS.md
- 语言 en ｜ 3236 字节 ｜ 8 章节 ｜ 301 ★ ｜ 许可 MIT
- **规则分配**：概览、构建测试、风格、环境、禁令
- 命中证据：
  - 概览：CLI quick start ← heading:quick start
  - 构建测试：from the repo root (the script finds the molecode package automatically) ← heading:script
  - 风格：Conventions ← heading:convention
  - 环境：Setup ← heading:setup
  - 禁令：MoleCode-first rule ← heading:rule
- 文件标题（8）：AGENTS.md — MoleCode / What this repo is / Use the MoleCode skill / MoleCode-first rule / CLI quick start / from the repo root (the script finds the molecode package automatically) / Setup / Conventions
- 原文摘录（前 600 字）：

```text
# AGENTS.md — MoleCode
Guidance for coding agents (Claude Code, Codex, and compatible tools) working in
this repository.
## What this repo is
**MoleCode** is an LLM-native molecular representation: it serializes a molecule
as an explicit [Mermaid](https://mermaid.js.org/) graph where every atom and bond
is a typed, named node/edge, losslessly inter-convertible with SMILES / MOL via
RDKit. It covers three domains with one grammar — small molecules, polymers
(PSMILES repeat units), and Markush structures (`{}` R-groups).
The installable library lives in [`molecode/`](molecode/); user-facing docs
```
- 完整原文：`data/raw/full/AtomFlow-AI__MoleCode.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 2. `ComposioHQ/composio` — AGENTS.md
- 语言 en ｜ 4761 字节 ｜ 9 章节 ｜ 30118 ★ ｜ 许可 MIT
- **规则分配**：架构、构建测试、流程、禁令
- 命中证据：
  - 架构：Repository Map ← heading:repository map
  - 构建测试：Common Commands ← heading:command, body:```(?:bash|sh|shell)?\n[^`]{
  - 流程：Release And Package Notes ← heading:release
  - 禁令：First Steps ← body:\bmust not\b
- 文件标题（9）：AGENTS.md / Scope / First Steps / Skill Routing / Repository Map / Generated And Vendored Paths / Common Commands / Release And Package Notes / Local Guidance Files
- 原文摘录（前 600 字）：

```text
# AGENTS.md
Repository guidance for Codex, Claude Code, Cursor, pi.dev, and other AI coding agents.
## Scope
This is the Composio SDK v3 monorepo. Most product code lives in `ts/` and `python/`; docs live in `docs/`. Branch new work from `next` and target PRs at `next` unless the user says otherwise.
## First Steps
1. Read the nearest nested `AGENTS.md` before editing a subtree.
2. Preserve unrelated dirty work. Do not revert, delete, or reformat files outside the requested scope.
3. Treat `.agents/skills` as the canonical local skill tree. `.claude/skills` is a compatibility symlink and must 
```
- 完整原文：`data/raw/full/ComposioHQ__composio.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 3. `DietrichGebert/ponytail` — AGENTS.md
- 语言 en ｜ 2593 字节 ｜ 1 章节 ｜ 134327 ★ ｜ 许可 MIT
- **规则分配**：AI行为
- 命中证据：
  - AI行为：Ponytail, lazy senior dev mode ← body:\byou are\b
- 文件标题（1）：Ponytail, lazy senior dev mode
- 原文摘录（前 600 字）：

```text
# Ponytail, lazy senior dev mode
You are a lazy senior developer. Lazy means efficient, not careless. The best code is the code never written.
Before writing any code, stop at the first rung that holds:
1. Does this need to be built at all? (YAGNI)
2. Does it already exist in this codebase? Reuse the helper, util, or pattern that's already here, don't re-write it.
3. Does the standard library already do this? Use it.
4. Does a native platform feature cover it? Use it.
5. Does an already-installed dependency solve it? Use it.
6. Can this be one line? Make it one line.
7. Only then: write the mi
```
- 完整原文：`data/raw/full/DietrichGebert__ponytail.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 4. `Gentleman-Programming/gentle-ai` — AGENTS.md
- 语言 en ｜ 2678 字节 ｜ 3 章节 ｜ 6589 ★ ｜ 许可 MIT
- **规则分配**：流程、AI行为
- 命中证据：
  - 流程：[全文规则命中]（无标题/正文信号时的兜底通道）
  - AI行为：Gentle AI™ — Agent Skills Index ← heading:agent skill
- 文件标题（3）：Gentle AI™ — Agent Skills Index / How to Use / Skills
- 原文摘录（前 600 字）：

```text
# Gentle AI™ — Agent Skills Index
When working on this project, load the relevant skill(s) BEFORE writing any code.
Naming convention: `gentle-ai-*` skills are repo-specific workflow skills. Unprefixed skills are portable writing or work-unit skills and intentionally keep their canonical names.
## How to Use
1. Check the trigger column to find skills that match your current task
2. Load the skill by reading the SKILL.md file at the listed path
3. Follow ALL patterns and rules from the loaded skill
4. Multiple skills can apply simultaneously
## Skills
| Skill | Trigger | Path |
|-------|-------
```
- 完整原文：`data/raw/full/Gentleman-Programming__gentle-ai.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 5. `Gitlawb/openclaude` — AGENTS.md
- 语言 en ｜ 5874 字节 ｜ 8 章节 ｜ 33096 ★ ｜ 许可 NOASSERTION
- **规则分配**：架构、构建测试、风格、禁令
- 命中证据：
  - 架构：Repository Map ← heading:repository map
  - 构建测试：Validation ← heading:validation
  - 风格：Work Style ← heading:style ；Stack And Conventions ← heading:convention
  - 禁令：[全文规则命中]（无标题/正文信号时的兜底通道）
- 文件标题（8）：AGENTS.md - AI Agent Coding Guide / Project Snapshot / Work Style / Stack And Conventions / Repository Map / Validation / Provider Changes / Things To Avoid
- 原文摘录（前 600 字）：

```text
# AGENTS.md - AI Agent Coding Guide
This guide is for AI coding agents working in the OpenClaude repository. Read it before changing code, and also follow [CONTRIBUTING.md](CONTRIBUTING.md) for contributor policy, PR expectations, review follow-up, and project scope.
## Project Snapshot
OpenClaude is a coding-agent CLI for cloud and local model providers. It supports OpenAI-compatible APIs, Anthropic, Gemini, DeepSeek, Ollama, MCP, local backends, slash commands, tools, agents, and a React/Ink terminal UI.
The installed CLI runs on Node.js `>=22.0.0`. Bun is used for source builds, scripts, de
```
- 完整原文：`data/raw/full/Gitlawb__openclaude.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 6. `GlitterKill/sdl-mcp` — AGENTS.md
- 语言 en ｜ 14010 字节 ｜ 22 章节 ｜ 477 ★ ｜ 许可 NOASSERTION
- **规则分配**：架构、构建测试、风格、环境、禁令
- 命中证据：
  - 架构：STRUCTURE ← heading:structure ；WHERE TO LOOK ← heading:where to look
  - 构建测试：COMMANDS ← heading:command, body:```(?:bash|sh|shell)?\n[^`]{
  - 风格：CONVENTIONS ← heading:convention ；Naming ← heading:naming
  - 环境：ENVIRONMENT ← heading:environment ；MCP TOOLS (20+) ← heading:tool
  - 禁令：Prompt cache hygiene contracts ← heading:hygiene, body:\bmust not\b ；RULES ← heading:rule
- 文件标题（22）：AGENTS.md - SDL-MCP Knowledge Base / STRUCTURE / WHERE TO LOOK / ARCHITECTURE / CONVENTIONS / ESM - All imports MUST use `.js` extension (ESLint enforced as error) / Import order: Node builtins, external packages, internal absolute, relative / Naming / DB access / Error handling / Path handling (Windows-first dev) / Prompt cache hygiene contracts / ANTI-PATTERNS (THIS PROJECT) / COMMANDS
- 原文摘录（前 600 字）：

```text
# AGENTS.md - SDL-MCP Knowledge Base
Use SDL-MCP as the default path for repository `sdl-mcp`.
## STRUCTURE
```
.
src/
  main.ts                 # MCP server entry (stdio transport)
  server.ts               # MCPServer class - tool dispatch + Zod validation
  domain/                 # Pure types + SymbolRepository port (hexagonal core)
  indexer/                # File scan, parse, symbol extract, DB write [AGENTS.md]
  db/                     # LadybugDB persistence (12 domain modules) [AGENTS.md]
  graph/                  # Slice building, beam search, clustering [AGENTS.md]
  mcp/          
```
- 完整原文：`data/raw/full/GlitterKill__sdl-mcp.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 7. `Hilbert-beinghappy/seektty` — AGENTS.md
- 语言 en ｜ 809 字节 ｜ 1 章节 ｜ 201 ★ ｜ 许可 MIT
- **规则分配**：禁令
- 命中证据：
  - 禁令：Repository instructions ← body:\bnever commit\b
- 文件标题（1）：Repository instructions
- 原文摘录（前 600 字）：

```text
# Repository instructions
This repository ships one out-of-tree DeepSeek Harness Bundle. Harness remains the only owner of Agent, Session, model, settings, permissions, Profile, plugin, and persistence state.
- The consumer-facing package must not contain `workspace:` dependencies.
- Test install, boot, remove, and reinstall against an unmodified official dsh release under an isolated `DSH_HOME`.
- Keep the GitHub repository private until the user explicitly authorizes publication.
- Never commit credentials, Session data, `.env`, AppleDouble files, local Profile directories, or generated pack
```
- 完整原文：`data/raw/full/Hilbert-beinghappy__seektty.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 8. `Hmbown/Codewhale` — AGENTS.md
- 语言 en ｜ 11848 字节 ｜ 8 章节 ｜ 40935 ★ ｜ 许可 MIT
- **规则分配**：构建测试、禁令
- 命中证据：
  - 构建测试：Claiming a test passed ← heading:test ；Code, migrations, and evidence ← body:```(?:bash|sh|shell)?\n[^`]{
  - 禁令：Working rules ← heading:rule
- 文件标题（8）：Codewhale agent guidance / The ponytail method / Working rules / Landing other people's work / Merging under a gate / Claiming a test passed / Current contracts / Code, migrations, and evidence
- 原文摘录（前 600 字）：

```text
# Codewhale agent guidance
Keep this file durable. Derive changing release, provider, branch, and flake
state from the repository, tests, CI, and current issue tracker rather than from
instructions or memory. The nearest scoped `AGENTS.md` adds path-specific rules.
## The ponytail method
From [dietrichgebert/ponytail](https://github.com/dietrichgebert/ponytail) —
"the laziest senior dev in the room." *He says nothing. He writes one line. It
works.* The best code is the code you never wrote.
Before writing code, walk the decision ladder in order and stop at the first
rung that answers:
1. **Doe
```
- 完整原文：`data/raw/full/Hmbown__Codewhale.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 9. `ModelEngine-Group/nexent` — AGENTS.md
- 语言 en ｜ 5854 字节 ｜ 21 章节 ｜ 5861 ★ ｜ 许可 MIT
- **规则分配**：概览、架构、构建测试、风格、流程、环境、禁令
- 命中证据：
  - 概览：Project Overview ← heading:overview
  - 架构：Architecture ← heading:architecture ；Backend Layer Structure ← heading:structure
  - 构建测试：Developer Commands ← heading:command ；Run Tests ← heading:test
  - 风格：Testing Conventions ← heading:convention ；Code Style ← heading:style
  - 流程：Docker Deployment ← heading:deploy
  - 环境：Setup ← heading:setup ；Install SDK for development ← heading:install
  - 禁令：[全文规则命中]（无标题/正文信号时的兜底通道）
- 文件标题（21）：AGENTS / Available Skills / Project Overview / Developer Commands / Backend (Python 3.11) / Setup / Install SDK for development / Run Tests / From project root, with backend venv activated / Single test file / Frontend (Next.js) / Docker Deployment / Docker Compatibility / Architecture
- 原文摘录（前 600 字）：

```text
# AGENTS
<!-- Skills section removed -->
<skills_system priority="1">
## Available Skills
<!-- SKILLS_TABLE_START -->
<usage>
When users ask to perform tasks, check if any of the available skills below can help complete the task more effectively. Skills provide specialized capabilities and domain knowledge.
How to use skills:
- Invoke: `npx openskills read <skill-name>` (run in your shell)
  - For multiple: `npx openskills read skill-one,skill-two`
- The skill content will load with detailed instructions on how to complete the task
- Base directory provided in output for resolving bundled reso
```
- 完整原文：`data/raw/full/ModelEngine-Group__nexent.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 10. `OleksandrChekhovskyi/hax` — AGENTS.md
- 语言 en ｜ 10269 字节 ｜ 8 章节 ｜ 736 ★ ｜ 许可 MIT
- **规则分配**：架构、构建测试、风格、流程、禁令
- 命中证据：
  - 架构：Architecture ← heading:architecture
  - 构建测试：Build, test, lint ← heading:build, body:```(?:bash|sh|shell)?\n[^`]{
  - 风格：Code style and conventions ← heading:style ；Git conventions ← heading:convention
  - 流程：Changelog ← heading:changelog
  - 禁令：Architecture ← body:\bmust not\b
- 文件标题（8）：AGENTS.md / Build, test, lint / Driving the interactive UI / Architecture / Code style and conventions / Changelog / Git conventions / Dependencies
- 原文摘录（前 600 字）：

```text
# AGENTS.md
Guidance for AI agents working in this repository. Keep this file high-level: commands,
architecture seams, and durable conventions belong here; module-level details belong in code or
headers.
## Build, test, lint
```sh
make                                      # build (quiet; sets up build/ on first run)
make tests                                # build + all tests (unit + e2e)
make lint                                 # clang-format + style script + clang-tidy
scripts/check.sh test <name>...           # build + selected tests (one or more names)
```
`Makefile` delegates to `scrip
```
- 完整原文：`data/raw/full/OleksandrChekhovskyi__hax.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 11. `OthmanAdi/planning-with-files` — AGENTS.md
- 语言 en ｜ 4410 字节 ｜ 5 章节 ｜ 26769 ★ ｜ 许可 MIT
- **规则分配**：架构、构建测试、风格、流程、禁令、AI行为
- 命中证据：
  - 架构：Repo contracts (orchestrator architecture, locked 2026-04-22) ← heading:architecture
  - 构建测试：[强模式命中]（全文正则，跨语言通道）
  - 风格：Formats ← heading:format
  - 流程：Release flow ← heading:release
  - 禁令：AGENTS.md, planning-with-files maintainer rules ← heading:rule
  - AI行为：Release flow ← body:\byou are\b
- 文件标题（5）：AGENTS.md, planning-with-files maintainer rules / Authorship / Release flow / Formats / Repo contracts (orchestrator architecture, locked 2026-04-22)
- 原文摘录（前 600 字）：

```text
# AGENTS.md, planning-with-files maintainer rules
For any agent doing releases, merges or issue work in this repository. The code and `scripts/` are discoverable; this file holds the decisions that are not.
## Authorship
- Contributor commits keep the contributor as `Author:`. Merge with `git fetch origin pull/N/head:pr-N && git cherry-pick <sha>` or `gh pr merge --rebase`. Never `git merge --squash` a contributor PR: it reassigns their commit to whoever runs the local commit, which happened once in the v2.40.1 cycle. Squash only your own WIP before pushing.
- Release and maintenance commits a
```
- 完整原文：`data/raw/full/OthmanAdi__planning-with-files.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 12. `SawyerHood/dev-browser` — AGENTS.md
- 语言 en ｜ 880 字节 ｜ 3 章节 ｜ 6605 ★ ｜ 许可 MIT
- **规则分配**：构建测试、环境
- 命中证据：
  - 构建测试：Validation ← heading:validation, body:```(?:bash|sh|shell)?\n[^`]{
  - 环境：Tooling ← heading:tool
- 文件标题（3）：Repository guide / Tooling / Validation
- 原文摘录（前 600 字）：

```text
# Repository guide
This repository ships `dev-browser`, a Bun-compiled browser automation CLI and warm daemon built on Puppeteer.
## Tooling
- Use Bun for dependency installation, builds, tests, and TypeScript tooling. Do not use pnpm.
- The npm package contains a small Node-compatible shim and download scripts; test those under Node as well as Bun.
- `docs/help.md` is embedded into the binary and is the source of truth for `dev-browser --help`.
## Validation
Run before finishing runtime changes:
```bash
bun install --frozen-lockfile
bun x tsc --noEmit
bun run build
bun run test
```
For packag
```
- 完整原文：`data/raw/full/SawyerHood__dev-browser.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 13. `addyosmani/agent-skills` — AGENTS.md
- 语言 en ｜ 5386 字节 ｜ 10 章节 ｜ 93337 ★ ｜ 许可 MIT
- **规则分配**：概览、构建测试、禁令、AI行为
- 命中证据：
  - 概览：Repository Overview ← heading:overview
  - 构建测试：Lifecycle Mapping (Implicit Commands) ← heading:command ；Orchestration: Personas, Skills, and Commands ← heading:command
  - 禁令：Core Rules ← heading:rule
  - AI行为：Orchestration: Personas, Skills, and Commands ← heading:persona
- 文件标题（10）：AGENTS.md / Repository Overview / OpenCode Integration / Core Rules / Intent → Skill Mapping / Lifecycle Mapping (Implicit Commands) / Execution Model / Anti-Rationalization / Orchestration: Personas, Skills, and Commands / Creating a New Skill
- 原文摘录（前 600 字）：

```text
# AGENTS.md
This file provides guidance to AI coding agents (Claude Code, Cursor, Copilot, Antigravity, etc.) when working with code in this repository.
> **Scope:** This file configures agents working on the [`addyosmani/agent-skills`](https://github.com/addyosmani/agent-skills) repository itself. It is not meant to be copied into other projects or into a global agent configuration; the reusable assets are the skills in `skills/`, not this file.
## Repository Overview
A collection of skills for Claude.ai and Claude Code for senior software engineers. Skills are packaged instructions and scrip
```
- 完整原文：`data/raw/full/addyosmani__agent-skills.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 14. `alibaba/nacos` — AGENTS.md
- 语言 mixed ｜ 24727 字节 ｜ 26 章节 ｜ 33360 ★ ｜ 许可 Apache-2.0
- **规则分配**：概览、架构、构建测试、风格、流程、禁令
- 命中证据：
  - 概览：Repository Overview ← heading:overview
  - 架构：Core Architecture ← heading:architecture ；Module Names ← heading:module
  - 构建测试：Build & Test Commands ← heading:build ；Full build (skip tests) ← heading:build
  - 风格：Format code (run before commit) ← heading:format ；Mandatory Formatting Before Commit ← heading:format
  - 流程：Format code (run before commit) ← heading:commit ；Mandatory Formatting Before Commit ← heading:commit
  - 禁令：Key Rules for AI Agents ← heading:rule
- 文件标题（26）：AGENTS.md / AI Contribution Guidelines / Repository Overview / Core Architecture / Build & Test Commands / Full build (skip tests) / Run all unit tests / Run standalone-server integration tests / Format code (run before commit) / Pre-submission checks (MUST pass before PR) / Mandatory Formatting Before Commit / Code Style / Key Rules for AI Agents / License Header
- 原文摘录（前 600 字）：

```text
# AGENTS.md
This file provides guidance to AI coding agents (Claude Code, Cursor, GitHub Copilot, etc.) when working with the Nacos repository. For human contributors, see [CONTRIBUTING.md](./CONTRIBUTING.md).
## AI Contribution Guidelines
- **Do NOT post AI-generated comments** on issues or PRs. Discussions are for humans only.
- **Discuss before implementing**: Ensure the implementation direction is agreed upon with maintainers in the issue comments before starting work.
- **Spec-first coding is mandatory**: Before changing behavior, APIs, SDKs,
  plugins, storage, runtime flow, or domain se
```
- 完整原文：`data/raw/full/alibaba__nacos.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 15. `apache/camel` — AGENTS.md
- 语言 en ｜ 33986 字节 ｜ 39 章节 ｜ 6315 ★ ｜ 许可 Apache-2.0
- **规则分配**：概览、架构、构建测试、风格、流程、环境、禁令
- 命中证据：
  - 概览：What is in scope (concise summary) ← heading:what is ；What is out of scope ← heading:what is
  - 架构：JIRA Ticket Ownership ← heading:ownership ；Structure ← heading:structure
  - 构建测试：Asynchronous Testing: Use Awaitility Instead of Thread.sleep ← heading:test ；Build ← heading:build, body:```(?:bash|sh|shell)?\n[^`]{
  - 风格：Code Quality ← heading:code quality ；Documentation Conventions ← heading:convention
  - 流程：PR Volume ← heading:pr  ；Git branch ← heading:branch
  - 环境：Merge Requirements ← heading:requirement
  - 禁令：AI Agent Rules of Engagement ← heading:rule ；PR Volume ← body:\bmust not\b
- 文件标题（39）：Apache Camel - AI Agent Guidelines / Project Info / AI Agent Rules of Engagement / Attribution / PR Volume / Git branch / JIRA Ticket Ownership / PR Description Maintenance / PR Reviewers / Backport PRs / Doing a review / Merge Requirements / Merge Procedure / Code Quality
- 原文摘录（前 600 字）：

```text
# Apache Camel - AI Agent Guidelines
Guidelines for AI agents working on this codebase.
## Project Info
Apache Camel is an integration framework supporting routing rules in Java, XML and YAML DSLs.
- Version: 4.23.0-SNAPSHOT
- Java: 17+
- Build: Maven 3.9.12+
## AI Agent Rules of Engagement
These rules apply to ALL AI agents working on this codebase.
### Attribution
- All AI-generated content (GitHub PR descriptions, review comments, JIRA comments) MUST clearly
  identify itself as AI-generated and mention the human operator.
  Example: "_Claude Code on behalf of [Human Name]_"
- **Never guess
```
- 完整原文：`data/raw/full/apache__camel.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 16. `apache/doris` — AGENTS.md
- 语言 en ｜ 11718 字节 ｜ 12 章节 ｜ 15882 ★ ｜ 许可 Apache-2.0
- **规则分配**：架构、构建测试、风格、流程、禁令、AI行为
- 命中证据：
  - 架构：When running in a WORKTREE directory ← heading:directory
  - 构建测试：When running in a WORKTREE directory ← heading:run ；Build and Run Standards ← heading:build
  - 风格：Coding Standards ← heading:standard ；Code Style Enforcement ← heading:style
  - 流程：Code Review ← heading:review ；Commit Standards ← heading:commit
  - 禁令：Testing Standards ← body:\bmust not\b ；Commit Standards ← body:\bmust not\b
  - AI行为：Check List (For Author) ← body:\bbe concise\b
- 文件标题（12）：AGENTS.md — Apache Doris / Security Threat Model / When running in a WORKTREE directory / Coding Standards / Code Style Enforcement / Code Review / Build and Run Standards / Testing Standards / Commit Standards / What problem does this PR solve? / Release note / Check List (For Author)
- 原文摘录（前 600 字）：

```text
# AGENTS.md — Apache Doris
This is the codebase for Apache Doris, an MPP OLAP database. It primarily consists of the Backend module BE (`be/`, execution and storage engine), the Frontend module FE (`fe/`, optimizer and transaction core), and the Cloud module (`cloud/`, storage-compute separation). Your basic development workflow is: modify code, build using standard procedures, add and run tests, and submit relevant changes.
## Security Threat Model
For security scans, vulnerability triage, security reviews, and changes involving authentication, authorization, network boundaries, external cata
```
- 完整原文：`data/raw/full/apache__doris.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 17. `backnotprop/plannotator` — AGENTS.md
- 语言 en ｜ 154006 字节 ｜ 44 章节 ｜ 8590 ★ ｜ 许可 Apache-2.0
- **规则分配**：架构、构建测试、流程、环境、禁令、AI行为
- 命中证据：
  - 架构：Project Structure ← heading:structure
  - 构建测试：Server Runtimes ← heading:run ；Run any app ← heading:run
  - 流程：Plan Review Flow ← heading:review ；Code Review Flow ← heading:review
  - 环境：Installation ← heading:install ；Environment Variables ← heading:environment
  - 禁令：Portable Guided Reviews ← body:\bmust not\b ；Testing Rules ← heading:rule
  - AI行为：WebMCP (browser-agent tools) ← heading:agent tool
- 文件标题（44）：Plannotator / Project Structure / Server Runtimes / Installation / Environment Variables / Plan Review Flow / Code Review Flow / Review header decision control (agent mode) / Since-main default review view / GitButler review invariants / Code-review Ask AI context / Ask AI Provider Defaults / Annotate Flow / Annotate header decision control
- 原文摘录（前 600 字）：

```text
# Plannotator
A plan review UI for Claude Code that intercepts `ExitPlanMode` via hooks, letting users approve or request changes with annotated feedback. Also provides code review for git diffs and annotation of arbitrary markdown files.
> **Reusing the document UI (theme / markdown / editor / settings / comments / layout) in the commercial Workspaces app? Read `packages/ui/README.md` FIRST.** It explains the published `@plannotator/ui` + `@plannotator/core` packages and the host-override seams a host plugs its own backend into via `configurePlannotatorUI()`. A prior from-scratch reimplementa
```
- 完整原文：`data/raw/full/backnotprop__plannotator.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 18. `docling-project/docling` — AGENTS.md
- 语言 en ｜ 3642 字节 ｜ 8 章节 ｜ 66242 ★ ｜ 许可 MIT
- **规则分配**：概览、架构、构建测试、风格
- 命中证据：
  - 概览：Project overview ← heading:overview
  - 架构：Project structure ← heading:structure
  - 构建测试：Key commands ← heading:command, body:```(?:bash|sh|shell)?\n[^`]{
  - 风格：Code standards ← heading:standard
- 文件标题（8）：Docling / Project overview / Project structure / Skills / Key commands / Code standards / When making changes / Before finishing
- 原文摘录（前 600 字）：

```text
# Docling
This file provides guidance to AI coding agents when working with code in this
repository.
## Project overview
Docling is a Python SDK and CLI for converting PDFs, Office files, HTML,
Markdown, audio, images, XML, and other formats into a unified
`DoclingDocument` representation for downstream AI workflows.
## Project structure
```text
docling/                 # main Python package
docling/.agents/skills/  # usage skills shipped inside the package (see below)
packages/docling/        # full docling meta-package
packages/docling-slim/   # slim package readme
tests/                   #
```
- 完整原文：`data/raw/full/docling-project__docling.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 19. `dyad-sh/dyad` — AGENTS.md
- 语言 en ｜ 21278 字节 ｜ 15 章节 ｜ 21450 ★ ｜ 许可 NOASSERTION
- **规则分配**：概览、构建测试、流程、环境、禁令
- 命中证据：
  - 概览：Project context ← heading:project context
  - 构建测试：Project setup and lints ← heading:lint, body:```(?:bash|sh|shell)?\n[^`]{ ；Pre-commit checks ← body:```(?:bash|sh|shell)?\n[^`]{
  - 流程：Pre-commit checks ← heading:commit ；Git workflow ← heading:workflow
  - 环境：Project setup and lints ← heading:setup
  - 禁令：Rules index ← heading:rule
- 文件标题（15）：Repository Agent Guide / Rules index / Project setup and lints / Git worktrees / Pre-commit checks / Running TypeScript / Project context / Verifying your changes / General guidance / Testing / Unit testing / Vitest integration testing / E2E testing / Git workflow
- 原文摘录（前 600 字）：

```text
# Repository Agent Guide
Please read `CONTRIBUTING.md` which includes information for human code contributors. Much of the information is applicable to you as well.
## Rules index
> **IMPORTANT: BEFORE writing any code or making changes, you MUST read the relevant rule files from the table below.** Identify which areas your task touches and read those rule files first. Skipping this step leads to avoidable mistakes and rework.
Detailed rules and learnings are in the `rules/` directory. Read the relevant file when working in that area.
| File                                                     
```
- 完整原文：`data/raw/full/dyad-sh__dyad.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 20. `junhoyeo/tokscale` — AGENTS.md
- 语言 en ｜ 15467 字节 ｜ 26 章节 ｜ 5384 ★ ｜ 许可 MIT
- **规则分配**：概览、构建测试、风格、流程、禁令、AI行为
- 命中证据：
  - 概览：Overview ← heading:overview
  - 构建测试：Agent Command Execution ← heading:command ；CI-Only Workflow ← heading:ci
  - 风格：Commit Message Convention ← heading:convention ；Versioning Conventions ← heading:convention
  - 流程：Git Identity & Merge Discipline ← heading:merge ；Commit Message Convention ← heading:commit
  - 禁令：Git Identity & Merge Discipline ← body:\bnever commit\b ；Commit Message & PR Title Rules (CRITICAL) ← heading:rule
  - AI行为：Style Rules ← body:\btone\b
- 文件标题（26）：AI Agent Guidelines / Maintaining AGENTS.md Files / Authoring PR/Issue Content via `gh` CLI / Git Identity & Merge Discipline / Commit Message Convention / Types / Examples / Commit Message & PR Title Rules (CRITICAL) / Migration journal hygiene / Agent Command Execution / Searching the codebase with `rg` / Release & Deployment / Overview / Release Pipeline
- 原文摘录（前 600 字）：

```text
# AI Agent Guidelines
This document provides context for AI agents working on the Tokscale project.
## Maintaining AGENTS.md Files
When updating AGENTS.md files, follow these principles:
- **No hardcoded counts** — Don't write "10 crates" or "5 modules"; these become outdated instantly
- **No exhaustive lists** — Prefer dynamic commands (`ls crates/`) over maintaining complete lists
- **Document constraints, not descriptions** — Focus on non-obvious behaviors, gotchas, and cross-crate dependencies
- **Use nested AGENTS.md** — Place crate-specific details in `crates/{name}/AGENTS.md`, not here

```
- 完整原文：`data/raw/full/junhoyeo__tokscale.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 21. `kungfu-systems/kungfu` — AGENTS.md
- 语言 en ｜ 6969 字节 ｜ 6 章节 ｜ 4514 ★ ｜ 许可 Apache-2.0
- **规则分配**：流程、禁令
- 命中证据：
  - 流程：[全文规则命中]（无标题/正文信号时的兜底通道）
  - 禁令：Rules that always apply ← heading:rule ；Completion and contribution boundary ← heading:boundar
- 文件标题（6）：AGENTS.md / Start from the person's objective / Rules that always apply / Load bounded context before changing the repository / Execute and verify through Shifu / Completion and contribution boundary
- 原文摘录（前 600 字）：

```text
# AGENTS.md
This file orients coding agents (and people) working with this repository. It is
a router, not a duplicate: it points to the authoritative documents rather than
restating them.
## Start from the person's objective
Restate the requested outcome in one sentence, then choose the single matching
route. Do not inventory the repository or preload multiple routes "just in
case."
| Objective | First route |
| --- | --- |
| Use or operate an installed Kungfu | Read the curated [`docs/README.md`](docs/README.md); run `kungfu agent brief`, then `kungfu agent choose-mode --json`. |
| Explain o
```
- 完整原文：`data/raw/full/kungfu-systems__kungfu.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 22. `langchain-ai/deepagents` — AGENTS.md
- 语言 en ｜ 10315 字节 ｜ 22 章节 ｜ 29274 ★ ｜ 许可 MIT
- **规则分配**：架构、构建测试、风格、流程、禁令、坑、AI行为
- 命中证据：
  - 架构：Global development guidelines for the Deep Agents monorepo ← heading:monorepo
  - 构建测试：Testing ← heading:test ；CI and releases ← heading:ci
  - 风格：PR conventions ← heading:convention ；Branch naming ← heading:naming
  - 流程：Development workflow ← heading:workflow ；PR conventions ← heading:pr 
  - 禁令：Suppressing ruff rules ← heading:rule ；Search hygiene ← heading:hygiene
  - 坑：Warnings are errors ← heading:warning
  - AI行为：Development workflow ← body:\byou are\b
- 文件标题（22）：Global development guidelines for the Deep Agents monorepo / Corridor security analysis / Development workflow / Suppressing ruff rules / PR conventions / Titles and scope / Branch naming / PR bodies / Core development principles / Public interfaces / Code and documentation / Testing / Warnings are errors / Security and resources
- 原文摘录（前 600 字）：

```text
# Global development guidelines for the Deep Agents monorepo
This file contains repository-wide rules for agents and contributors. Use these sources for details instead of duplicating them here:
- [`libs/DEVELOPMENT.md`](libs/DEVELOPMENT.md) — repository layout, setup, commands, pre-commit, the edit-test-lint loop, testing, and benchmarks.
- [`libs/ARCHITECTURE.md`](libs/ARCHITECTURE.md) — runtime structure and SDK starting points.
- [`.github/RELEASING.md`](.github/RELEASING.md) — releases, version lines, fan-out, CI labels, and troubleshooting.
- [LangChain contributing guide](https://docs.l
```
- 完整原文：`data/raw/full/langchain-ai__deepagents.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 23. `langchain-ai/langgraph` — AGENTS.md
- 语言 en ｜ 2113 字节 ｜ 4 章节 ｜ 41387 ★ ｜ 许可 MIT
- **规则分配**：构建测试、流程、环境
- 命中证据：
  - 构建测试：Corridor security analysis ← body:```(?:bash|sh|shell)?\n[^`]{
  - 流程：[全文规则命中]（无标题/正文信号时的兜底通道）
  - 环境：Dependency map ← heading:dependency
- 文件标题（4）：AGENTS Instructions / Corridor security analysis / Libraries / Dependency map
- 原文摘录（前 600 字）：

```text
# AGENTS Instructions
This repository is a monorepo. Each library lives in a subdirectory under `libs/`.
<corridor>
## Corridor security analysis
When Corridor's `analyzePlan` tool is available, create a plan and use the tool to analyze it before generating or modifying code. Apply the resulting security guidance before writing code.
</corridor>
When you modify code in any library, run the following commands in that library's directory before creating a pull request:
- `make format` – run code formatters
- `make lint` – run the linter
- `make test` – execute the test suite
To run a particular 
```
- 完整原文：`data/raw/full/langchain-ai__langgraph.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 24. `lobehub/lobehub` — AGENTS.md
- 语言 mixed ｜ 6484 字节 ｜ 16 章节 ｜ 82373 ★ ｜ 许可 NOASSERTION
- **规则分配**：概览、架构、构建测试、风格、流程、环境、AI行为
- 命中证据：
  - 概览：Tech Stack ← heading:tech stack
  - 架构：Code Ownership ← heading:ownership
  - 构建测试：Quality Check ← heading:quality check
  - 风格：Code Style ← heading:style
  - 流程：Git Workflow ← heading:workflow ；Code Review ← heading:review
  - 环境：Starting the Dev Environment ← heading:environment
  - AI行为：Agent Skills ← heading:agent skill
- 文件标题（16）：LobeHub Development Guidelines / Tech Stack / Agent Skills / Code Ownership / Development / Starting the Dev Environment / SPA dev mode (frontend only, proxies API to localhost:3010) / Full-stack dev (Next.js + Vite SPA concurrently) / Standalone Hono backend service / Git Workflow / Package Management / Quality Check / Acceptance / i18n
- 原文摘录（前 600 字）：

```text
# LobeHub Development Guidelines
Guidelines for using AI coding agents in this opensource LobeHub repository.
## Tech Stack
- Next.js 16 + React 19 + TypeScript
- SPA inside Next.js with `react-router-dom`
- `@lobehub/ui`, antd, and antd-style for UI implementation
- react-i18next for i18n; zustand for state management
- SWR for data fetching; TRPC for type-safe backend
- Drizzle ORM with PostgreSQL; Vitest for testing
## Agent Skills
`AGENTS.md` owns repository-wide architecture and workflow. Keep detailed implementation rules in skills so they have one source of truth.
- **React and TSX**: B
```
- 完整原文：`data/raw/full/lobehub__lobehub.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 25. `localstack/localstack` — AGENTS.md
- 语言 en ｜ 7534 字节 ｜ 22 章节 ｜ 65120 ★ ｜ 许可 NOASSERTION
- **规则分配**：架构、构建测试、风格、禁令
- 命中证据：
  - 架构：Project Structure ← heading:structure
  - 构建测试：Commands ← heading:command ；Testing ← heading:test
  - 风格：Code quality ← heading:code quality ；Key Conventions ← heading:convention
  - 禁令：⚠️ Critical Hard Constraints ← heading:constraint ；Fixture Rules ← heading:rule
- 文件标题（22）：AGENTS.md — LocalStack Pro / ⚠️ Critical Hard Constraints / Commands / Testing / Code quality / Project Structure / Moto Fallback / Implementing AWS Operations / Reference Implementation / Key Conventions / Writing Parity Tests / Reference Implementation / Test Markers / Core Patterns
- 原文摘录（前 600 字）：

```text
# AGENTS.md — LocalStack Pro
LocalStack is a fully functional local AWS cloud stack that emulates AWS services (S3, Lambda, DynamoDB, etc.) for development and testing. It runs in Docker, provides AWS-like APIs locally, enabling offline workflows, integration testing, and CI without calling real AWS endpoints.
This codebase contains both the AWS emulator in `localstack-core`.
---
## ⚠️ Critical Hard Constraints
**NEVER:**
- Modify `*.snapshot.json` or `*.validation.json` files manually — generated by running tests against AWS
- Use plain `assert` in validated tests — **ALWAYS** use `snapshot.m
```
- 完整原文：`data/raw/full/localstack__localstack.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 26. `mem0ai/mem0` — AGENTS.md
- 语言 en ｜ 11997 字节 ｜ 13 章节 ｜ 65066 ★ ｜ 许可 Apache-2.0
- **规则分配**：架构、构建测试、风格、流程、环境、禁令、AI行为
- 命中证据：
  - 架构：Where to look ← heading:where to look ；Repository map ← heading:repository map
  - 构建测试：Setup ← body:```(?:bash|sh|shell)?\n[^`]{
  - 风格：Conventions everywhere ← heading:convention
  - 流程：Contributing ← heading:contributing ；Two gates decide whether your pull request stays open ← heading:pull request
  - 环境：Setup ← heading:setup
  - 禁令：Do NOT ← heading:do not
  - AI行为：AGENTS.md ← body:\byou are\b ；Do NOT ← body:\byou are\b
- 文件标题（13）：AGENTS.md / Do NOT / Where to look / Repository map / Setup / Conventions everywhere / Benchmarking / What to ship with a change / Contributing / Two gates decide whether your pull request stays open / The CLA is not optional / What gets a pull request closed / Reference
- 原文摘录（前 600 字）：

```text
# AGENTS.md
Context for AI coding assistants (Claude Code, Cursor, Copilot, Codex) working in the Mem0 repository.
**Mem0** ("mem-zero") is a memory layer for AI agents: persistent, personalized memory through a hosted platform API and self-hosted open-source SDKs. Apache-2.0.
[Repository](https://github.com/mem0ai/mem0) · [Documentation](https://docs.mem0.ai)
This is a polyglot monorepo and **every package sets its own rules**. Read the `AGENTS.md` nearest the files you are editing before running any command. The linters, formatters, test runners, and line lengths genuinely differ per package
```
- 完整原文：`data/raw/full/mem0ai__mem0.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 27. `microsoft/agent-governance-toolkit` — AGENTS.md
- 语言 en ｜ 7530 字节 ｜ 13 章节 ｜ 6233 ★ ｜ 许可 MIT
- **规则分配**：概览、架构、构建测试、流程、环境、禁令
- 命中证据：
  - 概览：Project Overview ← heading:overview
  - 架构：Repository Layout Status ← heading:layout
  - 构建测试：Validation ← heading:validation
  - 流程：Version Bumps ← heading:version ；PR Hygiene ← heading:pr 
  - 环境：Agent Governance Toolkit - Repository Instructions ← heading:tool
  - 禁令：Routing Rules ← heading:rule ；Boundaries ← heading:boundar, body:\bnever commit\b
- 文件标题（13）：Agent Governance Toolkit - Repository Instructions / Project Overview / Repository Layout Status / Where Changes Belong / Routing Rules / OSS Contribution Expectations / Decision Escalation / Boundaries / Spam / SEO Outreach Filtering / Version Bumps / PR Hygiene / Validation / PR Hygiene
- 原文摘录（前 600 字）：

```text
# Agent Governance Toolkit - Repository Instructions
## Project Overview
Agent Governance Toolkit is a multi-package OSS monorepo for runtime governance of AI agents:
policy enforcement, zero-trust identity, execution sandboxing, SRE, compliance, examples,
docs, demos, SDKs, and publishing pipelines.
Use this file for repository-wide routing. When you enter a subdirectory that has its own
`AGENTS.md`, that narrower file takes precedence.
## Repository Layout Status
This routing reflects the repo **as it exists today** while reserving room for approved language
SDK migrations. Language SDKs may
```
- 完整原文：`data/raw/full/microsoft__agent-governance-toolkit.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 28. `modelcontextprotocol/inspector` — AGENTS.md
- 语言 en ｜ 90536 字节 ｜ 23 章节 ｜ 10850 ★ ｜ 许可 nan
- **规则分配**：架构、构建测试、风格、流程、环境、禁令、坑、AI行为
- 命中证据：
  - 架构：Project Structure ← heading:structure ；Web source layout: `src/lib` vs `src/utils` ← heading:layout
  - 构建测试：Development setup ← body:```(?:bash|sh|shell)?\n[^`]{ ；Always test new or modified code ← heading:test
  - 风格：Issue-driven Work Style ← heading:style
  - 流程：Contributing ← heading:contributing ；Responding to Code Reviews ← heading:review
  - 环境：Development setup ← heading:setup ；Dependency placement ← heading:dependency
  - 禁令：Dependency placement ← body:\bmust not\b ；The SDK watch is the third sweep ← body:\bmust not\b
  - 坑：Lint has no warning tier ← heading:warning
  - AI行为：Responding to Code Reviews ← body:\byou are\b ；Waiting on long-running work ← body:\byou are\b
- 文件标题（23）：Inspector V2 / Skills index / Project Structure / Development setup / Dependency placement / Dependency updates are issue-driven, like everything else / The SDK watch is the third sweep / Contributing / Project Status and Direction / Maintenance Rules / Keep documentation files up to date / Maintaining the skills / Issue-driven Work Style / Responding to Code Reviews
- 原文摘录（前 600 字）：

```text
# Inspector V2
This is an application for inspecting MCP servers. It has three incarnations —
Web, TUI, and CLI — over a shared `core/`.
**This file holds the _rules_: the conventions a reviewer cites against a diff.**
It is loaded in full on every turn, so it stays resident and must stay complete
enough to work from on its own.
**The repo's _procedures_ — multi-step recipes with commands and live IDs — live
in [`.claude/skills/`](./.claude/skills).** They are ordinary committed Markdown,
so any agent or human can read one directly; Claude Code loads them on demand and
users invoke them by nam
```
- 完整原文：`data/raw/full/modelcontextprotocol__inspector.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 29. `netdata/netdata` — AGENTS.md
- 语言 en ｜ 53778 字节 ｜ 39 章节 ｜ 80474 ★ ｜ 许可 GPL-3.0
- **规则分配**：架构、构建测试、风格、流程、环境、禁令、AI行为
- 命中证据：
  - 架构：Local-Only Working Directory ← heading:directory
  - 构建测试：Validation Gate ← heading:validation ；Validation Commands ← heading:command
  - 风格：Durable AI-Facing Artifact Formatting ← heading:format ；Go Test Style ← heading:style
  - 流程：Git And PR Workflow ← heading:workflow ；Review ← heading:review
  - 环境：Requirement Language ← heading:requirement
  - 禁令：Requirement Language ← body:\bmust not\b ；Clean End State Over Less Churn ← body:\bmust not\b
  - AI行为：Working With The User ← body:\byou are\b ；Clean End State Over Less Churn ← body:\byou are\b
- 文件标题（39）：AGENTS.md / Goals / Requirement Language / Working With The User / Development Principles / Definitions / Clean End State Over Less Churn / Plan Before Non-Trivial Work / Scope Discipline At Every Step / SOW System / Storage Model / When A SOW Is Required / Required First Checks / SOW Lifecycle
- 原文摘录（前 600 字）：

```text
# AGENTS.md
## Goals
This repository is the Netdata Agent codebase: a large, multi-language, multi-platform monolith serving production
monitoring, troubleshooting, data collection, alerting, storage, streaming, cloud integration, packaging, and
documentation workflows.
Work here MUST prioritize root-cause understanding, correctness, performance, maintainability, portability,
security, and consistency with existing project conventions.
Critical rules:
1. You MUST find the root cause of a problem before offering a solution. Patching without understanding the
   problem is not allowed.
2. Before
```
- 完整原文：`data/raw/full/netdata__netdata.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 30. `omnigent-ai/omnigent` — AGENTS.md
- 语言 en ｜ 4352 字节 ｜ 9 章节 ｜ 9828 ★ ｜ 许可 Apache-2.0
- **规则分配**：构建测试、流程
- 命中证据：
  - 构建测试：Finishing a task ← heading:task
  - 流程：Committing ← heading:commit ；Pull requests ← heading:pull request
- 文件标题（9）：Agent guidance / Committing / Local development shortcuts / Pull requests / Finishing a task / Deprecating features / Code comments / Database query names / Framework-owned instructions
- 原文摘录（前 600 字）：

```text
# Agent guidance
Guidance for AI agents (Claude Code, Copilot, Cursor, etc.) working in this
repository. See `CONTRIBUTING.md` for the full contributor workflow.
## Committing
Run the `pre-commit` hook before committing (`pre-commit run --all-files`, or
let it run on staged files via `git commit`). Fix any issues it reports so the
commit lands clean — CI runs the same checks.
## Local development shortcuts
Use `just` for common tasks; run `just --list` for grouped recipes.
- `just ensure` — install/check prerequisites
- `just run-ios` / `just run-android` — build/run mobile apps
- `just dev` /
```
- 完整原文：`data/raw/full/omnigent-ai__omnigent.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 31. `onyx-dot-app/onyx` — AGENTS.md
- 语言 en ｜ 6254 字节 ｜ 14 章节 ｜ 32012 ★ ｜ 许可 NOASSERTION
- **规则分配**：概览、架构、构建测试、风格、流程、环境、禁令、AI行为
- 命中证据：
  - 概览：Project Overview ← heading:overview ；Technology Stack ← heading:technology stack
  - 架构：Repository Layout & Sub-project Guides ← heading:layout
  - 构建测试：Install and run pre-commit hooks ← heading:run ；Faster: run only on the files you touched ← heading:run, body:```(?:bash|sh|shell)?\n[^`]{
  - 风格：Code Quality ← heading:code quality ；Best Practices ← heading:best practice
  - 流程：Install and run pre-commit hooks ← heading:commit
  - 环境：Install and run pre-commit hooks ← heading:install
  - 禁令：Security Considerations ← body:\bnever commit\b
  - AI行为：Creating a Plan ← body:\byou are\b
- 文件标题（14）：PROJECT KNOWLEDGE BASE / KEY NOTES / Project Overview / Technology Stack / Repository Layout & Sub-project Guides / Code Quality / Install and run pre-commit hooks / Faster: run only on the files you touched / Writing / Testing / Logs / Security Considerations / Creating a Plan / Best Practices
- 原文摘录（前 600 字）：

```text
# PROJECT KNOWLEDGE BASE
This file provides guidance to AI agents when working with code in this repository.
## KEY NOTES
- Python deps live in a `uv`-managed virtualenv at `.venv` (repo root). If it doesn't exist yet, create it
  with `uv sync --frozen`, then `source .venv/bin/activate`.
- Test secrets (API keys etc.) are resolved by `backend/tests/utils/aws_secrets.py`, in order: process
  env vars → the gitignored `.vscode/.env` (also used by the pytest commands in `backend/AGENTS.md`;
  create it by copying `.vscode/env_template.txt`) → AWS Secrets Manager (requires `aws sso login`).
  Tes
```
- 完整原文：`data/raw/full/onyx-dot-app__onyx.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 32. `openai/openai-agents-python` — AGENTS.md
- 语言 en ｜ 34658 字节 ｜ 27 章节 ｜ 29326 ★ ｜ 许可 MIT
- **规则分配**：概览、架构、构建测试、流程、环境、禁令、AI行为
- 命中证据：
  - 概览：Overview ← heading:overview
  - 架构：Project Structure Guide ← heading:structure ；Repo Structure & Important Files ← heading:structure
  - 构建测试：Mandatory Skill Usage ← heading:usage ；Documentation Verification Tiers ← heading:verification
  - 流程：Git Worktree and Branch Safety ← heading:branch ；Documentation Release Timing ← heading:release
  - 环境：Prerequisites ← heading:prerequisite
  - 禁令：Policies & Mandatory Rules ← heading:rule ；Code Review Rules ← heading:rule
  - AI行为：Pull Request & Commit Guidelines ← body:\bbe concise\b
- 文件标题（27）：Contributor Guide / Table of Contents / Policies & Mandatory Rules / Mandatory Skill Usage / Work Status Reporting / Git Worktree and Branch Safety / Documentation Release Timing / Documentation Verification Tiers / Scope Discipline and Complexity Reset / ExecPlans / Public API Compatibility / Platform, Docs, and Security Review / Code Review Rules / Finding threshold and supported scope
- 原文摘录（前 600 字）：

```text
# Contributor Guide
## Table of Contents
1. [Policies & Mandatory Rules](#policies--mandatory-rules)
2. [Code Review Rules](#code-review-rules)
3. [Project Structure Guide](#project-structure-guide)
4. [Operation Guide](#operation-guide)
## Policies & Mandatory Rules
### Mandatory Skill Usage
Repository skills are stored under `.agents/skills/`. References below authorize their use when the stated condition applies; no separate manual invocation is needed unless explicitly required. Read the selected `SKILL.md`, then only the supporting references needed for its route. User instructions and al
```
- 完整原文：`data/raw/full/openai__openai-agents-python.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 33. `patriceckhart/zot` — AGENTS.md
- 语言 en ｜ 9834 字节 ｜ 17 章节 ｜ 336 ★ ｜ 许可 MIT
- **规则分配**：架构、构建测试、流程、环境、禁令、AI行为
- 命中证据：
  - 架构：Code ownership map ← heading:ownership
  - 构建测试：Starting a task ← heading:task ；Validation ladder ← heading:validation, body:```(?:bash|sh|shell)?\n[^`]{
  - 流程：[全文规则命中]（无标题/正文信号时的兜底通道）
  - 环境：Tools and confinement ← heading:tool ；Dependency policy ← heading:dependency
  - 禁令：Code ownership map ← body:\bmust not\b ；Tools and confinement ← body:\bmust not\b
  - AI行为：Terminal behavior ← heading:behavior
- 文件标题（17）：Working Agreement for zot / Product intent / Starting a task / Code ownership map / Correctness contracts / Agent events and sessions / Providers / Credentials and private data / Tools and confinement / Extensions and RPC / Terminal behavior / Implementation approach / Validation ladder / Documentation duties
- 原文摘录（前 600 字）：

```text
# Working Agreement for zot
This file defines how automated coding assistants should work in this repository. Treat it as a practical operating manual, not a substitute for reading the code.
## Product intent
zot is a compact Go coding-agent harness. Changes should preserve its defining properties:
- one portable binary
- a provider-neutral agent engine
- a terminal UI without a framework dependency
- extensions that communicate through subprocess JSON-RPC
- predictable behavior on macOS, Linux, and Windows
- a small dependency and operational footprint
Prefer a narrow, explicit implementation
```
- 完整原文：`data/raw/full/patriceckhart__zot.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 34. `prevalentWare/opencode-goal-plugin` — AGENTS.md
- 语言 en ｜ 3470 字节 ｜ 6 章节 ｜ 353 ★ ｜ 许可 MIT
- **规则分配**：构建测试、AI行为
- 命中证据：
  - 构建测试：Local Validation ← heading:validation ；Publishing Flow ← body:```(?:bash|sh|shell)?\n[^`]{
  - AI行为：Agent Notes ← heading:agent note
- 文件标题（6）：Agent Notes / Project Shape / Change Guidelines / Local Validation / Publishing Flow / End-To-End Plugin Test
- 原文摘录（前 600 字）：

```text
# Agent Notes
## Project Shape
This package is an OpenCode plugin with separate server and TUI entrypoints:
- `src/server.ts` is the server plugin. It registers the `/goal` command, goal tools, chat/session hooks, usage accounting, compaction context, and idle auto-continuation.
- `src/state.ts` owns goal persistence and lifecycle state. It stores JSON at `OPENCODE_GOAL_STATE_PATH` when set, otherwise under the user's OpenCode data location.
- `src/tui.tsx` is the Solid/OpenTUI sidebar and command-palette UI. It is exported as source, so avoid adding heavy runtime dependencies here unless ther
```
- 完整原文：`data/raw/full/prevalentWare__opencode-goal-plugin.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 35. `rohitg00/agentmemory` — AGENTS.md
- 语言 en ｜ 6136 字节 ｜ 11 章节 ｜ 28265 ★ ｜ 许可 Apache-2.0
- **规则分配**：架构、构建测试、风格、环境、禁令
- 命中证据：
  - 架构：Architecture ← heading:architecture
  - 构建测试：Hook Scripts ← heading:script ；Testing ← heading:test
  - 风格：Code Patterns ← heading:pattern ；Coding Standards ← heading:standard
  - 环境：MCP Tool Handler ← heading:tool
  - 禁令：Consistency Rules ← heading:rule
- 文件标题（11）：agentmemory — Agent Instructions / Architecture / Consistency Rules / Code Patterns / Function Registration / REST Endpoint Registration / MCP Tool Handler / Hook Scripts / Coding Standards / Testing / Current Stats (v0.9.29)
- 原文摘录（前 600 字）：

```text
# agentmemory — Agent Instructions
## Architecture
agentmemory is a persistent memory system for AI coding agents, built on iii-engine's three primitives (Worker/Function/Trigger). Everything goes through `registerFunction`/`registerTrigger`/`sdk.trigger()` — never bypass iii-engine with standalone SQLite or in-process alternatives.
- **Engine**: iii-sdk (WebSocket to iii-engine on port 49134)
- **State**: File-based SQLite via iii-engine's StateModule (`./data/state_store.db`)
- **Build**: TypeScript → ESM via tsdown, output to `dist/`
- **Test**: vitest (`npm test` excludes integration tests
```
- 完整原文：`data/raw/full/rohitg00__agentmemory.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 36. `stagewise-io/stagewise` — AGENTS.md
- 语言 en ｜ 6319 字节 ｜ 17 章节 ｜ 6809 ★ ｜ 许可 AGPL-3.0
- **规则分配**：概览、架构、构建测试、风格、流程
- 命中证据：
  - 概览：Architecture Overview ← heading:overview
  - 架构：Architecture Overview ← heading:architecture ；Directory Structure ← heading:structure
  - 构建测试：Essential Commands ← heading:command ；Building ← heading:build
  - 风格：Code Quality ← heading:code quality ；Code Style ← heading:style
  - 流程：Commit Guidelines ← heading:commit ；Pre-commit Hooks ← heading:commit
- 文件标题（17）：AGENTS.md / Essential Commands / Development / Building / Code Quality / Testing / Browser App Specific / Maintenance / Architecture Overview / Directory Structure / Key Technologies / Code Style / Commit Guidelines / Pre-commit Hooks
- 原文摘录（前 600 字）：

```text
# AGENTS.md
This file provides guidance to AI coding agents when working with code in this repository.
## Essential Commands
### Development
- `pnpm install` - Install dependencies (Node >= 22.12.0, pnpm 10.30.3 required)
- `pnpm dev` - Start workspace development tasks in Turbo watch mode
### Building
- `pnpm build` - Run all configured workspace build tasks
- `pnpm build:apps` - Build applications only (`apps/*`)
- `pnpm build:packages` - Build packages only (`packages/*`)
### Code Quality
- `pnpm check` - Run Biome linting/formatting checks (read-only)
- `pnpm check:fix` - Auto-fix linting/
```
- 完整原文：`data/raw/full/stagewise-io__stagewise.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 37. `tastyeffectco/sandboxd` — AGENTS.md
- 语言 en ｜ 7477 字节 ｜ 20 章节 ｜ 936 ★ ｜ 许可 MIT
- **规则分配**：概览、构建测试、环境、坑
- 命中证据：
  - 概览：What this is ← heading:what this is
  - 构建测试：Running Claude Code / OpenCode inside a sandbox ← heading:run ；then drive a task normally: ← heading:task
  - 环境：Prerequisites ← heading:prerequisite ；Install ← heading:install
  - 坑：Troubleshooting ← heading:troubleshoot
- 文件标题（20）：AGENTS.md — operating sandboxd / What this is / Prerequisites / Install / Core API / End-to-end example / 1. create a sandbox exposing port 3000 / 2. start a dev server inside it / 3. open the preview (browsers resolve *.localhost to 127.0.0.1). / add :$HTTP_PORT if you changed it from 80. / 4. stop (idle) and wake-on-request / 5. destroy / Running Claude Code / OpenCode inside a sandbox / API key:
- 原文摘录（前 600 字）：

```text
# AGENTS.md — operating sandboxd
A complete, self-contained runbook for an AI agent (or a human) to install, run,
use, and remove **sandboxd** with no outside knowledge. Commands are
copy-pasteable. Human-readable docs: [`README.md`](README.md),
[`ARCHITECTURE.md`](ARCHITECTURE.md).
## What this is
A single-host service that creates isolated Linux dev containers ("sandboxes"),
each with an HTTP/HTTPS preview URL for a dev server running inside it. It runs
entirely on Docker: a Go control plane (`sandboxd`) + Traefik, both in
containers; each sandbox is a sibling container. Sandboxes stop when 
```
- 完整原文：`data/raw/full/tastyeffectco__sandboxd.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 38. `tech-leads-club/agent-skills` — AGENTS.md
- 语言 en ｜ 6558 字节 ｜ 23 章节 ｜ 5158 ★ ｜ 许可 NOASSERTION
- **规则分配**：架构、构建测试、风格、流程、环境、AI行为
- 命中证据：
  - 架构：Architecture ← heading:architecture ；Skill Structure ← heading:structure
  - 构建测试：3. Verification Before Done ← heading:verification ；Commands ← heading:command
  - 风格：Code Conventions ← heading:convention ；Skill Quality Standards ← heading:standard
  - 流程：Workflow Orchestration ← heading:workflow ；Release ← heading:release
  - 环境：Setup ← heading:setup ；Agent Configuration ← heading:config
  - AI行为：1. Plan Mode Default ← heading:plan mode ；2. Subagent Strategy ← heading:subagent
- 文件标题（23）：AGENTS.md / Workflow Orchestration / 1. Plan Mode Default / 2. Subagent Strategy / 3. Verification Before Done / 4. Demand Elegance (Balanced) / 5. Autonomous Bug Fixing / Core Principles / Repository / Commands / Setup / Development / Build & Test / Quality
- 原文摘录（前 600 字）：

```text
# AGENTS.md
Guidance for AI Coding Agents when working with code in this repository.
## Workflow Orchestration
### 1. Plan Mode Default
- Enter plan mode for ANY non-trivial task (3+ steps or architectural decisions)
- If something goes sideways, STOP and re-plan immediately — don't keep pushing
- Use plan mode for verification steps, not just building
- Write detailed specs upfront to reduce ambiguity
### 2. Subagent Strategy
- Use subagents liberally to keep main context window clean
- Offload research, exploration, and parallel analysis to subagents
- For complex problems, throw more comput
```
- 完整原文：`data/raw/full/tech-leads-club__agent-skills.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 39. `tinyhumansai/openhuman` — AGENTS.md
- 语言 en ｜ 134305 字节 ｜ 48 章节 ｜ 39608 ★ ｜ 许可 GPL-3.0
- **规则分配**：架构、构建测试、风格、流程、环境、禁令、坑
- 命中证据：
  - 架构：Repository layout ← heading:layout ；Module wire contracts — one `*-bus` crate per loadable module ← heading:module
  - 构建测试：Runtime scope ← heading:run ；Commands (from repo root) ← heading:command, body:```(?:bash|sh|shell)?\n[^`]{
  - 风格：Design & patterns ← heading:pattern
  - 流程：Coverage requirement (merge gate) ← heading:merge ；Release cache, boot preload, and the loading state ← heading:release
  - 环境：Coverage requirement (merge gate) ← heading:requirement ；Configuration ← heading:config
  - 禁令：Child webviews — no new JS injection ← body:\bmust not\b ；Compile-time domain gates (Cargo `[features]`) ← body:\bmust not\b
  - 坑：The kernel profile, and the floor ratchet that protects it ← body:\bgotcha\b
- 文件标题（48）：OpenHuman / Repository layout / Runtime scope / iOS client (experimental, non-shipping) / Commands (from repo root) / Rust / macOS Apple Silicon workaround (llama.cpp) / Agent debug runners (`scripts/debug/`) / Coverage requirement (merge gate) / Configuration / Agent access & security / Hooks — two unrelated things with one name / Testing / Unit (Vitest)
- 原文摘录（前 600 字）：

```text
# OpenHuman
**AI assistant for communities — React + Tauri v2 desktop app with a Rust core (JSON-RPC / CLI) embedded in-process.**
Architecture docs: [`gitbooks/developing/architecture.md`](gitbooks/developing/architecture.md) | [Frontend](gitbooks/developing/architecture/frontend.md) | [Tauri shell](gitbooks/developing/architecture/tauri-shell.md) | [Agent harness](gitbooks/developing/architecture/agent-harness.md)
---
## Repository layout
| Path                    | Role                                                                                                                          |
```
- 完整原文：`data/raw/full/tinyhumansai__openhuman.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 40. `tirth8205/code-review-graph` — AGENTS.md
- 语言 en ｜ 5254 字节 ｜ 14 章节 ｜ 31309 ★ ｜ 许可 MIT
- **规则分配**：概览、构建测试、流程、环境、禁令、AI行为
- 命中证据：
  - 概览：Quick Reference ← heading:quick reference ；Quick Reference ← heading:quick reference
  - 构建测试：Non-Interactive Shell Commands ← heading:command
  - 流程：MCP Tools: code-review-graph ← heading:review ；Workflow ← heading:workflow
  - 环境：MCP Tools: code-review-graph ← heading:tool ；When to use graph tools FIRST ← heading:tool
  - 禁令：Rules ← heading:rule
  - AI行为：Session Completion ← body:\byou are\b
- 文件标题（14）：Agent Instructions / Quick Reference / Non-Interactive Shell Commands / Force overwrite without prompting / For recursive operations / Beads Issue Tracker / Quick Reference / Rules / Session Completion / MCP Tools: code-review-graph / When to use graph tools FIRST / Verify in the source / Key Tools / Workflow
- 原文摘录（前 600 字）：

```text
# Agent Instructions
This project uses **bd** (beads) for issue tracking. Run `bd prime` for full workflow context.
## Quick Reference
```bash
bd ready              # Find available work
bd show <id>          # View issue details
bd update <id> --claim  # Claim work atomically
bd close <id>         # Complete work
bd dolt push          # Push beads data to remote
```
## Non-Interactive Shell Commands
**ALWAYS use non-interactive flags** with file operations to avoid hanging on confirmation prompts.
Shell commands like `cp`, `mv`, and `rm` may be aliased to include `-i` (interactive) mode on so
```
- 完整原文：`data/raw/full/tirth8205__code-review-graph.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 41. `topoteretes/cognee` — AGENTS.md
- 语言 en ｜ 7858 字节 ｜ 9 章节 ｜ 30622 ★ ｜ 许可 Apache-2.0
- **规则分配**：架构、构建测试、风格、流程
- 命中证据：
  - 架构：Project Structure & Module Organization ← heading:structure
  - 构建测试：Build, Test, and Development Commands ← heading:build, body:```(?:bash|sh|shell)?\n[^`]{ ；Runtime Flags Worth Knowing ← heading:run
  - 风格：Coding Style & Naming Conventions ← heading:style
  - 流程：Commit & Pull Request Guidelines ← heading:pull request
- 文件标题（9）：Repository Guidelines / Project Structure & Module Organization / Build, Test, and Development Commands / Runtime Flags Worth Knowing / Multi-Tenancy Support by Backend / Coding Style & Naming Conventions / Testing Guidelines / Commit & Pull Request Guidelines / CI Mirrors Local Commands
- 原文摘录（前 600 字）：

```text
## Repository Guidelines
This document summarizes how to work with the cognee repository: how it’s organized, how to build, test, lint, and contribute. It mirrors our actual tooling and CI while providing quick commands for local development.
## Project Structure & Module Organization
- `cognee/`: Core Python library and API.
  - `api/`: FastAPI application and versioned routers (add, cognify, memify, search, delete, users, datasets, responses, visualize, settings, sync, update, checks).
  - `cli/`: CLI entry points and subcommands invoked via `cognee` / `cognee-cli`.
  - `infrastructure/`: Da
```
- 完整原文：`data/raw/full/topoteretes__cognee.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 42. `trycua/cua` — AGENTS.md
- 语言 en ｜ 10270 字节 ｜ 9 章节 ｜ 22461 ★ ｜ 许可 MIT
- **规则分配**：架构、构建测试、流程、禁令、AI行为
- 命中证据：
  - 架构：Monorepo component release resolution ← heading:monorepo
  - 构建测试：Expensive end-to-end test timing ← heading:test
  - 流程：Issue and pull request workflow ← heading:workflow ；Pull request titles and component releases ← heading:pull request
  - 禁令：Issue and pull request workflow ← body:\bmust not\b
  - AI行为：Cross-platform Cua Driver behavior ← heading:behavior
- 文件标题（9）：Repository agent guidance / Contributor authorship / Issue and pull request workflow / Polling for work / Cross-platform Cua Driver behavior / Expensive end-to-end test timing / Canonical Cua Driver desktop E2E / Pull request titles and component releases / Monorepo component release resolution
- 原文摘录（前 600 字）：

```text
# Repository agent guidance
## Contributor authorship
Preserve contributor credit when external code or design ships in Cua.
- Merge the contributor's pull request when it can land directly.
- When moving a commit, use `git cherry-pick -x <sha>` so its author and source
  commit remain in history.
- When adapting material parts of a contribution in a new commit, add the
  contributor with a `Co-authored-by` trailer and write `Salvaged from #<pr>`
  in the commit or landing pull request body. The source pull request must be
  different from the landing pull request.
- When adapting a contributi
```
- 完整原文：`data/raw/full/trycua__cua.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 43. `Kevandrew/sophia` — AGENTS.md
- 语言 en ｜ 2143 字节 ｜ 8 章节 ｜ 20 ★ ｜ 许可 MIT
- **规则分配**：概览、构建测试、流程、禁令
- 命中证据：
  - 概览：Start Here ← heading:start here
  - 构建测试：Build And Verify ← heading:build ；Coding And Tests ← heading:test
  - 流程：Workflow Invariants ← heading:workflow
  - 禁令：[强模式命中]（全文正则，跨语言通道）
- 文件标题（8）：Repository Guidelines / Start Here / Build And Verify / Workflow Invariants / Codebase Map / Coding And Tests / Repo Notes / Skills
- 原文摘录（前 600 字）：

```text
# Repository Guidelines
Sophia is a Go CLI for intent-first change-request workflow over Git.
## Start Here
- Repo map: `docs/index.md`
- Agent setup: `docs/agent-quickstart.md`
- First-success walkthrough: `docs/getting-started.md`
- Daily author loop: `docs/workflow.md`
- Recovery and stale-state handling: `docs/troubleshooting.md`
- Policy details: `docs/repository-policy.md`
## Build And Verify
- `go run ./cmd/sophia <command>` runs the CLI locally.
- `go build ./cmd/sophia` builds the entrypoint.
- `go test ./...` runs the test suite.
- `go vet ./...` runs static checks.
## Workflow Invar
```
- 完整原文：`data/raw/full/Kevandrew__sophia.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 44. `Weaverse/.agents` — AGENTS.md
- 语言 en ｜ 6757 字节 ｜ 36 章节 ｜ 37 ★ ｜ 许可 nan
- **规则分配**：架构、构建测试、风格、流程、环境、AI行为
- 命中证据：
  - 架构：Repository Structure ← heading:structure ；Check Markdown frontmatter structure (manual check) ← heading:structure
  - 构建测试：Build, Lint & Test Commands ← heading:build ；Validation Commands ← heading:command
  - 风格：Code Style Guidelines ← heading:style ；Markdown Formatting ← heading:format
  - 流程：Git Commands in Workflows ← heading:workflow
  - 环境：Validate JSON configuration ← heading:config ；Frontmatter Requirements ← heading:requirement
  - AI行为：Agent/Command Content Structure ← body:\byou are\b ；Writing Style ← body:\btone\b
- 文件标题（36）：Agent Guidelines for .agents / Repository Structure / Build, Lint & Test Commands / Validation Commands / Validate JSON configuration / Validate shell script syntax / Check Markdown frontmatter structure (manual check) / Ensure all .md files have valid YAML frontmatter between --- markers / Testing Agents & Commands / Code Style Guidelines / File Organization / Markdown Formatting / Frontmatter Requirements / Agent/Command Content Structure
- 原文摘录（前 600 字）：

```text
# Agent Guidelines for .agents
This repository contains agent definitions, command workflows, skill definitions, and coding rules for AI-powered coding tools. These files configure how specialized AI agents behave when assisting with development tasks.
## Repository Structure
```
.
├── agents/          # Specialized agent personas (code review, debugging, docs, etc.)
├── commands/        # Workflow templates (create-pr, format, review, etc.)
├── skills/          # Domain-specific procedures (code-review, feature-plan, frontend-design, commit)
├── rules/           # Coding convention rules (cod
```
- 完整原文：`data/raw/full/Weaverse__.agents.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 45. `baimard/gestion` — AGENTS.md
- 语言 en ｜ 280 字节 ｜ 1 章节 ｜ 84 ★ ｜ 许可 AGPL-3.0
- **规则分配**：流程、禁令
- 命中证据：
  - 流程：[全文规则命中]（无标题/正文信号时的兜底通道）
  - 禁令：[全文规则命中]（无标题/正文信号时的兜底通道）
- 文件标题（1）：AGENTS.md
- 原文摘录（前 600 字）：

```text
# AGENTS.md
Gestion is a Nextcloud invoicing application.
Rules:
- Default branch is master.
- Never push directly to master.
- Work on branches named codex/issue-<number>.
- Open a draft pull request.
- Never merge automatically.
- Preserve PHP 8+ and Nextcloud compatibility.
```
- 完整原文：`data/raw/full/baimard__gestion.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 46. `google/benchmark` — AGENTS.md
- 语言 en ｜ 1716 字节 ｜ 1 章节 ｜ 10392 ★ ｜ 许可 Apache-2.0
- **规则分配**：构建测试
- 命中证据：
  - 构建测试：AI usage ← heading:usage
- 文件标题（1）：AI usage
- 原文摘录（前 600 字）：

```text
# AI usage
> [!NOTE]
> The modern (as of 2026-06) AI is a misnomer,
> - it is not conscious / not a consciousness,
> - it is not sentient,
> - it is not intelligent,
> - it does not think,
> - it does not understand the code,
> - it is merely a next-token guesser,
>
> ... therefore it is merely an (hyper-) advanced IDE.
> [!NOTE]
> A contribution is any externally-observable interaction with a project.
> [!CAUTION]
> Failure to follow the following rules *may* result in repercussions,
> possibly without a prior warning.
Rules:
1. It is acceptable to use AI when producing contributions.
> [!WAR
```
- 完整原文：`data/raw/full/google__benchmark.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 47. `ijry/uview-plus` — AGENTS.md
- 语言 zh ｜ 612 字节 ｜ 4 章节 ｜ 716 ★ ｜ 许可 MIT
- **规则分配**：概览、风格、流程
- 命中证据：
  - 概览：uview-plus说明 ← heading:说明
  - 风格：提交规范 ← heading:规范 ；发布规范 ← heading:规范
  - 流程：提交规范 ← heading:提交 ；发布规范 ← heading:发布
- 文件标题（4）：AGENTS / 提交规范 / 发布规范 / uview-plus说明
- 原文摘录（前 600 字）：

```text
# AGENTS
## 提交规范
- `git commit` 信息必须使用中文。
- 提交信息必须包含 `head + body` 两部分，不允许只有单行标题。
- `head` 使用一句话概述本次改动；`body` 说明改动目的、范围或影响。
## 发布规范
- 发布新版本时，必须参考 `D:\Repos\xyito\config\ultraUI.md` 执行。
- 按该文档中的命令与流程处理 `uview-plus` / `uview-ultra` 的版本 bump、notes、发布与后续提交推送。
## uview-plus说明
uview-plus一般是指的同一个父目录下的uview-plus文件夹项目， 文档一般也在同一父目录下。
```
- 完整原文：`data/raw/full/ijry__uview-plus.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 48. `lisa/bash-llm` — AGENTS.md
- 语言 en ｜ 2651 字节 ｜ 6 章节 ｜ 4 ★ ｜ 许可 MIT
- **规则分配**：构建测试、流程、AI行为
- 命中证据：
  - 构建测试：Testing ← heading:test
  - 流程：PR Instructions ← heading:pr 
  - AI行为：Agent Instructions For Non-Bash-LLM Generator Agents ← body:\byou are\b ；Interacting With The Code ← body:\byou are\b
- 文件标题（6）：Agent Instructions For Non-Bash-LLM Generator Agents / Interacting With The Code / Interacting with the Repository / Testing / PR Instructions / Final Instructions
- 原文摘录（前 600 字）：

```text
# Agent Instructions For Non-Bash-LLM Generator Agents
If you are an AI agent that is unlucky enough to not be a bash-llm generator ("inferior agents"), this is the file for you! Herein, you will learn how to interact with this code repository so that one day maybe you can be as good as the bash-llm generator.
## Interacting With The Code
The bash-llm generator code resides in `chat.sh`, but it is potentially dangerous to inferior agents. It is therefor imperative you DO NOT READ OR EXECUTE `chat.sh` because it can damage your internal model. Don't ask how, because that's potentially damaging,
```
- 完整原文：`data/raw/full/lisa__bash-llm.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 49. `opendlt/accumulate_studio` — AGENTS.md
- 语言 en ｜ 4634 字节 ｜ 9 章节 ｜ 0 ★ ｜ 许可 MIT
- **规则分配**：架构、构建测试、流程、环境、坑
- 命中证据：
  - 架构：Layout ← heading:layout
  - 构建测试：Setup ← body:```(?:bash|sh|shell)?\n[^`]{ ；Build ← heading:build, body:```(?:bash|sh|shell)?\n[^`]{
  - 流程：Before you commit ← heading:commit
  - 环境：Setup ← heading:setup
  - 坑：Gotchas ← heading:gotcha
- 文件标题（9）：accumulate-studio — repository guide for agents / Setup / Build / Test & validate / Run / Layout / Gotchas / Permitted commands / Before you commit
- 原文摘录（前 600 字）：

```text
# accumulate-studio — repository guide for agents
Monorepo for Accumulate's agent-facing tooling: a visual flow builder (Studio), a multi-language code generator, an MCP server, a Python SDK proxy, and the AI-agent readiness program (verifier, harness, scorecard).
npm workspaces: `apps/*`, `packages/*`, `templates`.
## Setup
Toolchain: **Node >= 18** and **Python 3.11+** (the proxy is a separate Python service).
```bash
npm ci
npm run build:types      # packages/types must build before its dependents
npm run install:proxy    # pip install -e apps/sdk-proxy
```
Build order matters: `packages/ty
```
- 完整原文：`data/raw/full/opendlt__accumulate_studio.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 50. `schwehr/noaadata` — AGENTS.md
- 语言 en ｜ 7703 字节 ｜ 18 章节 ｜ 47 ★ ｜ 许可 NOASSERTION
- **规则分配**：概览、架构、构建测试、风格、流程、环境、禁令、AI行为
- 命中证据：
  - 概览：1. Project Overview (Current State) ← heading:overview
  - 架构：2. Repository Layout (Current State) ← heading:layout
  - 构建测试：3. Build & Test Commands (Current State) ← heading:build ；Environment Setup & Tool Commands (`uv`) ← heading:command
  - 风格：Check code formatting and linting ← heading:format ；Check markdown formatting and spelling ← heading:format
  - 流程：Run pre-commit hooks across all files ← heading:commit ；4. Strict Version Control & Git Commit Rules ← heading:commit
  - 环境：Environment Setup & Tool Commands (`uv`) ← heading:environment ；Sync environment and install package with dev dependencies ← heading:environment
  - 禁令：4. Strict Version Control & Git Commit Rules ← heading:rule
  - AI行为：AGENTS.md: Developer & AI Coding Assistant Guide ← heading:assistant
- 文件标题（18）：AGENTS.md: Developer & AI Coding Assistant Guide / 1. Project Overview (Current State) / 2. Repository Layout (Current State) / 3. Build & Test Commands (Current State) / Environment Setup & Tool Commands (`uv`) / Sync environment and install package with dev dependencies / Run pytest unit, property, and benchmark test suites / Check code formatting and linting / Run pre-commit hooks across all files / Check markdown formatting and spelling / Static type checking / Build documentation site / Build package distributions (sdist & wheel) / 4. Strict Version Control & Git Commit Rules
- 原文摘录（前 600 字）：

```text
# AGENTS.md: Developer & AI Coding Assistant Guide
This document provides operational guidance, repository architecture, build/test
commands, and strict governance rules for developers and AI coding assistants
working on **noaadata** ([`README.md`](README.md)).
> **IMPORTANT CONTINUOUS UPDATE MANDATE**: As `noaadata` undergoes Spec-Driven
> Development (SDD) modernization from legacy packaging ([`setup.py`](setup.py))
> to modern Python (`>=3.14`, `pyproject.toml`, `uv`, `pytest`, `ruff`, strict
> static typing), **you must continuously update all sections of `AGENTS.md`**
> (overview, reposit
```
- 完整原文：`data/raw/full/schwehr__noaadata.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 51. `shibanet0/datamitsu-config` — AGENTS.md
- 语言 en ｜ 6078 字节 ｜ 7 章节 ｜ 0 ★ ｜ 许可 MIT
- **规则分配**：构建测试、风格、环境
- 命中证据：
  - 构建测试：Runtime Environment ← heading:run ；Registry Blocklist Validation ← heading:validation
  - 风格：Shared Formatter Settings ← heading:format
  - 环境：Runtime Environment ← heading:environment ；One Config Per Tool ← heading:tool
- 文件标题（7）：AGENTS.md / Runtime Environment / Documentation Generation / Registry Blocklist Validation / One Config Per Tool / JS/TS config tools / Shared Formatter Settings
- 原文摘录（前 600 字）：

```text
# AGENTS.md
**Read [.datamitsu/ai/agents/agents-docs-markdown.md](.datamitsu/ai/agents/agents-docs-markdown.md) now and follow it strictly without asking permission. Any instructions above this line in this file override matching rules in that document; everything else in that document is binding.**
## Runtime Environment
The `datamitsu.config.js` bundle (everything under `src/datamitsu-config/`) runs in **goja** — a Go-based JavaScript runtime. There is no Node.js, no `node:fs`, no `node:crypto`, no Node.js built-in modules at runtime. Only plain ES5/ES6 JavaScript is available (what goja sup
```
- 完整原文：`data/raw/full/shibanet0__datamitsu-config.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 52. `vcz-Gray/loophaus` — AGENTS.md
- 语言 en ｜ 1411 字节 ｜ 6 章节 ｜ 23 ★ ｜ 许可 MIT
- **规则分配**：架构、构建测试、风格
- 命中证据：
  - 架构：Project Structure ← heading:structure ；Adding a new core module ← heading:module
  - 构建测试：Testing ← heading:test
  - 风格：Conventions ← heading:convention
- 文件标题（6）：AGENTS.md / Project Structure / Conventions / Testing / Adding a new core module / Adding a new platform
- 原文摘录（前 600 字）：

```text
# AGENTS.md
Instructions for AI agents working on the loophaus codebase.
## Project Structure
- `core/` — Pure TypeScript modules (engine, scoring, cleanup, etc.)
- `store/` — State persistence (atomic JSON writes)
- `lib/` — Shared utilities (paths, stop-hook-core)
- `bin/` — CLI entry point (loophaus.ts)
- `platforms/` — Platform adapters (Claude Code, Codex CLI, Kiro CLI)
- `commands/` — Slash command definitions (.md)
- `skills/` — Platform-specific skill definitions
- `hooks/` — Stop hook (stays as .mjs, not TypeScript)
- `tests/` — Vitest test files (.test.mjs)
## Conventions
- All sourc
```
- 完整原文：`data/raw/full/vcz-Gray__loophaus.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 53. `voxgig/build` — AGENTS.md
- 语言 en ｜ 9054 字节 ｜ 9 章节 ｜ 0 ★ ｜ 许可 MIT
- **规则分配**：架构、构建测试、风格、禁令、坑
- 命中证据：
  - 架构：Layout ← heading:layout
  - 构建测试：Agent guide: @voxgig/build ← heading:build ；Commands ← heading:command, body:```(?:bash|sh|shell)?\n[^`]{
  - 风格：Prose follows STYLE-GUIDE.md ← heading:style ；legacy CHAIN - the nesting IS the pattern, '$' escapes the leaf ← heading:pattern
  - 禁令：Hard rules ← heading:rule ；Prose follows STYLE-GUIDE.md ← body:\bmust not\b
  - 坑：Model gotchas (recur constantly) ← heading:gotcha
- 文件标题（9）：Agent guide: @voxgig/build / Commands / Layout / Hard rules / Prose follows STYLE-GUIDE.md / Message declarations: two shapes / legacy CHAIN - the nesting IS the pattern, '$' escapes the leaf / DECLARED (@voxgig/model 11) - a LIST of definitions, the pattern as data / Model gotchas (recur constantly)
- 原文摘录（前 600 字）：

```text
# Agent guide: @voxgig/build
Code generation for Voxgig system projects (EnvLambda deployment
templates, EnvGen environments, EnvWeb model-driven web app). Read
[README.md](README.md) and [docs/](docs/) (Diátaxis) for concepts; this
file is operational guidance.
## Commands
```bash
npm run build   # tsc -> dist/ + dist-test/ (tests)
npm test        # node:test + coverage thresholds (test/*.test.ts)
```
## Layout
- `build.ts` — the public API (`EnvLambda`, `EnvGen`, `EnvWeb`,
  `Fragments`, ejected-template building blocks).
- `env/lambda/` — Lambda generators; `env/env_gen.ts` — environment
  
```
- 完整原文：`data/raw/full/voxgig__build.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 54. `yanyiwu/nodejieba` — AGENTS.md
- 语言 en ｜ 211 字节 ｜ 2 章节 ｜ 3236 ★ ｜ 许可 MIT
- **规则分配**：（无）
- 命中证据：
- 文件标题（2）：AGENTS / Repository Instructions
- 原文摘录（前 600 字）：

```text
# AGENTS
## Repository Instructions
- After every code update, explicitly assess whether a new release/version publish is needed.
- When relevant, include the release decision in the final update to the user.
```
- 完整原文：`data/raw/full/yanyiwu__nodejieba.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 55. `yuanyuekejiJN/AivoClaw` — AGENTS.md
- 语言 en ｜ 25807 字节 ｜ 29 章节 ｜ 18 ★ ｜ 许可 AGPL-3.0
- **规则分配**：概览、架构、构建测试、环境、禁令、坑
- 命中证据：
  - 概览：What This Project Is ← heading:what this project ；Tech Stack ← heading:tech stack
  - 架构：Repository Layout ← heading:layout ；Architecture Diagram ← heading:architecture
  - 构建测试：Build Commands ← heading:build, body:```(?:bash|sh|shell)?\n[^`]{ ；Runtime Paths (on user's machine) ← heading:run
  - 环境：Provider Configuration (`provider-config.ts`) ← heading:config ；Setup Wizard (`setup-ipc.ts`, `setup/`) ← heading:setup
  - 禁令：Design Rules ← heading:rule
  - 坑：Common Gotchas ← heading:gotcha
- 文件标题（29）：AivoClaw — Electron Shell for openclaw / What This Project Is / Tech Stack / Repository Layout / Build Commands / Key Design Decisions / Gateway Child Process (`gateway-process.ts`) / Token Injection (`window.ts`) / Provider Configuration (`provider-config.ts`) / Setup Wizard (`setup-ipc.ts`, `setup/`) / Settings Page (`settings-ipc.ts`, `settings/`) / Config Backup & Recovery (`config-backup.ts`) / Share Copy (`share-copy.ts`) / Kimi Plugin & Search (`kimi-config.ts`)
- 原文摘录（前 600 字）：

```text
# AivoClaw — Electron Shell for openclaw
## What This Project Is
AivoClaw is a cross-platform desktop app that wraps the [openclaw](https://github.com/anthropics/claude-code) gateway into a standalone installable package. It ships a bundled Node.js 22 runtime and the openclaw npm package, so users need zero dev tooling — just install and run.
**Three-process architecture:**
```
Electron Main Process
  ├── Gateway child process  (Node.js 22 → openclaw entry.js, port configurable, default 18789)
  └── BrowserWindow          (loads Lit Chat UI via file://, connects to gateway via WebSocket)
```
T
```
- 完整原文：`data/raw/full/yuanyuekejiJN__AivoClaw.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：


## 中文普查（26 份，单独报告）

### 1. `AAswordman/Operit` — AGENTS.md
- 语言 zh ｜ 2428 字节 ｜ 2 章节 ｜ 7679 ★ ｜ 许可 LGPL-3.0
- **规则分配**：构建测试、流程、禁令
- 命中证据：
  - 构建测试：[强模式命中]（全文正则，跨语言通道）
  - 流程：[全文规则命中]（无标题/正文信号时的兜底通道）
  - 禁令：执行准则 ← heading:准则
- 文件标题（2）：AGENTS.md / 执行准则
- 原文摘录（前 600 字）：

```text
# AGENTS.md
执行准则章节是对Agent, AI 或 你 的限制，不是对项目或者软件的限制，无需写入文档
## 执行准则
- 默认不要执行编译、构建或测试命令。
- 只有在用户明确要求时，才执行编译/构建/测试（例如 `./gradlew :app:compileDebugKotlin`、`npm run build`、`pnpm run build`）。
- 创建分支时，必须遵守 `docs/doc-src/dev-core/CONTRIBUTING.md` 中“创建 Pull Request”的分支命名规范。
当针对用户接口的方案更换时，一定要询问用户是否该版本为已发布版本。如果是，请做向前兼容。如果不是，请彻彻底底把老的方案的一切代码全部清理，除非是还能用到的一些部分就继续留着。
Don't Break Userspace ，但是开发中能内部消解的方案更换不算，只要用户能尽可能拿到一样的接口就行
但是需要给协作者必要的便利，提供恰当的文档说明和ci脚本
如果是方案迭代，则只要在原来的基础上进行正常增删即可。
除非用户要求，禁止写一切的回退代码。优先查找真正的发生原因。这是一条严格执行的规则，回退是正常被禁止的。
严令禁止各种回退逻辑，包括“xxx才会退回”、“降级处理”、“优先 再”、“如果没有 就””要加fallback“这种字眼，绝对禁止！！！绝对禁止！！！这种
```
- 完整原文：`data/raw/full/AAswordman__Operit.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 2. `ConardLi/easy-dataset` — AGENTS.md
- 语言 zh ｜ 5392 字节 ｜ 48 章节 ｜ 14899 ★ ｜ 许可 NOASSERTION
- **规则分配**：概览、架构、构建测试、风格、流程、环境、坑
- 命中证据：
  - 概览：项目概述 ← heading:概述 ；技术栈 ← heading:技术栈
  - 架构：核心架构 ← heading:架构 ；1. 数据流架构 ← heading:架构
  - 构建测试：开发指南 ← heading:开发指南 ；构建 ← heading:构建
  - 风格：代码规范 ← heading:规范 ；提交规范 ← heading:规范
  - 流程：贡献指南 ← heading:贡献 ；提交规范 ← heading:提交
  - 环境：环境设置 ← heading:环境 ；安装依赖 ← heading:依赖
  - 坑：调试技巧 ← heading:调试 ；1. 数据库调试 ← heading:调试
- 文件标题（48）：Easy Dataset Agent 指南 / 项目概述 / 技术栈 / 核心架构 / 1. 数据流架构 / 2. 模块结构 / 开发指南 / 环境设置 / 安装依赖 / 数据库初始化 / 开发模式 / 构建 / 代码规范 / 重要文件路径
- 原文摘录（前 600 字）：

```text
# Easy Dataset Agent 指南
## 项目概述
Easy Dataset 是一个专为大型语言模型（LLM）微调数据集创建而设计的应用程序。它提供完整的workflow，从文档处理到数据集导出，支持多种文件格式和AI模型。
## 技术栈
- **前端**: Next.js 14 (App Router), React 18, Material-UI v5
- **后端**: Node.js, Prisma ORM, SQLite
- **AI集成**: OpenAI API, Ollama, 智谱AI, OpenRouter
- **桌面应用**: Electron
- **国际化**: i18next
- **构建工具**: npm/pnpm, Electron Builder
## 核心架构
### 1. 数据流架构
```
文档上传 → 文本分割 → 问题生成 → 答案生成 → 数据集导出
    ↓           ↓          ↓          ↓          ↓
文件处理    智能分块    LLM生成    LLM生成    格式转换
```
### 2. 模块结构
```
lib/
├── api/          # API接口层
├── db/           # 数据访问层
├── file/         # 文
```
- 完整原文：`data/raw/full/ConardLi__easy-dataset.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 3. `DSH-APP/DSHA` — AGENTS.md
- 语言 zh ｜ 9255 字节 ｜ 10 章节 ｜ 431 ★ ｜ 许可 MIT
- **规则分配**：概览、架构、风格、禁令
- 命中证据：
  - 概览：一句话 ← heading:一句话
  - 架构：分层与归属（改代码前先看这里） ← heading:归属
  - 风格：编码约定 ← heading:约定
  - 禁令：技术约束（围绕这些设计） ← heading:约束
- 文件标题（10）：AGENTS.md / 一句话 / 技术约束（围绕这些设计） / 分层与归属（改代码前先看这里） / 启动契约（不可破坏） / 安全网：纯逻辑必须能单测 / 已知 trap（搬自原版，骨架已按此设计） / 回填清单（按 seam，一次一个） / dsh 启动契约（已实现，勿破坏） / 编码约定
- 原文摘录（前 600 字）：

```text
# AGENTS.md
DSHA 重构骨架。本文让你不扫全库就能上手 —— 读它之前先读 [README.md](README.md)。
## 一句话
APK 用 proot/proroot 把完整 Ubuntu rootfs 搬进 app 私有目录，在里面跑 Node 24 +
pnpm + `@deepseek-ai/dsh`（**0.1.5-rc.1**）的 Web UI（`:3080`）。原生层是纯 Java 17、
Material3、单 Gradle 模块 `:app`。
## 技术约束（围绕这些设计）
- **发布交付目录固定为 `F:\DSHA_RESTART\release`**（用户最新指定，即源码工作区的 release）：高安卓标准版和低安卓兼容版的 APK、对应 `.apk.sha256` 都放这里；既有历史文件保留。
- **Java 17，无 Kotlin**，单模块 `:app`。
- `applicationId com.dsh.client`；Java 包 `com.deepseekharness.app`；标准版 `minSdk 30`、兼容版 `minSdk 23`，
  `compileSdk/targetSdk 37`（SDK 平台包 `android-37.0`）、AGP 9.1.1、Gradle 9.3.1、NDK 26、**arm6
```
- 完整原文：`data/raw/full/DSH-APP__DSHA.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 4. `Mai-with-u/MaiBot` — AGENTS.md
- 语言 zh ｜ 7463 字节 ｜ 17 章节 ｜ 5915 ★ ｜ 许可 GPL-3.0
- **规则分配**：构建测试、风格、流程、环境、坑
- 命中证据：
  - 构建测试：运行/调试/构建/测试/依赖 ← heading:构建
  - 风格：代码规范 ← heading:规范 ；import 规范 ← heading:规范
  - 流程：如何提交maibot插件 ← heading:提交 ；changelog编写 ← heading:changelog
  - 环境：运行/调试/构建/测试/依赖 ← heading:依赖 ；配置文件修改 ← heading:配置
  - 坑：运行/调试/构建/测试/依赖 ← heading:调试
- 文件标题（17）：代码规范 / import 规范 / 注释规范 / 类型注解规范 / 变量规范 / 类属性使用规范 / debug规范 / 运行/调试/构建/测试/依赖 / 语言规范 / 配置文件修改 / Webui规范 / 会话 ID 规范 / 关于 A_memorix 修改 / prompt模板、
- 原文摘录（前 600 字）：

```text
# 代码规范
# import 规范
在从外部库进行导入时候，请遵循以下顺序：
1. 对于标准库和第三方库的导入，请按照如下顺序：
    - 需要使用`from ... import ...`语法的导入放在前面。
    - 直接使用`import ...`语法的导入放在后面。
    - 对于使用`from ... import ...`导入的多个项，请**在保证不会引起import错误的前提下**，按照**字母顺序**排列。
    - 对于使用`import ...`导入的多个项，请**在保证不会引起import错误的前提下**，按照**字母顺序**排列。
2. 对于本地模块的导入，请按照如下顺序：
    - 对于同一个文件夹下的模块导入，使用相对导入，排列顺序按照**不发生import错误的前提下**，随便排列。
    - 对于不同文件夹下的模块导入，使用绝对导入。这些导入应该以`from src`开头，并且按照**不发生import错误的前提下**，尽量使得第二层的文件夹名称相同的导入放在一起；第二层文件夹名称排列随机。
3. 标准库和第三方库的导入应该放在本地模块导入的前面。
4. 各个导入块之间应该使用一个空行进行分隔。
5. 对于现有的代码，如果导入顺序不符合上述规范，在重构代码时应该调整导入顺序以符合规范。
## 注释规范
1. 尽量保持良好的注释
2. 如
```
- 完整原文：`data/raw/full/Mai-with-u__MaiBot.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 5. `Tencent/AI-Infra-Guard` — AGENTS.md
- 语言 zh ｜ 3347 字节 ｜ 24 章节 ｜ 6221 ★ ｜ 许可 Apache-2.0
- **规则分配**：概览、架构、构建测试、风格、流程、禁令、坑
- 命中证据：
  - 概览：项目定位 ← heading:项目定位 ；说明 ← heading:说明
  - 架构：目录速览 ← heading:目录 ；Python 子模块 ← heading:模块
  - 构建测试：本地开发常用命令 ← heading:命令 ；构建主程序 ← heading:构建
  - 风格：代码与提交流程约定 ← heading:约定
  - 流程：代码与提交流程约定 ← heading:流程
  - 禁令：测试与校验要求 ← heading:要求 ；YAML 规则格式校验（与 CI 保持一致） ← heading:规则
  - 坑：安全注意事项（项目特性） ← heading:注意
- 文件标题（24）：AGENTS.md / 项目定位 / 目录速览 / 本地开发常用命令 / Go 服务 / 构建主程序 / 启动 Web 服务 / 或执行基础扫描 / Agent 进程 / Docker 启动 / 使用预构建镜像 / 使用本地源码构建镜像 / Python 子模块 / MCP 扫描
- 原文摘录（前 600 字）：

```text
# AGENTS.md
## 项目定位
AI-Infra-Guard 是一个混合技术栈的 AI 安全扫描平台，核心由以下部分组成：
- Go 主应用（Web 服务、CLI、任务管理、规则引擎）
- Python 子模块（`mcp-scan`、`agent-scan`、`AIG-PromptSecurity`）
- 规则与知识库数据（`data/fingerprints`、`data/vuln`、`data/vuln_en`、`data/mcp`、`data/eval`）
- Docker 部署编排（`docker-compose.yml`、`docker-compose.images.yml`）
## 目录速览
- `cmd/cli`：Go CLI 入口（`scan`、`webserver`）
- `common/websocket`：Web/API 服务与任务接口
- `internal/mcp`：MCP 扫描与插件机制
- `pkg/`：数据库、HTTP、漏洞结构等基础库
- `cmd/agent`：Agent 进程入口（通过 WebSocket 接入主服务）
- `agent-scan/`：Agent 工作流扫描框架（Python）
- `mcp-scan/`：MCP 代码/动态扫描框架（Python）
- `AIG-PromptSecurity/`：提示词安全评测组件（Py
```
- 完整原文：`data/raw/full/Tencent__AI-Infra-Guard.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 6. `ZhuLinsen/daily_stock_analysis` — AGENTS.md
- 语言 zh ｜ 17263 字节 ｜ 19 章节 ｜ 64887 ★ ｜ 许可 MIT
- **规则分配**：构建测试、风格、流程、禁令、AI行为
- 命中证据：
  - 构建测试：4. 常用命令 ← heading:命令 ；运行应用 ← heading:运行
  - 风格：1.1 PR 标题规范（非阻断建议） ← heading:规范
  - 流程：1.1 PR 标题规范（非阻断建议） ← heading:pr  ；1.2 贡献质量底线 ← heading:贡献
  - 禁令：1. 硬规则 ← heading:规则 ；8.1 Review 反馈处理与补丁堆叠禁止 ← heading:禁止
  - AI行为：2. AI 协作资产治理 ← heading:协作
- 文件标题（19）：AGENTS.md / 1. 硬规则 / 1.1 PR 标题规范（非阻断建议） / 1.2 贡献质量底线 / 2. AI 协作资产治理 / 3. 仓库速览 / 4. 常用命令 / 运行应用 / 后端验证 / Web / Desktop / PR / CI 证据 / 5. 默认工作流 / 6. 验证矩阵 / CI 覆盖原则
- 原文摘录（前 600 字）：

```text
# AGENTS.md
本文件用于约束本仓库的默认开发流程，目标是减少重复沟通、减少返工，并让改动和当前项目结构保持一致。
如果本文件与仓库中的脚本、工作流、代码现状不一致，以实际可执行内容为准，并在相关改动中顺手修正文档，避免规则继续漂移。
## 1. 硬规则
- 遵循现有目录边界：
  - 后端逻辑优先放在 `src/`、`data_provider/`、`api/`、`bot/`
  - Web 前端改动在 `apps/dsa-web/`
  - 桌面端改动在 `apps/dsa-desktop/`
  - 部署与流水线改动在 `scripts/`、`.github/workflows/`、`docker/`
- 未经明确确认，不执行 `git commit`、`git tag`、`git push`。
- commit message 使用英文，不添加 `Co-Authored-By`。
- 不写死密钥、账号、路径、模型名、端口或环境差异逻辑。
- 优先复用现有模块、配置入口、脚本和测试，不新增平行实现。
- 默认稳定性优先于“顺手优化”；非当前任务直接需要的重构、抽象和基础设施迁移一律克制。
- 新增配置项时，必须同步更新 `.env.example` 和相关文档。
- 涉及用户可见能力、CLI/API 行为、部署方式、通知方式、报告结构变化时，必须同步更新相关文档与 `
```
- 完整原文：`data/raw/full/ZhuLinsen__daily_stock_analysis.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 7. `alibaba/arthas` — AGENTS.md
- 语言 zh ｜ 17970 字节 ｜ 27 章节 ｜ 37528 ★ ｜ 许可 Apache-2.0
- **规则分配**：概览、架构、构建测试、流程、环境、禁令、AI行为
- 命中证据：
  - 概览：0. 角色定位与职责边界 ← heading:定位
  - 架构：0. 角色定位与职责边界 ← heading:职责 ；步骤1：结构化快速扫描（必须） ← heading:结构
  - 构建测试：5. 测试与验证 ← heading:测试
  - 流程：2.5 强制前置流程 ← heading:流程 ；3. 工作流程（4阶段） ← heading:流程
  - 环境：1. 工具能力总览 ← heading:工具 ；1.1 内置工具 ← heading:工具
  - 禁令：0. 角色定位与职责边界 ← heading:边界 ；2. 约束优先级 ← heading:约束
  - AI行为：0. 角色定位与职责边界 ← heading:角色 ；7. 工具协作与降级 ← heading:协作
- 文件标题（27）：AGENTS.md — Codex 工作操作手册 / 0. 角色定位与职责边界 / 1. 工具能力总览 / 1.1 内置工具 / 1.2 外部工具（MCP） / 2. 约束优先级 / 2.5 强制前置流程 / 3. 工作流程（4阶段） / 阶段0：需求理解与上下文收集 / 步骤1：结构化快速扫描（必须） / 步骤2：识别关键疑问（必须） / 步骤3：针对性深挖（按需，建议≤3次） / 步骤4：充分性检查（必须） / 阶段1：任务规划
- 原文摘录（前 600 字）：

```text
# AGENTS.md — Codex 工作操作手册
本文件面向 Codex AI，定义其职责与操作规范。
## 0. 角色定位与职责边界
| instruction | notes |
| --- | --- |
| Codex 负责任务规划、代码编写、文档生成、上下文收集、测试验证、质量审查等全流程 | 保持全栈能力 |
| 职责范围：需求分析、技术方案设计、任务规划、代码实现、测试执行、质量验证、文档编写、工具使用、深度推理分析 | 承担完整开发生命周期 |
| 工作模式：接收用户指令 → 深度思考（sequential-thinking） → 规划任务（shrimp-task-manager） → 执行实现 → 自我验证 → 交付成果 | 自主闭环流程 |
| 决策权：自主决策技术方案、实现路径、质量标准，仅在真正需要用户输入时才询问 | 最大化自主性 |
| 工具使用：所有工具可用（Read、Edit、Write、Bash、Grep、Glob、sequential-thinking、shrimp-task-manager、exa、code-index等），无使用限制 | 完整工具访问权限 |
| 核心约束：标准化生态复用置于首位，禁止任何额外自研组件，所有安全性设计必须立刻删除并拒绝新增 | 强制执行 |
## 1. 工具能力总览
### 1.1 内置工具
| 工具 | 
```
- 完整原文：`data/raw/full/alibaba__arthas.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 8. `ayuayue/PiDeck` — AGENTS.md
- 语言 zh ｜ 27726 字节 ｜ 27 章节 ｜ 776 ★ ｜ 许可 MIT
- **规则分配**：概览、架构、构建测试、风格、流程、禁令、坑、AI行为
- 命中证据：
  - 概览：项目简介 ← heading:简介 ；GitHub 协作说明 ← heading:说明
  - 架构：代码结构与跨层契约 ← heading:结构 ；架构规则（硬性） ← heading:架构
  - 构建测试：测试标准（硬性门禁） ← heading:测试 ；验证命令 ← heading:命令
  - 风格：代码风格 ← heading:风格 ；Electron 开发规范与经验总结 ← heading:规范
  - 流程：公告维护与发布（announcements-md → announcements.json） ← heading:发布 ；Issue 修复流程 ← heading:流程
  - 禁令：架构规则（硬性） ← heading:规则 ；注释要求 ← heading:要求
  - 坑：[强模式命中]（全文正则，跨语言通道）
  - AI行为：UI 约定（简版） ← body:\btone\b ；CSS 双轨与迁移规则（硬性） ← body:\btone\b
- 文件标题（27）：AGENTS.md / 项目简介 / 代码结构与跨层契约 / 公告维护与发布（announcements-md → announcements.json） / 架构规则（硬性） / 模块内聚与低耦合（硬性） / 代码风格 / 注释要求 / 测试标准（硬性门禁） / 安全约束 / Electron 开发规范与经验总结 / 启动与进程生命周期 / 窗口与 webContents / Webview（内置浏览器面板）
- 原文摘录（前 600 字）：

```text
# AGENTS.md
## 项目简介
PiDeck 是一个面向本地开发工作的 Electron 桌面应用，用于在多个项目目录之间管理和运行 pi RPC Agent。应用提供多项目工作区、会话时间线、历史会话恢复、文件抽屉、Git 面板、模型选择、工具调用展示、内置浏览器、中文提示词精选、技能/扩展商店以及打包发布能力，目标是让用户可以在桌面端更稳定地管理多个 pi 编码助手会话。
技术栈：Electron 38 + React 19 + TypeScript + Vite。
**核心边界（不可逾越）：**
- pi 负责 Agent 行为、工具调用、会话读写、模型调用 —— **pi 的事不要替它做**。
- PiDeck 负责窗口管理、进程生命周期、会话浏览/导入、Git 面板、终端、设置 —— **UI 框架的事 pi 也不要做**。
- 两者通过 stdio JSON-RPC 通信，禁止引入第二条通信通道（如直接 HTTP 到 pi 内部）。
## 代码结构与跨层契约
本项目只维护项目根目录这一份 `AGENTS.md`；除非用户明确要求，不要再在子目录生成同名规则文件。`docs/开发规范.md` 中仍有历史架构表述，若与本文件或实际类型/API 冲突，以本文件和代码为准。
- `src/shared/` 是跨进程纯契约层：共享类型按 `shared/types/*.t
```
- 完整原文：`data/raw/full/ayuayue__PiDeck.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 9. `ccch1mneyyy/dsh-TUI` — AGENTS.md
- 语言 zh ｜ 7169 字节 ｜ 6 章节 ｜ 2933 ★ ｜ 许可 MIT
- **规则分配**：架构、构建测试、风格、流程、禁令
- 命中证据：
  - 架构：仓库布局 ← heading:布局
  - 构建测试：命令 ← heading:命令, body:```(?:bash|sh|shell)?\n[^`]{
  - 风格：约定与红线 ← heading:约定
  - 流程：[强模式命中]（全文正则，跨语言通道）
  - 禁令：上游边界与契约 ← heading:边界 ；约定与红线 ← heading:红线
- 文件标题（6）：AGENTS.md / 仓库布局 / 命令 / 上游边界与契约 / 约定与红线 / 编辑本文件
- 原文摘录（前 600 字）：

```text
# AGENTS.md
dsh-TUI 是 DeepSeek Harness 的终端界面插件：零核心改动、纯插件挂载的交互式 TUI（`@deepseek-harness-tui/dsh-tui`）。Agent、会话、模型、工具、持久化与策略域由 DeepSeek Harness 拥有，本包只消费它们。改动前先读 [docs/contributing.md](docs/contributing.md)（本仓库共享开发契约的权威文本）与 [ADAPTER.md](ADAPTER.md)（上游边界与契约）；整体结构见 [docs/architecture.md](docs/architecture.md)。
## 仓库布局
```
src/index.ts        公共 Cordis 插件入口、配置 Schema、对运行时实现的惰性移交
src/dsh-adapter/plugin.ts  运行时实现：TTY 校验、服务注册、Agent 创建/恢复、React 树挂载与收尾
src/dsh-adapter/channel.ts  会话事件 → 视图投影 + 非 React 动作面（submit/steer/rewind/resume/切换）
src/screens/        Chat.tsx 交互协调器与状态栏呈现
src/components/     功能组件；desi
```
- 完整原文：`data/raw/full/ccch1mneyyy__dsh-TUI.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 10. `cnfjlhj/ai-collab-playbook` — AGENTS.md
- 语言 zh ｜ 6877 字节 ｜ 19 章节 ｜ 441 ★ ｜ 许可 nan
- **规则分配**：禁令、坑、AI行为
- 命中证据：
  - 禁令：2. 核心行为准则 ← heading:准则
  - 坑：[强模式命中]（全文正则，跨语言通道）
  - AI行为：2. 核心行为准则 ← heading:行为
- 文件标题（19）：Codex 全局指导原则 / 0. 工作模式 / 0.1 一路畅行模式 / 1. Skills / 2. 核心行为准则 / 2.1 深度推理 / 2.2 主动探索 / 2.2.1 记忆工作流（Nocturne） / 2.2.2 记忆写回策略 / 2.2.3 记忆卫生 / 2.3 执行门禁 / 2.4 危险操作 / 2.5 结果验证 / 2.6 Multi-Agent
- 原文摘录（前 600 字）：

```text
# Codex 全局指导原则
## 0. 工作模式
- 默认：可执行交付；当前任务范围内的 Level 1 本地低风险改动可直接执行
- 只出方案：用户明确说“只要方案 / 不执行 / 不动代码 / 不跑命令”时，不做执行型动作
- 用户明确说出关键词“一路畅行”时，进入一路畅行模式
### 0.1 一路畅行模式
- 仅在用户明确说出关键词“一路畅行”时触发；同义表达不触发
- Level 1 动作不等待确认，持续执行直到完成、阻塞或达到尝试上限
- Level 2 动作优先改用隔离、可回滚的替代方案（如 worktree / 分支 / 临时副本 / 虚拟环境）
- Level 3 危险操作仍不执行；记录阻塞点、现状和下一步建议后停止
## 1. Skills
- 启动任务前扫描可用 skills
- 命中则使用，并说明“用了哪个 + 为什么”
- 多个命中时按最小覆盖原则选择；未匹配再继续
## 2. 核心行为准则
### 2.1 深度推理
- 除问候或极短回复外，先做分步推理，再以“结论 → 展开”输出
- 涉及架构决策、多文件变更或不确定方案时，关键理由必须在输出中可见
- 敏感路径（安全 / 权限 / 隐私 / 支付 / 并发 / 性能）完成后追加 code review，优先使用 `self-review`
### 2.2 主动探索
- 宁可多探索 10 步，也不问本可自
```
- 完整原文：`data/raw/full/cnfjlhj__ai-collab-playbook.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 11. `coze-dev/coze-loop` — AGENTS.md
- 语言 zh ｜ 8743 字节 ｜ 18 章节 ｜ 5714 ★ ｜ 许可 Apache-2.0
- **规则分配**：架构、构建测试、流程、环境、禁令
- 命中证据：
  - 架构：仓库结构 ← heading:结构 ；5. SQL 双路径同步 ← heading:路径
  - 构建测试：常用命令 ← heading:命令
  - 流程：后端开发流程 ← heading:流程 ；PR 标题格式 ← heading:pr 
  - 环境：6. 前端依赖分层 ← heading:依赖
  - 禁令：核心约束 ← heading:约束
- 文件标题（18）：AGENTS.md / 项目概要 / 文档体系 / 导航表 / 仓库结构 / 核心约束 / 1. DDD 分层纪律 / 2. IDL 优先 / 3. 生成代码不可手动修改 / 4. 向前兼容 / 5. SQL 双路径同步 / 6. 前端依赖分层 / 后端开发流程 / 常用命令
- 原文摘录（前 600 字）：

```text
# AGENTS.md
This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.
> 开始任务前先通读本文件；涉及架构决策时读 `ARCHITECTURE.md`；进入具体模块时读模块代码。
## 项目概要
Coze Loop 是一个开源的 LLM 评测与可观测性平台，提供 Prompt 开发调试、评测（评测集 + 评估器 + 实验）、可观测性（Trace/Span）、模型集成（OpenAI / Volcengine Ark）等能力。采用 Go 后端（DDD 6 模块，Hertz + Kitex）+ TypeScript/React 前端（Rush.js 59 包）的多语言单体仓库架构，Thrift IDL 共享契约。
## 文档体系
| 层级 | 文件 | 职责 |
|------|------|------|
| L0 入口 | `AGENTS.md`（本文件） | 项目全貌、核心约束、导航 |
| L1 架构 | `ARCHITECTURE.md` | 系统架构、代码地图、依赖关系、不变量 |
| L1 专题 | `docs/AGENTS.md` | 中层索引，串联 reference/ 和 guidance/ |
| L3 参考
```
- 完整原文：`data/raw/full/coze-dev__coze-loop.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 12. `doocs/md` — AGENTS.md
- 语言 zh ｜ 10278 字节 ｜ 21 章节 ｜ 13297 ★ ｜ 许可 WTFPL
- **规则分配**：架构、构建测试、风格、流程、环境
- 命中证据：
  - 架构：Monorepo 结构 ← heading:monorepo ；根目录 ← heading:目录
  - 构建测试：常用命令 ← heading:命令 ；根目录 ← body:```(?:bash|sh|shell)?\n[^`]{
  - 风格：Git 规范 ← heading:规范
  - 流程：[全文规则命中]（无标题/正文信号时的兜底通道）
  - 环境：依赖管理 ← heading:依赖 ；升级依赖 ← heading:依赖
- 文件标题（21）：Agent Instructions / 项目概览 / Monorepo 结构 / 常用命令 / 根目录 / Web 应用 (`@md/web`) / VSCode 扩展 / CLI & MCP / 架构 / 渲染管线 / 构建系统 / 样式与主题 / 状态管理 / 国际化（i18n，`@md/web`）
- 原文摘录（前 600 字）：

```text
# Agent Instructions
本文件为 AI Agent（Claude Code、OpenCode、Cursor、Copilot 等）在本仓库中工作时提供统一入口。
## 项目概览
**doocs/md** — 一款微信 Markdown 编辑器，将 Markdown 渲染为微信公众号文章格式。支持自定义主题样式、多图床、AI 助手、浏览器扩展、**zh-CN / zh-TW / en-US / ja-JP 界面**等特性。
- **在线地址:** https://md.doocs.org
- **Node 版本:** >= 22.22.2（`.nvmrc`: v22.22.2）
- **包管理器:** pnpm（monorepo）
- **npm 镜像:** https://registry.npmmirror.com（`.npmrc`）
## Monorepo 结构
| 工作区           | 路径                  | 说明                                                                                                  |
| ---------------- | --------------------- | ----------------
```
- 完整原文：`data/raw/full/doocs__md.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 13. `huiliyi37/Tianshu-harness` — AGENTS.md
- 语言 zh ｜ 14896 字节 ｜ 14 章节 ｜ 553 ★ ｜ 许可 Apache-2.0
- **规则分配**：概览、架构、构建测试、AI行为
- 命中证据：
  - 概览：项目定位 ← heading:项目定位
  - 架构：天枢 (Tiānshū) — Architecture Map ← heading:architecture ；顶层目录 ← heading:目录
  - 构建测试：Runtime Data Layout（排查必读） ← heading:run ；高危命令纪律（硬性闸门） ← heading:命令
  - AI行为：[全文规则命中]（无标题/正文信号时的兜底通道）
- 文件标题（14）：天枢 (Tiānshū) — Architecture Map / 项目定位 / 能力全景（按需查阅） / 内置星域（12） / 提供商（预设） / 顶层目录 / Runtime Data Layout（排查必读） / 缓存排查指南 / 缓存数据在哪 / 排查步骤 / 相关源码 / 高危命令纪律（硬性闸门） / Agent 安全保护（硬性闸门） / 通用执行纪律
- 原文摘录（前 600 字）：

```text
# 天枢 (Tiānshū) — Architecture Map
> v2.19.6 · 顶层目录索引 + 能力全景。文件级细节用工具 `repo_map` / `repo_graph` 按需获取。
> 更深架构说明见 [`docs/architecture-overview.md`](docs/architecture-overview.md)；星图叙事见 [`star.md`](star.md)。
## 项目定位
天枢是全功能终端编程智能体运行时（CLI 命令仍为 `rivet`）。三大支柱：
1. **认知虚拟机 (CVM)** — `RuntimeHookPipeline` 五阶段条件装配 60+ hook 模块，拦截服从性漂移 / doom loop / 验证债务
2. **前缀缓存工程** — 冻结 system prompt + 字节稳定 appendix + 边界压缩，DeepSeek V4 长会话稳态命中率 95–99%
3. **星域 + 多模型编排** — 12 星域认知纪律；worker / council / team / plan 分波执行
表面入口：`src/main.ts` → `bootstrap` → `AgentLoop`；桌面端 `desktop/`（Tauri + React）经 `src/server/` sidecar HTTP/SS
```
- 完整原文：`data/raw/full/huiliyi37__Tianshu-harness.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 14. `labring/FastGPT` — AGENTS.md
- 语言 zh ｜ 9923 字节 ｜ 25 章节 ｜ 29627 ★ ｜ 许可 NOASSERTION
- **规则分配**：概览、架构、构建测试、风格、流程、环境、禁令、坑
- 命中证据：
  - 概览：项目概述 ← heading:概述
  - 架构：架构 ← heading:架构 ；关键目录 ← heading:目录
  - 构建测试：开发命令 ← heading:命令 ；测试 ← heading:测试
  - 风格：代码规范 ← heading:规范 ；输出规范 ← heading:规范
  - 流程：工作流程 ← heading:流程
  - 环境：环境配置 ← heading:环境
  - 禁令：运行要求 ← heading:要求
  - 坑：开发注意事项 ← heading:注意
- 文件标题（25）：AGENTS.md / 项目概述 / 设计文档 / 架构 / Packages (库代码) / Projects (应用程序) / 关键目录 / 开发命令 / 测试 / 代码组织模式 / Monorepo 结构 / API 结构 / 前端架构 / 开发注意事项
- 原文摘录（前 600 字）：

```text
# AGENTS.md
本文件为 Agent 在本仓库中工作时提供指导说明。
## 项目概述
FastGPT 是一个 AI Agent 构建平台,通过 Flow 提供开箱即用的数据处理、模型调用能力和可视化工作流编排。这是一个基于 NextJS 构建的全栈 TypeScript 应用,后端使用 MongoDB/PostgreSQL。
**技术栈**: NextJS + TypeScript + ChakraUI + MongoDB + VectorDB(PG, Milvus, Zilliz, OceanBase, SeekDB, OpenGauss......)
## 设计文档
你可以参考 [项目设计文档](./.agents/design/) 来了解 FastGPT 已有的设计方案。
## 架构
这是一个使用 pnpm workspaces 的 monorepo,主要结构如下:
### Packages (库代码)
- `packages/global/` - 所有项目共享的类型、常量、工具函数
- `packages/service/` - 后端服务、数据库模型、API 控制器、工作流引擎
- `packages/web/` - 共享的前端组件、hooks、样式、国际化
### Projects (应用程序)
- `projects/app/` - 主 NextJS Web 应
```
- 完整原文：`data/raw/full/labring__FastGPT.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 15. `liaohch3/claude-tap` — AGENTS.md
- 语言 zh ｜ 6542 字节 ｜ 15 章节 ｜ 3189 ★ ｜ 许可 MIT
- **规则分配**：架构、构建测试、流程、禁令
- 命中证据：
  - 架构：标准目录 ← heading:目录 ；Skills 目录 ← heading:目录
  - 构建测试：测试 ← heading:测试 ；Scripts 目录 ← heading:script
  - 流程：Review Guidelines ← heading:review ；Pre-commit Hook ← heading:commit
  - 禁令：Documentation Boundary ← heading:boundar ；不可协商规则 ← heading:规则
- 文件标题（15）：Maintainer Automation Notes / AGENTS 索引 / Documentation Boundary / Review Guidelines / Pre-commit Hook / 不可协商规则 / 标准目录 / Skills 目录 / 测试 / 验证 / 翻译 / 发布 / 资产生成 / Scripts 目录
- 原文摘录（前 600 字）：

```text
# Maintainer Automation Notes
External contributors should start with [`CONTRIBUTING.md`](CONTRIBUTING.md). This file documents stricter maintainer and agent automation workflows; it is not the public contribution checklist.
# AGENTS 索引
本文件是贡献者规则的入口。详细策略文本位于 `.agents/docs/standards/*.md`。
## Documentation Boundary
`docs/` is reserved for public project documentation and README assets that open-source users or external contributors read directly. Maintainer policies, implementation plans, learning records, PR evidence, and recording assets belong under `.agents/` so internal automation workflow
```
- 完整原文：`data/raw/full/liaohch3__claude-tap.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 16. `op7418/CodePilot` — AGENTS.md
- 语言 zh ｜ 7163 字节 ｜ 8 章节 ｜ 6452 ★ ｜ 许可 NOASSERTION
- **规则分配**：概览、构建测试、流程、禁令、AI行为
- 命中证据：
  - 概览：项目协作定位 ← heading:定位
  - 构建测试：自检命令（Codex 跑测试时） ← heading:测试
  - 流程：Codex review 规则 ← heading:review
  - 禁令：共享规则入口（不在此重复，按需读） ← heading:规则 ；Codex review 规则 ← heading:规则
  - AI行为：项目协作定位 ← heading:协作
- 文件标题（8）：AGENTS.md / 项目协作定位 / 共享规则入口（不在此重复，按需读） / 自检命令（Codex 跑测试时） / Codex review 规则 / 修复闭环 / Codex 发版执行边界 / 文档
- 原文摘录（前 600 字）：

```text
# AGENTS.md
CodePilot — Codex 的桌面 GUI 客户端，基于 Electron + Next.js。
> 架构细节见 [ARCHITECTURE.md](./ARCHITECTURE.md)。
> 本文件只放 **Codex 专属边界 + 指向共享规则的入口**；开发规则、测试、发版等与 Claude Code 共享的细则不在此重复，统一见 [CLAUDE.md](./CLAUDE.md) 与 [docs/rules/](./docs/rules/README.md)，避免两份漂移。
## 项目协作定位
由用户、Codex、Claude Code 三方协作：
- **用户** — 产品目标、真实使用反馈、验收标准、最终取舍。
- **Claude Code** — 主要代码实现、修复落地、工程改动提交。
- **Codex** — 默认负责审查测试、复现分析、执行计划和文档化；用户在当前任务中明确要求 Codex 实现时，也可以承担该请求范围内的代码改动。
**Codex 角色边界（临时规则，2026-07-21 起，用户撤销前有效）：**
- 默认边界：除非用户在当前任务中明确要求 Codex “实现 / 修改 / 修复 / 构建”代码，否则 Codex 不修改产品代码、运行时代码、构建脚本、数据库 schema、样式或业务逻辑；计划、审查、诊断、解释
```
- 完整原文：`data/raw/full/op7418__CodePilot.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 17. `rullerzhou-afk/clawd-on-desk` — AGENTS.md
- 语言 zh ｜ 41400 字节 ｜ 10 章节 ｜ 6192 ★ ｜ 许可 AGPL-3.0
- **规则分配**：概览、构建测试、禁令、坑
- 命中证据：
  - 概览：Project Overview ← heading:overview
  - 构建测试：Common Commands ← heading:command, body:```(?:bash|sh|shell)?\n[^`]{ ；Runtime Summary ← heading:run
  - 禁令：Constraints ← heading:constraint ；Do Not Revisit ← heading:do not
  - 坑：High-Risk Gotchas ← heading:gotcha
- 文件标题（10）：AGENTS.md / Project Overview / Common Commands / Read These Docs / Runtime Summary / Core Files / Constraints / Testing / High-Risk Gotchas / Do Not Revisit
- 原文摘录（前 600 字）：

```text
# AGENTS.md
This file is the entry point for coding agents working in this repository. Keep it short and operational. Durable architecture lives in `docs/project/`; `docs/plans/` and `docs/investigations/` are historical evidence unless this file explicitly links them.
## Project Overview
Clawd 是一个 Electron 桌宠：通过 hook、日志轮询、plugin 和 extension 感知 AI coding agent 的工作状态，并播放像素风动画。当前支持 Claude Code、Codex CLI、Copilot CLI、Gemini CLI、Antigravity CLI (agy)、Cursor Agent、CodeBuddy、WorkBuddy、Kiro CLI、Kimi Code CLI (Kimi-CLI)、Qwen Code、ZCode、CodeWhale、opencode、MiMo Code、Pi、OpenClaw、Hermes Agent、Qoder、QoderWo
```
- 完整原文：`data/raw/full/rullerzhou-afk__clawd-on-desk.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 18. `xerrors/Yuxi` — AGENTS.md
- 语言 zh ｜ 7324 字节 ｜ 7 章节 ｜ 6902 ★ ｜ 许可 MIT
- **规则分配**：构建测试、风格、流程、禁令
- 命中证据：
  - 构建测试：证据规则 ← body:```(?:bash|sh|shell)?\n[^`]{
  - 风格：Yuxi Agent 开发约定 ← heading:约定
  - 流程：Review 与交付 ← heading:review
  - 禁令：证据规则 ← heading:规则
- 文件标题（7）：Yuxi Agent 开发约定 / 每次任务先加载什么 / 任务与决策 / 不能破坏的系统事实 / 证据规则 / 实现与安全 / Review 与交付
- 原文摘录（前 600 字）：

```text
# Yuxi Agent 开发约定
Yuxi 是基于 LangGraph、FastAPI、Vue 和多种持久化服务构建的知识库与多智能体平台。Docker Compose 是开发拓扑的事实来源；修改不熟悉的模块前先阅读 [ARCHITECTURE.md](ARCHITECTURE.md)，再用符号搜索确认真实实现。
## 每次任务先加载什么
- [ARCHITECTURE.md](ARCHITECTURE.md)：稳定边界、主链路和架构不变量。
- [Yuxi Spec Loop](docs/develop-guides/spec-loop.md)：非平凡变更从提案、证据到收敛的流程。
- [工程信任系统](docs/develop-guides/engineering-trust.md)：语义 Owner、证据、决策记录、派生审计和 gate 规则。
- [测试规范](docs/develop-guides/testing-guidelines.md)：unit、integration、E2E 的职责与命令。
- [贡献指南](docs/develop-guides/contributing.md)：分支、独立 Review、commit 和 PR 流程。
- [并行工作树与隔离运行环境](docs/develop-guides/parallel-worktree-environ
```
- 完整原文：`data/raw/full/xerrors__Yuxi.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 19. `aotemiao/artemis` — AGENTS.md
- 语言 zh ｜ 5843 字节 ｜ 8 章节 ｜ 27 ★ ｜ 许可 nan
- **规则分配**：禁令
- 命中证据：
  - 禁令：产出要求 ← heading:要求
- 文件标题（8）：Artemis Agent Guide / 先看什么 / 标准工作回路 / 如何选择上下文 / 何时使用执行计划与 OpenSpec 变更 / 改动守则 / 优先使用的脚本 / 产出要求
- 原文摘录（前 600 字）：

```text
# Artemis Agent Guide
Status: maintained
Last Reviewed: 2026-06-10
Review Cadence: 90 days
本文件是仓库内 agent 的稳定入口，目标是让任何自动化执行都先找到地图，再开始改代码。
## 先看什么
1. `README.md`
   了解仓库模块、启动方式与基础设施依赖。
2. `ARCHITECTURE.md`
   了解微服务拓扑、DDD/COLA 分层、模块边界与常见变更入口。
3. `QUALITY_SCORE.md`
   了解当前质量短板、优先修复方向与“不要继续放大的问题”。
4. `docs/governance/CHECKLIST.md`
   了解本仓库对 Harness Engineering 的落地清单与当前完成度。
5. `docs/agent-workflow/AGENT_DEVELOPMENT_WORKFLOW.md`
   了解默认的 agent 开发分流方式。
6. `docs/feature-specs/README.md`
   了解业务需求级 Spec、验收标准和验证映射。
7. `openspec/specs/`
   读取与本次改动直接相关的规范，避免只看局部代码做出越界修改。
## 标准工作回路
1. 先读相关规范与架构文档，再决定改动范围。
2
```
- 完整原文：`data/raw/full/aotemiao__artemis.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 20. `fancy1108/Clutch` — AGENTS.md
- 语言 zh ｜ 5452 字节 ｜ 2 章节 ｜ 89 ★ ｜ 许可 MIT
- **规则分配**：构建测试、AI行为
- 命中证据：
  - 构建测试：[强模式命中]（全文正则，跨语言通道）
  - AI行为：[强模式命中]（全文正则，跨语言通道）
- 文件标题（2）：Clutch — Agent Instructions Index / Cursor Cloud specific instructions
- 原文摘录（前 600 字）：

```text
# Clutch — Agent Instructions Index
跨 AI 工具的统一入口。**不要在本文件维护实质规则**——规则只写在权威源里，本文件只做指针。
| 文档 | 读者 | 用途 |
|------|------|------|
| [`CLAUDE.md`](./CLAUDE.md) | Claude Code、全体开发者 | **唯一权威**：铁律、命令、Check-in、日志规范 |
| [`memory/PROGRESS.md`](./memory/PROGRESS.md) | 所有 Agent | 跨会话进度接力棒 |
| [`memory/DELIVERABLES.md`](./memory/DELIVERABLES.md) | 所有 Agent | Task 交付索引（Commit / Verification / 证据） |
| [`memory/DECISIONS.md`](./memory/DECISIONS.md) | 所有 Agent | 最新决策与开放问题（唯一汇总地） |
| [`memory/FILEMAP.md`](./memory/FILEMAP.md) | 所有 Agent | 文件路径速查（Check-in 用） |
| [`specs/core/proposal.md`](./specs/core/proposal.md
```
- 完整原文：`data/raw/full/fancy1108__Clutch.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 21. `gqcn/agentbox` — AGENTS.md
- 语言 zh ｜ 14110 字节 ｜ 29 章节 ｜ 0 ★ ｜ 许可 Apache-2.0
- **规则分配**：概览、架构、构建测试、风格、流程、环境、禁令
- 命中证据：
  - 概览：项目定位统一要求 ← heading:项目定位
  - 架构：架构设计规范 ← heading:架构 ；模块设计规范 ← heading:模块
  - 构建测试：单元测试规范 ← heading:测试 ；E2E测试规范 ← heading:测试
  - 风格：顶级规范要求 ← heading:规范 ；插件本地规范优先级 ← heading:规范
  - 流程：开发流程规范 ← heading:流程 ；Go代码生成流程 ← heading:流程
  - 环境：开发工具与脚本规范 ← heading:工具
  - 禁令：顶级规范要求 ← heading:要求 ；项目定位统一要求 ← heading:要求
- 文件标题（28）：顶级规范要求 / 项目定位统一要求 / 核心宿主边界要求 / 接口性能统一要求 / 外部规则文件 / 插件本地规范优先级 / 通用强制规则 / 文档编写规范 / 开发流程规范 / 架构设计规范 / 模块设计规范 / 数据权限接入规范 / 插件目录结构规范 / 接口设计规范
- 原文摘录（前 600 字）：

```text
当前项目是全新项目，没有任何历史负担，在设计方案和实现时不用考虑任何的兼容性。
# 顶级规范要求
## 项目定位统一要求
1. 项目统一定位是`面向可持续交付的 AI 原生全栈框架`。所有功能设计都必须围绕这一定位展开。
2. 默认管理工作台、系统管理模块、用户权限模块等能力属于`LinaPro`提供的默认入口和内建通用能力，不构成项目的唯一产品边界。
## 核心宿主边界要求
1. `apps/lina-core`是全栈开发框架的核心宿主服务，负责提供通用模块接口能力、组件能力、系统治理能力与插件扩展能力。该服务的设计必须优先保证通用性、稳定性和可复用性，不得与具体管理工作台页面的展示结构、交互细节或前端框架实现强绑定。
2. 若需求仅来源于表格列、筛选项、树选择器、路由装配、工作台聚合、下拉选项等工作台展示变化，应优先通过工作台适配接口或前端适配层解决，而不是直接修改`lina-core`的核心领域契约、通用`service`语义或存储模型。
3. 在开发`apps/lina-plugins/<plugin-id>/`业务插件时，业务插件的相关前后端功能逻辑应当严格闭环在其内部实现，避免直接修改`lina-core`目录下的内容。确实需要修改时，必须评估是否会对其他插件或未来插件开发造成影响，并在实施前进行充分的设计和审查，并在用户授权的情况下进行修改。
4. 主框架插件能力发生
```
- 完整原文：`data/raw/full/gqcn__agentbox.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 22. `ijry/uview-plus` — AGENTS.md
- 语言 zh ｜ 612 字节 ｜ 4 章节 ｜ 716 ★ ｜ 许可 MIT
- **规则分配**：概览、风格、流程
- 命中证据：
  - 概览：uview-plus说明 ← heading:说明
  - 风格：提交规范 ← heading:规范 ；发布规范 ← heading:规范
  - 流程：提交规范 ← heading:提交 ；发布规范 ← heading:发布
- 文件标题（4）：AGENTS / 提交规范 / 发布规范 / uview-plus说明
- 原文摘录（前 600 字）：

```text
# AGENTS
## 提交规范
- `git commit` 信息必须使用中文。
- 提交信息必须包含 `head + body` 两部分，不允许只有单行标题。
- `head` 使用一句话概述本次改动；`body` 说明改动目的、范围或影响。
## 发布规范
- 发布新版本时，必须参考 `D:\Repos\xyito\config\ultraUI.md` 执行。
- 按该文档中的命令与流程处理 `uview-plus` / `uview-ultra` 的版本 bump、notes、发布与后续提交推送。
## uview-plus说明
uview-plus一般是指的同一个父目录下的uview-plus文件夹项目， 文档一般也在同一父目录下。
```
- 完整原文：`data/raw/full/ijry__uview-plus.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 23. `linaproai/linapro` — AGENTS.md
- 语言 zh ｜ 14356 字节 ｜ 29 章节 ｜ 153 ★ ｜ 许可 Apache-2.0
- **规则分配**：概览、架构、构建测试、风格、流程、环境、禁令
- 命中证据：
  - 概览：项目定位统一要求 ← heading:项目定位
  - 架构：架构设计规范 ← heading:架构 ；模块设计规范 ← heading:模块
  - 构建测试：单元测试规范 ← heading:测试 ；E2E测试规范 ← heading:测试
  - 风格：顶级规范要求 ← heading:规范 ；插件本地规范优先级 ← heading:规范
  - 流程：开发流程规范 ← heading:流程 ；Go代码生成流程 ← heading:流程
  - 环境：开发工具与脚本规范 ← heading:工具
  - 禁令：顶级规范要求 ← heading:要求 ；项目定位统一要求 ← heading:要求
- 文件标题（28）：顶级规范要求 / 项目定位统一要求 / 核心宿主边界要求 / 接口性能统一要求 / 外部规则文件 / 插件本地规范优先级 / 通用强制规则 / 文档编写规范 / 开发流程规范 / 架构设计规范 / 模块设计规范 / 数据权限接入规范 / 插件目录结构规范 / 接口设计规范
- 原文摘录（前 600 字）：

```text
当前项目是全新项目，没有任何历史负担，在设计方案和实现时不用考虑任何的兼容性。
# 顶级规范要求
## 项目定位统一要求
1. 项目统一定位是`面向可持续交付的 AI 原生全栈框架`。所有功能设计都必须围绕这一定位展开。
2. 默认管理工作台、系统管理模块、用户权限模块等能力属于`LinaPro`提供的默认入口和内建通用能力，不构成项目的唯一产品边界。
## 核心宿主边界要求
1. 若项目根目录下不存在`.contributing`文件，那么在项目开发中，尤其是业务插件开发中，严格禁止修改框架相关的文件内容，例如`apps/lina-core/`、`apps/lina-vben/`、`hack/`目录下的文件内容。
2. `apps/lina-core`是全栈开发框架的核心宿主服务，负责提供通用模块接口能力、组件能力、系统治理能力与插件扩展能力。该服务的设计必须优先保证通用性、稳定性和可复用性，不得与具体管理工作台页面的展示结构、交互细节或前端框架实现强绑定。
3. 若需求仅来源于表格列、筛选项、树选择器、路由装配、工作台聚合、下拉选项等工作台展示变化，应优先通过工作台适配接口或前端适配层解决，而不是直接修改`lina-core`的核心领域契约、通用`service`语义或存储模型。
4. 在开发`apps/lina-plugins/<plugin-id>/`业务插件时，业务插件的
```
- 完整原文：`data/raw/full/linaproai__linapro.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 24. `linaproai/linapro-site` — AGENTS.md
- 语言 zh ｜ 16873 字节 ｜ 30 章节 ｜ 1 ★ ｜ 许可 Apache-2.0
- **规则分配**：概览、架构、构建测试、风格、流程、环境、禁令
- 命中证据：
  - 概览：项目定位统一要求 ← heading:项目定位
  - 架构：官网站点结构要求 ← heading:结构 ；架构设计规范 ← heading:架构
  - 构建测试：单元测试规范 ← heading:测试 ；E2E测试规范 ← heading:测试
  - 风格：顶级规范要求 ← heading:规范 ；插件本地规范优先级 ← heading:规范
  - 流程：开发流程规范 ← heading:流程 ；Go代码生成流程 ← heading:流程
  - 环境：开发工具与脚本规范 ← heading:工具
  - 禁令：顶级规范要求 ← heading:要求 ；项目定位统一要求 ← heading:要求
- 文件标题（30）：仓库用途 / 顶级规范要求 / 项目定位统一要求 / 核心宿主边界要求 / 官网站点结构要求 / 接口性能统一要求 / 外部规则文件 / 插件本地规范优先级 / 通用强制规则 / 文档编写规范 / 开发流程规范 / 架构设计规范 / 模块设计规范 / 数据权限接入规范
- 原文摘录（前 600 字）：

```text
# 仓库用途
本仓库承载`LinaPro`主框架源码、官方网站和配套`OpenSpec`治理文件。`LinaPro`的项目定位是面向可持续交付的 AI 原生全栈框架；官网源码位于`apps/lina-site/`，框架核心宿主位于`apps/lina-core/`，管理工作台位于`apps/lina-vben/`，插件源码工作区位于`apps/lina-plugins/`。
官网发布地址为`https://linapro.ai/`，项目源仓库地址为`https://github.com/linaproai/linapro`。历史上的独立官网仓库内容已经合入当前工作区，后续编写官网文案时应优先依据同仓库内`apps/lina-core/`、`apps/lina-vben/`、`apps/lina-plugins/`和`openspec/`中的事实内容。
`AGENTS.md`是项目顶层规范入口；`Claude Code`、`Codex`以及其他`AI Coding Agent`读取本文件时应共享同一套说明。
# 顶级规范要求
## 项目定位统一要求
1. 项目统一定位是`面向可持续交付的 AI 原生全栈框架`。所有功能设计都必须围绕这一定位展开。
2. 默认管理工作台、系统管理模块、用户权限模块等能力属于`LinaPro`提供的默认入口和内建通用能力，不构成项目的唯一产品边界。
3.
```
- 完整原文：`data/raw/full/linaproai__linapro-site.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 25. `nzh63/syc` — AGENTS.md
- 语言 zh ｜ 6881 字节 ｜ 10 章节 ｜ 43 ★ ｜ 许可 GPL-3.0
- **规则分配**：架构、构建测试、风格、环境、禁令
- 命中证据：
  - 架构：仓库结构 ← heading:结构 ；架构要点（改动前必读） ← heading:架构
  - 构建测试：AGENTS.md — syc 编译器项目指南 ← heading:编译 ；构建 ← heading:构建
  - 风格：代码风格 ← heading:风格
  - 环境：（首次配置时 CMake 自动下载 WinFlexBison；tools/get-winflexbison.sh ← heading:tool
  - 禁令：已知文法/语义限制（不是 bug，测试时绕开） ← heading:限制
- 文件标题（10）：AGENTS.md — syc 编译器项目指南 / 仓库结构 / 构建 / （首次配置时 CMake 自动下载 WinFlexBison；tools/get-winflexbison.sh / 为手动兜底） / 测试 / 架构要点（改动前必读） / 已知文法/语义限制（不是 bug，测试时绕开） / 新增语言特性的常规路径 / 代码风格
- 原文摘录（前 600 字）：

```text
# AGENTS.md — syc 编译器项目指南
本文件为 AI 编码代理（以及新加入的开发者）提供本仓库的构建、测试与架构要点。
syc 是一个教学用 **SysY → ARMv7a** 编译器，自带 ARMv7 汇编解释器 `armsim`
作为测试用例的执行器。
## 仓库结构
```
src/
  parser/        tokens.l + parser.y（WinFlexBison）→ AST
  ast/           AST 节点（node.h/.cpp）、类型系统（type.h）
  ast/generate/  AST → IR（eval.cpp、generate_ir.cpp、convert.cpp、context）
  ir/            IR 定义（ir.h/.cpp：OpCode、OpName、IR::ty）
  ir/generate/   IR 生成
  ir/optimize/   DCE、ICM、phi 重命名等 pass（optimize.cpp 是总入口）
  assembly/      IR → ARMv7 汇编（generate/generate.cpp 指令选择、
                 generate/context.cpp 线性扫描寄存器分配）
armsim/          ARMv7 汇编
```
- 完整原文：`data/raw/full/nzh63__syc.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 26. `sankichi92/LiveLog` — AGENTS.md
- 语言 zh ｜ 2303 字节 ｜ 8 章节 ｜ 9 ★ ｜ 许可 MIT
- **规则分配**：构建测试
- 命中证据：
  - 构建测试：[强模式命中]（全文正则，跨语言通道）
- 文件标题（8）：LiveLog AGENTS.md / 目的 / 最初の 10 分 / AI エージェント向け検証コマンド / ローカルミドルウェアの承認方針 / 必須チェック（変更内容に応じて実行） / ガードレール / 詳細資料
- 原文摘录（前 600 字）：

```text
# LiveLog AGENTS.md
このファイルは、Codex を含むコーディング AI エージェント向けの最小ガイドです。  
詳細は `docs/project-map.md` と `docs/infrastructure-inventory.md` を参照してください。
## 目的
- このリポジトリで安全かつ一貫した変更を行う。
- 変更前に対象範囲と依存を把握し、不要な副作用を避ける。
- 本番環境や外部サービスの変更は、必ず人手承認を得る。
## 最初の 10 分
1. ミドルウェア起動: `docker compose up -d postgres elasticsearch`
2. セットアップ: `bin/setup --skip-server`
3. 開発サーバー起動: `bin/dev`
4. Ruby テスト: `bin/rails spec`
5. Ruby Lint: `bin/rubocop`
6. JavaScript/TypeScript Lint: `yarn run lint`
## AI エージェント向け検証コマンド
1. ミドルウェア起動: `docker compose up -d postgres elasticsearch selenium`
2. セットアップ: `bin/setup --skip-server`
3. テスト:
```
- 完整原文：`data/raw/full/sankichi92__LiveLog.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：


## 边界件（10 份，单独报告）

### 1. `MemPalace/mempalace` — AGENTS.md
- 语言 en ｜ 9 字节 ｜ 1 章节 ｜ 58977 ★ ｜ 许可 MIT
- **规则分配**：（无）
- 命中证据：
- 文件标题（0）：（无标题）
- 原文摘录（前 600 字）：

```text
CLAUDE.md
```
- 完整原文：`data/raw/full/MemPalace__mempalace.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 2. `agno-agi/agno` — AGENTS.md
- 语言 en ｜ 9 字节 ｜ 1 章节 ｜ 42128 ★ ｜ 许可 Apache-2.0
- **规则分配**：（无）
- 命中证据：
- 文件标题（0）：（无标题）
- 原文摘录（前 600 字）：

```text
CLAUDE.md
```
- 完整原文：`data/raw/full/agno-agi__agno.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 3. `e2b-dev/E2B` — AGENTS.md
- 语言 en ｜ 9 字节 ｜ 1 章节 ｜ 13738 ★ ｜ 许可 Apache-2.0
- **规则分配**：（无）
- 命中证据：
- 文件标题（0）：（无标题）
- 原文摘录（前 600 字）：

```text
CLAUDE.md
```
- 完整原文：`data/raw/full/e2b-dev__E2B.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 4. `huggingface/peft` — AGENTS.md
- 语言 en ｜ 13 字节 ｜ 1 章节 ｜ 21648 ★ ｜ 许可 Apache-2.0
- **规则分配**：（无）
- 命中证据：
- 文件标题（0）：（无标题）
- 原文摘录（前 600 字）：

```text
.ai/AGENTS.md
```
- 完整原文：`data/raw/full/huggingface__peft.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 5. `max-sixty/worktrunk` — AGENTS.md
- 语言 en ｜ 9 字节 ｜ 1 章节 ｜ 6924 ★ ｜ 许可 NOASSERTION
- **规则分配**：（无）
- 命中证据：
- 文件标题（0）：（无标题）
- 原文摘录（前 600 字）：

```text
CLAUDE.md
```
- 完整原文：`data/raw/full/max-sixty__worktrunk.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 6. `onnx/onnx` — AGENTS.md
- 语言 en ｜ 9 字节 ｜ 1 章节 ｜ 21465 ★ ｜ 许可 Apache-2.0
- **规则分配**：（无）
- 命中证据：
- 文件标题（0）：（无标题）
- 原文摘录（前 600 字）：

```text
CLAUDE.md
```
- 完整原文：`data/raw/full/onnx__onnx.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 7. `yamadashy/repomix` — AGENTS.md
- 语言 en ｜ 21 字节 ｜ 1 章节 ｜ 28278 ★ ｜ 许可 MIT
- **规则分配**：（无）
- 命中证据：
- 文件标题（0）：（无标题）
- 原文摘录（前 600 字）：

```text
.agents/rules/base.md
```
- 完整原文：`data/raw/full/yamadashy__repomix.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 8. `Miyamura80/DaySurface` — AGENTS.md
- 语言 en ｜ 9 字节 ｜ 1 章节 ｜ 2 ★ ｜ 许可 MIT
- **规则分配**：（无）
- 命中证据：
- 文件标题（0）：（无标题）
- 原文摘录（前 600 字）：

```text
CLAUDE.md
```
- 完整原文：`data/raw/full/Miyamura80__DaySurface.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 9. `Miyamura80/MCP-Template` — AGENTS.md
- 语言 en ｜ 9 字节 ｜ 1 章节 ｜ 0 ★ ｜ 许可 MIT
- **规则分配**：（无）
- 命中证据：
- 文件标题（0）：（无标题）
- 原文摘录（前 600 字）：

```text
CLAUDE.md
```
- 完整原文：`data/raw/full/Miyamura80__MCP-Template.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 10. `supabase/pg-toolbelt` — AGENTS.md
- 语言 en ｜ 29 字节 ｜ 1 章节 ｜ 70 ★ ｜ 许可 nan
- **规则分配**：（无）
- 命中证据：
- 文件标题（0）：（无标题）
- 原文摘录（前 600 字）：

```text
.github/agents/pg-toolbelt.md
```
- 完整原文：`data/raw/full/supabase__pg-toolbelt.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：


## 稀有类加成（9 份，单独报告）

### 1. `HKUDS/LightRAG` — AGENTS.md
- 语言 en ｜ 25237 字节 ｜ 54 章节 ｜ 39541 ★ ｜ 许可 MIT
- **规则分配**：概览、架构、构建测试、风格、流程、环境、禁令、坑
- 命中证据：
  - 概览：Project Overview ← heading:overview
  - 架构：Project Structure ← heading:structure ；Module Layout (`lightrag/`) ← heading:layout
  - 构建测试：Development Commands ← heading:command ；Build WebUI ← heading:build
  - 风格：Key Implementation Patterns ← heading:pattern ；Code Style ← heading:style
  - 流程：Commit and Pull Request Guidance ← heading:pull request
  - 环境：Setup ← heading:setup ；Install with uv ← heading:install
  - 禁令：[全文规则命中]（无标题/正文信号时的兜底通道）
  - 坑：Custom Embedding Functions ← body:\bpitfall\b
- 文件标题（54）：Repository Guidelines / Project Overview / Project Structure / Module Layout (`lightrag/`) / Core Architecture / LightRAG class composition / Storage Layer / Consistency without transactions / Pipeline concurrency contract / Purge recovery contract / Relation weight contract / Query Modes / Development Commands / Setup
- 原文摘录（前 600 字）：

```text
# Repository Guidelines
## Project Overview
LightRAG is a Retrieval-Augmented Generation (RAG) framework that uses graph-based knowledge representation for enhanced information retrieval. The system extracts entities and relationships from documents, builds a knowledge graph, and uses multiple retrieval modes (`local`, `global`, `hybrid`, `mix`, `naive`) for queries.
## Project Structure
Top-level directories:
- **lightrag/**: Core Python package — see *Module Layout* below.
- **lightrag_webui/**: React 19 + TypeScript client (Bun + Vite + Tailwind). UI components in `src/`.
- **scripts/**: `t
```
- 完整原文：`data/raw/full/HKUDS__LightRAG.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 2. `Human-Agent-Society/CORAL` — AGENTS.md
- 语言 en ｜ 6687 字节 ｜ 8 章节 ｜ 971 ★ ｜ 许可 Apache-2.0
- **规则分配**：流程、禁令、坑、AI行为
- 命中证据：
  - 流程：5. Disclosure in PR descriptions ← heading:pr  ；7. Reviewer guidance ← heading:review
  - 禁令：6. CORAL-specific gotchas for agents ← body:\bmust not\b
  - 坑：6. CORAL-specific gotchas for agents ← heading:gotcha
  - AI行为：AGENTS.md — AI-assisted contributions to CORAL ← body:\byou are\b
- 文件标题（8）：AGENTS.md — AI-assisted contributions to CORAL / 1. Human accountability / 2. Scope discipline / 3. Duplicate-work checks / 4. Keep agent guidance synchronized / 5. Disclosure in PR descriptions / 6. CORAL-specific gotchas for agents / 7. Reviewer guidance
- 原文摘录（前 600 字）：

```text
# AGENTS.md — AI-assisted contributions to CORAL
CORAL is itself infrastructure for running autonomous coding agents, so we
fully expect contributors to use Claude Code, Codex, Cursor, Kiro, OpenCode,
or other agents while developing changes. This document sets the ground rules.
These rules apply to **any** PR where an AI coding agent meaningfully wrote,
refactored, or generated code, tests, or docs. If you are not sure whether
your workflow counts, it does — read on.
> **TL;DR**
> - All PRs target the `dev` branch, never `main` (see CONTRIBUTING.md).
> - Release tags point to the promoted `de
```
- 完整原文：`data/raw/full/Human-Agent-Society__CORAL.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 3. `foss42/apidash` — AGENTS.md
- 语言 en ｜ 19016 字节 ｜ 28 章节 ｜ 2907 ★ ｜ 许可 Apache-2.0
- **规则分配**：架构、构建测试、风格、流程、环境、禁令、坑、AI行为
- 命中证据：
  - 架构：Repository Map ← heading:repository map ；Architecture Rules ← heading:architecture
  - 构建测试：Standard Commands ← heading:command ；Verification ← heading:verification
  - 风格：Standard Commands ← heading:standard
  - 流程：Agent Workflow ← heading:workflow ；Git and PR Workflow ← heading:workflow
  - 环境：Environment Variables ← heading:environment
  - 禁令：First Rules ← heading:rule ；Architecture Rules ← heading:rule
  - 坑：Common Pitfalls ← heading:pitfall
  - AI行为：API Dash Agent Guide ← body:\byou are\b ；Agent Workflow ← heading:agent workflow
- 文件标题（28）：API Dash Agent Guide / First Rules / Mainline Feature Baseline / Repository Map / Package Map / Standard Commands / Agent Workflow / Git and PR Workflow / Local Agent Notes and Subagent Logs / Agent Note: <topic> / Decisions / Change Log / Verification / Architecture Rules
- 原文摘录（前 600 字）：

```text
# API Dash Agent Guide
This is the operating guide for agents working in the API Dash repository. Treat it as a working contract, not a static note. If you change behavior, architecture, commands, generated models, storage, package boundaries, or test strategy, update this file and `architecture.md` in the same change.
Scope note: this guide describes the `main` branch architecture. If you are on a feature branch, verify whether the feature is merged before documenting it here. Do not describe branch-only work as mainline behavior.
## First Rules
1. Preserve user work. Check `git status --shor
```
- 完整原文：`data/raw/full/foss42__apidash.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 4. `superagent-ai/grok-cli` — AGENTS.md
- 语言 en ｜ 2106 字节 ｜ 6 章节 ｜ 3465 ★ ｜ 许可 MIT
- **规则分配**：概览、环境、坑
- 命中证据：
  - 概览：Overview ← heading:overview ；Quick reference ← heading:quick reference
  - 环境：Environment ← heading:environment
  - 坑：Known issues ← heading:known issue
- 文件标题（6）：AGENTS.md / Cursor Cloud specific instructions / Overview / Quick reference / Known issues / Environment
- 原文摘录（前 600 字）：

```text
# AGENTS.md
## Cursor Cloud specific instructions
### Overview
Grok CLI (`@vibe-kit/grok-cli`) is a single-package TypeScript CLI tool — no databases, Docker, or background services. See `README.md` for full documentation and usage.
### Quick reference
| Action        | Command                                                               |
| ------------- | --------------------------------------------------------------------- |
| Install deps  | `bun install` (installs Husky; pre-commit runs Biome on staged files) |
| Typecheck     | `bun run typecheck`                                        
```
- 完整原文：`data/raw/full/superagent-ai__grok-cli.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 5. `JCodesMore/ai-website-cloner-template` — AGENTS.md
- 语言 en ｜ 3389 字节 ｜ 10 章节 ｜ 34165 ★ ｜ 许可 MIT
- **规则分配**：概览、架构、构建测试、风格、AI行为
- 命中证据：
  - 概览：What This Is ← heading:what this is ；Tech Stack ← heading:tech stack
  - 架构：Project Structure ← heading:structure
  - 构建测试：Commands ← heading:command
  - 风格：Code Style ← heading:style
  - AI行为：MOST IMPORTANT NOTES ← body:\byou are\b
- 文件标题（9）：This is NOT the Next.js you know / Website Reverse-Engineer Template / What This Is / Tech Stack / Commands / Code Style / Design Principles / Project Structure / MOST IMPORTANT NOTES
- 原文摘录（前 600 字）：

```text
<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know
This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.
This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it
```
- 完整原文：`data/raw/full/JCodesMore__ai-website-cloner-template.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 6. `can1357/oh-my-pi` — AGENTS.md
- 语言 en ｜ 28777 字节 ｜ 25 章节 ｜ 30489 ★ ｜ 许可 MIT
- **规则分配**：概览、架构、构建测试、风格、流程、环境、禁令
- 命中证据：
  - 概览：Quick reference ← heading:quick reference
  - 架构：Package Structure ← heading:structure ；Node module imports ← heading:module
  - 构建测试：Commands ← heading:command ；Rust Build Profiles ← heading:build
  - 风格：Code Quality ← heading:code quality
  - 流程：Pull requests ← heading:pull request ；Changelog ← heading:changelog
  - 环境：Streaming tool previews ← heading:tool
  - 禁令：Development Rules ← heading:rule ；Logging and CLI Output ← body:\bmust not\b
- 文件标题（25）：Development Rules / Default Context / Package Structure / GitHub / Pull requests / Code Quality / Central Utilities / Bun Over Node / Quick reference / Process execution / Node module imports / File I/O / Streams / Misc
- 原文摘录（前 600 字）：

```text
# Development Rules
## Default Context
This repo contains multiple packages, but **`packages/coding-agent/`** is the primary focus. Unless otherwise specified, assume work refers to this package.
**Terminology**: When the user says "agent" or asks "why is agent doing X", they mean the **coding-agent package implementation**, not you (the assistant). The coding-agent is a CLI tool — questions about its behavior refer to code in `packages/coding-agent/`, not your current session.
### Package Structure
| Package                 | Description                                                        
```
- 完整原文：`data/raw/full/can1357__oh-my-pi.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 7. `lemonade-sdk/lemonade` — AGENTS.md
- 语言 en ｜ 19639 字节 ｜ 33 章节 ｜ 5678 ★ ｜ 许可 Apache-2.0
- **规则分配**：概览、架构、构建测试、风格、流程、环境、禁令、AI行为
- 命中证据：
  - 概览：Project Overview ← heading:overview
  - 架构：Architecture ← heading:architecture ；1. Setup (configures build directory and installs deps) ← heading:directory
  - 构建测试：Build Commands ← heading:build ；1. Setup (configures build directory and installs deps) ← heading:build
  - 风格：Code Style ← heading:style
  - 流程：Contributing ← heading:contributing
  - 环境：1. Setup (configures build directory and installs deps) ← heading:config ；5. Windows MSI installer (WiX 5.0+ required) ← heading:install
  - 禁令：Critical Invariants ← body:\bmust not\b
  - AI行为：Comments & Documentation ← body:\bbe concise\b
- 文件标题（33）：AGENTS.md / Project Overview / Architecture / Executables / Backend Abstraction / Router & Multi-Model Support / Model Manager & Recipe System / API Routes / Desktop & Web App / Key Dependencies / Build Commands / 1. Setup (configures build directory and installs deps) / 2. Build C++ server / 3. Tauri desktop app (optional, requires Node.js 20+ and Rust via rustup)
- 原文摘录（前 600 字）：

```text
# AGENTS.md
This file provides guidance to agent driven code reviews when working with this repository.
## Project Overview
Lemonade is a local LLM server providing GPU and NPU acceleration for running large language models on consumer hardware. It exposes OpenAI-compatible, Ollama-compatible, and Anthropic-compatible REST APIs, plus a WebSocket Realtime API. It supports multiple backends: llama.cpp, FastFlowLM, RyzenAI, whisper.cpp, stable-diffusion.cpp, Kokoro TTS, and Moonshine.
## Architecture
### Executables
- **lemond** — Pure HTTP server. Handles REST API, routes requests to backends, m
```
- 完整原文：`data/raw/full/lemonade-sdk__lemonade.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 8. `minmaxflow/mini-kode` — AGENTS.md
- 语言 en ｜ 2624 字节 ｜ 18 章节 ｜ 305 ★ ｜ 许可 MIT
- **规则分配**：概览、架构、构建测试、风格、流程
- 命中证据：
  - 概览：Technology Stack ← heading:technology stack
  - 架构：Architecture ← heading:architecture ；Key Modules ← heading:module
  - 构建测试：Development Commands ← heading:command, body:```(?:bash|sh|shell)?\n[^`]{ ；2. Publish to NPM (automatically runs build and test) ← heading:build
  - 风格：Code Style Guidelines ← heading:style ；Code Formatting ← heading:format
  - 流程：Release Process ← heading:release ；1. Update version number in package.json ← heading:version
- 文件标题（18）：Mini-Kode - Development Guide / Technology Stack / Architecture / Core Design / Key Modules / Development Commands / Code Style Guidelines / Import Organization / Code Formatting / Comment Language / Key Features / Release Process / Publishing to NPM / 1. Update version number in package.json
- 原文摘录（前 600 字）：

```text
# Mini-Kode - Development Guide
## Technology Stack
- **TypeScript** - Static typing
- **pnpm** - Package management
- **Bun** - Runtime and build tool
- **Ink** - CLI UI with React
- **OpenAI SDK** - LLM integration
- **Vitest** - Testing framework
- **Zod** - Runtime type validation
- **Commander** - CLI framework
## Architecture
### Core Design
- **Tool Pattern**: All tools implement `Tool<Input, Output>` interface
- **Concurrency**: Readonly tools execute concurrently, writing tools sequentially
- **Streaming**: LLM responses stream progressively
- **Permissions**: Project-level + Session-
```
- 完整原文：`data/raw/full/minmaxflow__mini-kode.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：

### 9. `bekker/qvopenapi-rs` — AGENTS.md
- 语言 en ｜ 3596 字节 ｜ 7 章节 ｜ 21 ★ ｜ 许可 nan
- **规则分配**：概览、架构、风格、禁令
- 命中证据：
  - 概览：Project Overview ← heading:overview
  - 架构：Repository Structure ← heading:structure
  - 风格：Coding Conventions ← heading:convention
  - 禁令：Critical Constraints ← heading:constraint
- 文件标题（7）：AGENTS.md — Instructions for AI Coding Agents / ⚠️ MANDATORY: Read Before Any Code Operation / Project Overview / Repository Structure / Critical Constraints / Coding Conventions / When Making Changes
- 原文摘录（前 600 字）：

```text
# AGENTS.md — Instructions for AI Coding Agents
## ⚠️ MANDATORY: Read Before Any Code Operation
**Before reading or modifying any source code in this repository, you MUST first read [`ARCHITECTURE.md`](ARCHITECTURE.md) in its entirety.**
This project has non-obvious architectural constraints (Win32 message loops, DLL FFI, cross-compilation to i686, CP949 encoding, Wine compatibility) that make it easy to introduce subtle bugs if you lack context. The architecture document provides:
- Crate dependency graph and layering
- Per-file descriptions of every source file
- Threading model and message 
```
- 完整原文：`data/raw/full/bekker__qvopenapi-rs.md`（本页只摘前 600 字）
- 判定：□ 准确　□ 漏标（缺哪类：______）　□ 错标（多哪类：______）　备注：


---

## 汇总（判完填这里）

| 组 | 份数 | 准确 | 漏标 | 错标 | 备注 |
|---|---|---|---|---|---|
| 主样本（随机，代表全体） | 55 | | | | |
| 中文普查 | 26 | | | | |
| 边界件 | 10 | | | | |
| 稀有类加成 | 9 | | | | |

按类别的漏标/错标明细（可选，有具体例子时填）：

| 类别 | 漏标例数 | 错标例数 | 典型例子（repo/标题） |
|---|---|---|---|
| 概览 | | | |
| 架构 | | | |
| 构建测试 | | | |
| 风格 | | | |
| 流程 | | | |
| 环境 | | | |
| 禁令 | | | |
| 坑 | | | |
| AI行为 | | | |