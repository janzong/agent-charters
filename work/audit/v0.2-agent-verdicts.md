# v0.2 人工核对 —— AI 初判（不替代人判）

> 生成：2026-09-11｜Codex（AI）｜**早于**用户盲判（`work/audit/blind10.md` 仍空白）
> 判据材料：`work/audit/v0.2-worksheet.md`（规则分配 + 命中证据 + 标题 + 前 600 字）**＋ 对 16/40 份回到全文核对**
> 机器预筛（可复算）：`work/audit/v0.2-screen.md`、`work/audit/substring-impact.md`

## 0. 我是怎么判的，以及第一遍判错了什么（这段比结论重要）

第一遍我**只用工作表那一页**判，给出 12 条「错标」。回到全文复核后，**其中 8 条是我自己判错的**——
类别是对的，错的是抽取器列出来的证据。例如：

| 我第一遍说 | 全文实查 | 结论 |
|---|---|---|
| `Kiln` 错标：构建测试 | `### Agent Tools` 里有 `uv run pytest`、`npm run lint`、`npm run format_check` | **我错**，类别成立 |
| `career-ops` 错标：构建测试 | `## CI/CD, Community and Governance`：GitHub Actions 跑 `test-all.mjs`、分支保护 | **我错** |
| `pydantic-ai` 错标：构建测试 | `## Development workflow`：`make lint/format/typecheck`、pre-commit、CI 全量测试 | **我错** |
| `netdata` 错标：环境 | `### Per-User Secrets`、`### Local-Only Working Directory` | **我错** |
| `deepagents` 错标：风格 | `### Suppressing ruff rules`、`## Coding Guidelines`（docstring / 断句 / 美式英语） | **我错** |
| `cezar` 错标：环境 | `## Zero config` 讲的就是 `.env`/`config.json`/状态目录的配置口径 | **我错** |
| `doocs/md` 错标：风格 | `## Lint 与格式化` 就是风格章节 | **我错** |
| `DSHA` 错标：禁令 | `## 技术约束` 是环境（SDK/Gradle/NDK 版本），但同文确有禁令句 | 半错 |

**这就是"我做、你来选"的真实边界**：我能在**看得到全文**时判，也能在**只有一页证据**时给出看起来很像结论的东西——
而后者有约 2/3 的把握是错的。你"选"的那一步如果只是在我给的结论上打勾，等于把我的假阳性一起确认了。

## 1. 本次能负责的结论

- **确认的错标：4 条**（第 3 节标 ⛔）。全部**回到全文核对过**。
- **确认的漏标：6 条**（标 ⚠️）。判定标准是"标题里明摆着有这类内容、却没打标签"，不涉正文深挖。
- **其余 30 条：本次未发现错标**，其中 10 条我读了全文（标 ✅ 全文核），20 条只看了工作表那一页（标 ➖ 未核全文，
  **不代表没有漏标**）。
- 一句话：**这份文件适合当"优先复核清单"，不适合当"核对结果"**。

## 2. 一个已实测的抽取器缺陷：标题关键词按子串匹配

`agent_charters/taxonomy.py` 标题通道是 `if k in heading.lower()`——**纯子串，没有词边界**：

- 511 份实质文件里，**23 处** (文件, 类别) 组合、**23 份**文件，其某个类别**只**靠这种误命中撑着；
  按类别：构建测试 16、AI行为 2、环境 2、架构/流程/风格 各 1。
- 40 份核对样本命中 **10 处**：`Kiln`、`UltraRAG`、`deepagents`、`claudectl`、`netdata`、`supercli`（Type**Script**）、
  `Weaverse/.agents`、`Arnold`、`doocs/md`（allow**Build**s）、`Weaverse/.agents`（un**commit**ted）。
- **误命中 ≠ 类别错**（见第 0 节）。修法是一处代码：`k in low_head` 改为词首匹配
  （前缀命中要保留，`convention`→`Conventions` 是规则有意为之）。
  是否改、改完要不要重发数据集，是**决策**，不是我能自己定的。

## 3. 逐条判定

> 证据等级：✅ 已核全文 ／ ➖ 只看工作表（漏标不可判）。
> 只列**有发现**或**值得复核**的条目；未列出的条目含义是"本次未发现错标"。

### 有发现

| # | 仓库 | 发现 | 证据 | 等级 |
|---|---|---|---|---|
| 主4 | OpenBMB/UltraRAG | ⛔ 流程（疑） | 命中证据是 DSL 的 `5.4 Branch block`；全文无 git/PR/发布章节（环境/构建测试/风格/禁令均成立） | ✅ |
| 主5 | QuintinShaw/pi-dynamic-workflows | ⛔ 流程 ＋ ⚠️ 构建测试、禁令 | 685B 全文：章节讲"文档该放哪"，无分支/PR/发布；同文有 `npm run context:check`、`Do not copy…` | ✅ |
| 主6 | VoltAgent/voltagent | ⚠️ AI行为 | 1519B 全文：`## Important Notes for AI Agents`（5 条行为指令）未打 agent_meta | ✅ |
| 主13 | koala73/worldmonitor | ⚠️ 概览 | 开篇即产品定位；6 个标题无概览类 | ✅ |
| 主15 | mercurialsolo/claudectl | ⛔ 环境 | 环境证据是 `Key differentiators vs. similar tools`（卖点表）；全文无环境/工具链章节 | ✅ |
| 主23 | yashdev9274/supercli | ⛔ AI行为（低置信） | 标签由文档标题 `Agent Instructions` 命中；全文无 AI 行为/语气/身份规定 | ✅ |
| 主24 | zgsm-ai/costrict | ⚠️ 坑 | 445B 全文只有一个 `SettingsView` 绑定竞态提示——正是"坑"，零标签 | ✅ |
| 主28 | jantimon/web-performance-debugger | ⚠️ 坑、环境（疑） | 主体是 wpd 工具用法与 `Reading the numbers: three traps`；证据仅来自全文规则通道 | ✅ |
| 主30 | peteromallet/Arnold | ⛔ 构建测试 | 证据 `Settled De**ci**sions ← heading:ci`；全文无构建/测试命令（流程/环境/禁令成立） | ✅ |
| 中1 | DSH-APP/DSHA | ⚠️ 坑、环境 | 有 `## 已知 trap（搬自原版…）` 与 SDK/Gradle/NDK 版本段，均未打 | ✅ |
| 中7 | aotemiao/artemis | ⚠️ 流程、架构、环境 | 有 `标准工作回路`、`先看什么`、`优先使用的脚本` 三节，全未打；只打了一个"禁令" | ✅ |

**共 11 条有发现**（4 条确认错标、6 条确认漏标、1 条低置信）。

### 已复核、未发现错标（✅ 全文核）

`Kiln-AI/Kiln`、`career-ops-hq/career-ops`、`pydantic/pydantic-ai`、`netdata/netdata`、`langchain-ai/deepagents`、
`open-mercato/cezar`、`Weaverse/.agents`（`### Writing Style` 确为 agent 语气规定，AI行为成立）、`doocs/md`。

### 只看过工作表、未核全文（➖，漏标不可判）

主样本 `1,3,7,9,10,11,12,16,17*,18*,19,20*,21,22,25,26*,27,29`、中文 `2*,3,4,5,6,8,9,10`。
（带 `*` 的即第 0 节表中"证据列可疑但类别成立"的那些。）

## 4. 我判不了的（要全文，或要你）

1. **大文件的漏标**：`wsl2-distro-manager`(67KB)、`career-ops`(52KB)、`netdata`(53KB)、`Arnold`(32KB)、`UltraRAG`(17KB)
   都没法凭 600 字定漏标。
2. **口径本身，这是最要紧的**：`风格` 收不收"Git 规范"、`环境` 收不收"SDK/版本号"、
   `构建测试` 收不收"该不该跑构建"、`AI行为` 收不收"文档标题是 Agent Instructions"——
   第 3 节里至少 6 条判定取决于这几条边界。这是**定义问题**，该由人定，定完写进 `TAXONOMY.md` 再谈准确率。
3. `gqcn/agentbox`、`linaproai/linapro`、`linaproai/linapro-site` 三份是**同族模板**
   （标题结构几乎一致；正文 8 字符 shingle Jaccard ≈ 0.18，不是重复样本），
   中文 n=10 报"单独统计"时要在 `LIMITATIONS.md` 里写明这一点。

## 5. 本次顺手修掉、加上的东西

- `work/audit_sample.py`：补上漏掉的**全文规则通道**（`classify_fulltext`）证据——原先靠该通道打上的标签在工作表里
  显示成"空证据"，会把人误导成"错标"；同时每条加上**完整原文路径**与"证据列可能错"的警示。
- `work/audit_blind.py`：盲判组改为**从工作表自动切出**（此前是手抄，会随工作表重生成而漂移）。
- `work/audit_screen.py`、`work/substring_audit.py`：证据分级 + 子串误命中标记，语料库级影响可复算。
