# 盲判组 20 份：进度与中间结果（**未裁决，不得引用**）

> 协议：第一遍盲判（只看原文，不看规则输出）→ 第二遍对照 → 第三遍回原文裁决（`规则错` / `人错` / `口径不清`）。
> **本文件停在第二遍**。下面的数字是**待裁决的分歧数**，不是准确率。

## 进度

| 阶段 | 状态 |
|---|---|
| 第一遍盲判 | 10 / 20（v0.4 盲判组里较小的 10 份） |
| 第二遍对照 | ✅ 已完成（10 份） |
| 第三遍裁决 | ⬜ 未开始 |
| 你的 20 份盲判（算 κ 用） | ⬜ 等你 |

## 第二遍对照结果（10 份 × 9 类 = 90 格）

| | 处数 |
|---|---|
| 一致 | **32** |
| 规则有、我没有（候选**错标**） | **4** |
| 我有、规则没有（候选**漏标**） | **21** |

按类别看候选漏标：架构 4、AI行为 4、环境 3、禁令 3、风格 2、构建测试 2、流程 2、概览 1。
候选错标：流程 2、构建测试 1、禁令 1。

**最粗的读法**（若以盲判为真）：precision ≈ 89%、recall ≈ 60%。方向与项目既有认知一致
（"宁可漏标不做错标"确实实现了），但 **recall 比我原先估计的低得多**。

## 为什么这还不是结论

1. **裁决没做**：分歧里混着三类东西——真漏标、我判得太宽、定义边界不清（新版口径裁决第 2、3 条
   就明确"环境收版本号、构建测试收该不该跑"，会改变这几格的判定）。
   上次同类工作的教训：`work/audit/v0.2-agent-verdicts.md` §0——我拿一页证据判出的 12 条"错标"里
   **8 条是我自己判错的**。这次方向相反（我判"漏标"），出错的风险一样存在。
2. **只有 10 份**，且是盲判组里**最小的** 10 份（选小文件是为了先跑通流程）。小文件更容易漏掉
   "架构/概览"这类需要体量的信号，可能**系统性高估**漏标率。
3. 判者是我（规则作者）。κ 未算——用户盲判的 20 份与这 20 份重叠，κ 出来才知道这套判定本身的可信度。

## 待办

- [ ] 我判完剩余 10 份（含 154KB 的 `backnotprop/plannotator`）
- [ ] 第三遍裁决 25 处分歧，逐条写理由与原文引用
- [ ] 你交 20 份 → 算 Cohen's κ
- [ ] 再跑主样本剩余 45 份 + 中文普查 26 + 边界件 10 + 稀有加成 9
